import { Metadata, Status, ClientError } from 'nice-grpc-common';
import { BinaryWriter, BinaryReader } from '@bufbuild/protobuf/wire';
import { ChannelCredentials, createChannel, createClientFactory } from 'nice-grpc';

// src/errors/error-handler.ts

// src/utils/grpc-metadata.ts
function getMetadata(error) {
  return error.metadata;
}
function toStringValues(value) {
  if (Array.isArray(value)) {
    return value.flatMap(toStringValues);
  }
  return typeof value === "string" ? [value] : [];
}
function* iterateMetadataEntries(metadata) {
  const iterator = metadata[Symbol.iterator];
  if (typeof iterator === "function") {
    for (const entry of iterator.call(metadata)) {
      if (!Array.isArray(entry) || entry.length < 2) {
        continue;
      }
      const [key, value] = entry;
      if (typeof key !== "string") {
        continue;
      }
      yield [key, toStringValues(value)];
    }
    return;
  }
  if (typeof metadata.toJSON === "function") {
    for (const [key, value] of Object.entries(metadata.toJSON())) {
      yield [key, toStringValues(value)];
    }
    return;
  }
  if (typeof metadata.getMap === "function") {
    for (const [key, value] of Object.entries(metadata.getMap())) {
      yield [key, toStringValues(value)];
    }
  }
}
function readMetadataValues(error, key) {
  const metadata = getMetadata(error);
  if (!metadata) {
    return [];
  }
  if (typeof metadata.getAll === "function") {
    return toStringValues(metadata.getAll(key));
  }
  if (typeof metadata.get === "function") {
    return toStringValues(metadata.get(key));
  }
  return [];
}
function readMetadataValue(error, key) {
  return readMetadataValues(error, key)[0];
}
function readMetadataPrefixedEntries(error, prefix) {
  const metadata = getMetadata(error);
  if (!metadata) {
    return {};
  }
  const entries = {};
  for (const [key, values] of iterateMetadataEntries(metadata)) {
    if (!key.startsWith(prefix) || values.length === 0) {
      continue;
    }
    const suffix = key.slice(prefix.length);
    if (suffix.length === 0) {
      continue;
    }
    const firstValue = values[0];
    if (firstValue === void 0) {
      continue;
    }
    entries[suffix] = firstValue;
  }
  return entries;
}

// src/errors/imessage-error.ts
var IMessageError = class extends Error {
  code;
  retryable;
  grpcCode;
  context;
  constructor(message, options) {
    super(message, { cause: options.cause });
    this.name = "IMessageError";
    this.code = options.code;
    this.retryable = options.retryable;
    this.grpcCode = options.grpcCode;
    this.context = options.context ?? {};
  }
};
var AuthenticationError = class extends IMessageError {
  constructor(message, options) {
    super(message, options);
    this.name = "AuthenticationError";
  }
};
var NotFoundError = class extends IMessageError {
  constructor(message, options) {
    super(message, options);
    this.name = "NotFoundError";
  }
};
var RateLimitError = class extends IMessageError {
  constructor(message, options) {
    super(message, options);
    this.name = "RateLimitError";
  }
};
var ValidationError = class extends IMessageError {
  constructor(message, options) {
    super(message, options);
    this.name = "ValidationError";
  }
};
var ConnectionError = class extends IMessageError {
  constructor(message, options) {
    super(message, options);
    this.name = "ConnectionError";
  }
};

// src/errors/error-handler.ts
function fromGrpcError(error) {
  if (error instanceof IMessageError) {
    return error;
  }
  const isClientError = error instanceof ClientError;
  let grpcCode;
  if (isClientError) {
    grpcCode = error.code;
  } else if (typeof error.code === "number") {
    grpcCode = error.code;
  } else {
    grpcCode = Status.UNKNOWN;
  }
  let details;
  if (isClientError) {
    details = error.details;
  } else if (typeof error.details === "string") {
    details = error.details;
  } else if (error instanceof Error) {
    details = error.message;
  } else {
    details = String(error);
  }
  const errorCode = readMetadataValue(error, "error-code") ?? "internalError";
  const retryable = readMetadataValue(error, "x-retryable") === "true";
  const context = readMetadataPrefixedEntries(error, "error-context-");
  const cause = error instanceof Error ? error : void 0;
  const options = {
    code: errorCode,
    context,
    retryable,
    grpcCode,
    cause
  };
  switch (grpcCode) {
    case Status.UNAUTHENTICATED:
    case Status.PERMISSION_DENIED:
      return new AuthenticationError(details, options);
    case Status.NOT_FOUND:
      return new NotFoundError(details, options);
    case Status.RESOURCE_EXHAUSTED:
      return new RateLimitError(details, options);
    case Status.INVALID_ARGUMENT:
    case Status.FAILED_PRECONDITION:
      return new ValidationError(details, options);
    case Status.UNAVAILABLE:
    case Status.DEADLINE_EXCEEDED:
      return new ConnectionError(details, options);
    default:
      return new IMessageError(details, options);
  }
}
function chatServiceTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "CHAT_SERVICE_TYPE_UNSPECIFIED":
      return 0 /* CHAT_SERVICE_TYPE_UNSPECIFIED */;
    case 1:
    case "CHAT_SERVICE_TYPE_IMESSAGE":
      return 1 /* CHAT_SERVICE_TYPE_IMESSAGE */;
    case 2:
    case "CHAT_SERVICE_TYPE_SMS":
      return 2 /* CHAT_SERVICE_TYPE_SMS */;
    case 3:
    case "CHAT_SERVICE_TYPE_RCS":
      return 3 /* CHAT_SERVICE_TYPE_RCS */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function chatServiceTypeToJSON(object) {
  switch (object) {
    case 0 /* CHAT_SERVICE_TYPE_UNSPECIFIED */:
      return "CHAT_SERVICE_TYPE_UNSPECIFIED";
    case 1 /* CHAT_SERVICE_TYPE_IMESSAGE */:
      return "CHAT_SERVICE_TYPE_IMESSAGE";
    case 2 /* CHAT_SERVICE_TYPE_SMS */:
      return "CHAT_SERVICE_TYPE_SMS";
    case 3 /* CHAT_SERVICE_TYPE_RCS */:
      return "CHAT_SERVICE_TYPE_RCS";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseSingleServiceAddressInfo() {
  return { address: "", service: 0, country: void 0 };
}
var SingleServiceAddressInfo = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.service !== 0) {
      writer.uint32(16).int32(message.service);
    }
    if (message.country !== void 0) {
      writer.uint32(26).string(message.country);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSingleServiceAddressInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.service = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.country = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      service: isSet(object.service) ? chatServiceTypeFromJSON(object.service) : 0,
      country: isSet(object.country) ? globalThis.String(object.country) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.service !== 0) {
      obj.service = chatServiceTypeToJSON(message.service);
    }
    if (message.country !== void 0) {
      obj.country = message.country;
    }
    return obj;
  },
  create(base) {
    return SingleServiceAddressInfo.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSingleServiceAddressInfo();
    message.address = object.address ?? "";
    message.service = object.service ?? 0;
    message.country = object.country ?? void 0;
    return message;
  }
};
function createBaseMultiServiceAddressInfo() {
  return { address: "", services: [], country: void 0 };
}
var MultiServiceAddressInfo = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    writer.uint32(18).fork();
    for (const v of message.services) {
      writer.int32(v);
    }
    writer.join();
    if (message.country !== void 0) {
      writer.uint32(26).string(message.country);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMultiServiceAddressInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.services.push(reader.int32());
            continue;
          }
          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.services.push(reader.int32());
            }
            continue;
          }
          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.country = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      services: globalThis.Array.isArray(object?.services) ? object.services.map((e) => chatServiceTypeFromJSON(e)) : [],
      country: isSet(object.country) ? globalThis.String(object.country) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.services?.length) {
      obj.services = message.services.map((e) => chatServiceTypeToJSON(e));
    }
    if (message.country !== void 0) {
      obj.country = message.country;
    }
    return obj;
  },
  create(base) {
    return MultiServiceAddressInfo.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMultiServiceAddressInfo();
    message.address = object.address ?? "";
    message.services = object.services?.map((e) => e) || [];
    message.country = object.country ?? void 0;
    return message;
  }
};
function isSet(value) {
  return value !== null && value !== void 0;
}
function transferStateFromJSON(object) {
  switch (object) {
    case 0:
    case "TRANSFER_STATE_PENDING":
      return 0 /* TRANSFER_STATE_PENDING */;
    case -1:
    case "TRANSFER_STATE_UNAVAILABLE":
      return -1 /* TRANSFER_STATE_UNAVAILABLE */;
    case 1:
    case "TRANSFER_STATE_TRANSFERRING":
      return 1 /* TRANSFER_STATE_TRANSFERRING */;
    case 2:
    case "TRANSFER_STATE_FAILED":
      return 2 /* TRANSFER_STATE_FAILED */;
    case 5:
    case "TRANSFER_STATE_FINISHED":
      return 5 /* TRANSFER_STATE_FINISHED */;
    case 6:
    case "TRANSFER_STATE_UNKNOWN":
      return 6 /* TRANSFER_STATE_UNKNOWN */;
    default:
      return -1 /* TRANSFER_STATE_UNAVAILABLE */;
  }
}
function transferStateToJSON(object) {
  switch (object) {
    case 0 /* TRANSFER_STATE_PENDING */:
      return "TRANSFER_STATE_PENDING";
    case -1 /* TRANSFER_STATE_UNAVAILABLE */:
      return "TRANSFER_STATE_UNAVAILABLE";
    case 1 /* TRANSFER_STATE_TRANSFERRING */:
      return "TRANSFER_STATE_TRANSFERRING";
    case 2 /* TRANSFER_STATE_FAILED */:
      return "TRANSFER_STATE_FAILED";
    case 5 /* TRANSFER_STATE_FINISHED */:
      return "TRANSFER_STATE_FINISHED";
    case 6 /* TRANSFER_STATE_UNKNOWN */:
      return "TRANSFER_STATE_UNKNOWN";
    default:
      return "TRANSFER_STATE_UNAVAILABLE";
  }
}
function companionKindFromJSON(object) {
  switch (object) {
    case 0:
    case "COMPANION_KIND_UNSPECIFIED":
      return 0 /* COMPANION_KIND_UNSPECIFIED */;
    case 1:
    case "COMPANION_KIND_LIVE_PHOTO_VIDEO":
      return 1 /* COMPANION_KIND_LIVE_PHOTO_VIDEO */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function companionKindToJSON(object) {
  switch (object) {
    case 0 /* COMPANION_KIND_UNSPECIFIED */:
      return "COMPANION_KIND_UNSPECIFIED";
    case 1 /* COMPANION_KIND_LIVE_PHOTO_VIDEO */:
      return "COMPANION_KIND_LIVE_PHOTO_VIDEO";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseAttachmentInfo() {
  return {
    guid: "",
    originalGuid: void 0,
    fileName: "",
    mimeType: "",
    uti: "",
    totalBytes: 0,
    isOutgoing: false,
    transferState: 0,
    isHidden: false,
    isSticker: false,
    companionKind: void 0
  };
}
var AttachmentInfo = {
  encode(message, writer = new BinaryWriter()) {
    if (message.guid !== "") {
      writer.uint32(10).string(message.guid);
    }
    if (message.originalGuid !== void 0) {
      writer.uint32(18).string(message.originalGuid);
    }
    if (message.fileName !== "") {
      writer.uint32(26).string(message.fileName);
    }
    if (message.mimeType !== "") {
      writer.uint32(34).string(message.mimeType);
    }
    if (message.uti !== "") {
      writer.uint32(42).string(message.uti);
    }
    if (message.totalBytes !== 0) {
      writer.uint32(48).int64(message.totalBytes);
    }
    if (message.isOutgoing !== false) {
      writer.uint32(56).bool(message.isOutgoing);
    }
    if (message.transferState !== 0) {
      writer.uint32(64).int32(message.transferState);
    }
    if (message.isHidden !== false) {
      writer.uint32(72).bool(message.isHidden);
    }
    if (message.isSticker !== false) {
      writer.uint32(80).bool(message.isSticker);
    }
    if (message.companionKind !== void 0) {
      writer.uint32(88).int32(message.companionKind);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAttachmentInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.originalGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.fileName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.mimeType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.uti = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.totalBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.isOutgoing = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }
          message.transferState = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }
          message.isHidden = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }
          message.isSticker = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }
          message.companionKind = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      guid: isSet2(object.guid) ? globalThis.String(object.guid) : "",
      originalGuid: isSet2(object.originalGuid) ? globalThis.String(object.originalGuid) : isSet2(object.original_guid) ? globalThis.String(object.original_guid) : void 0,
      fileName: isSet2(object.fileName) ? globalThis.String(object.fileName) : isSet2(object.file_name) ? globalThis.String(object.file_name) : "",
      mimeType: isSet2(object.mimeType) ? globalThis.String(object.mimeType) : isSet2(object.mime_type) ? globalThis.String(object.mime_type) : "",
      uti: isSet2(object.uti) ? globalThis.String(object.uti) : "",
      totalBytes: isSet2(object.totalBytes) ? globalThis.Number(object.totalBytes) : isSet2(object.total_bytes) ? globalThis.Number(object.total_bytes) : 0,
      isOutgoing: isSet2(object.isOutgoing) ? globalThis.Boolean(object.isOutgoing) : isSet2(object.is_outgoing) ? globalThis.Boolean(object.is_outgoing) : false,
      transferState: isSet2(object.transferState) ? transferStateFromJSON(object.transferState) : isSet2(object.transfer_state) ? transferStateFromJSON(object.transfer_state) : 0,
      isHidden: isSet2(object.isHidden) ? globalThis.Boolean(object.isHidden) : isSet2(object.is_hidden) ? globalThis.Boolean(object.is_hidden) : false,
      isSticker: isSet2(object.isSticker) ? globalThis.Boolean(object.isSticker) : isSet2(object.is_sticker) ? globalThis.Boolean(object.is_sticker) : false,
      companionKind: isSet2(object.companionKind) ? companionKindFromJSON(object.companionKind) : isSet2(object.companion_kind) ? companionKindFromJSON(object.companion_kind) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.guid !== "") {
      obj.guid = message.guid;
    }
    if (message.originalGuid !== void 0) {
      obj.originalGuid = message.originalGuid;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.uti !== "") {
      obj.uti = message.uti;
    }
    if (message.totalBytes !== 0) {
      obj.totalBytes = Math.round(message.totalBytes);
    }
    if (message.isOutgoing !== false) {
      obj.isOutgoing = message.isOutgoing;
    }
    if (message.transferState !== 0) {
      obj.transferState = transferStateToJSON(message.transferState);
    }
    if (message.isHidden !== false) {
      obj.isHidden = message.isHidden;
    }
    if (message.isSticker !== false) {
      obj.isSticker = message.isSticker;
    }
    if (message.companionKind !== void 0) {
      obj.companionKind = companionKindToJSON(message.companionKind);
    }
    return obj;
  },
  create(base) {
    return AttachmentInfo.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAttachmentInfo();
    message.guid = object.guid ?? "";
    message.originalGuid = object.originalGuid ?? void 0;
    message.fileName = object.fileName ?? "";
    message.mimeType = object.mimeType ?? "";
    message.uti = object.uti ?? "";
    message.totalBytes = object.totalBytes ?? 0;
    message.isOutgoing = object.isOutgoing ?? false;
    message.transferState = object.transferState ?? 0;
    message.isHidden = object.isHidden ?? false;
    message.isSticker = object.isSticker ?? false;
    message.companionKind = object.companionKind ?? void 0;
    return message;
  }
};
function createBaseCompanionInfo() {
  return { fileName: "", mimeType: "", totalBytes: 0, kind: 0 };
}
var CompanionInfo = {
  encode(message, writer = new BinaryWriter()) {
    if (message.fileName !== "") {
      writer.uint32(10).string(message.fileName);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    if (message.totalBytes !== 0) {
      writer.uint32(24).int64(message.totalBytes);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCompanionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.fileName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.mimeType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.totalBytes = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.kind = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      fileName: isSet2(object.fileName) ? globalThis.String(object.fileName) : isSet2(object.file_name) ? globalThis.String(object.file_name) : "",
      mimeType: isSet2(object.mimeType) ? globalThis.String(object.mimeType) : isSet2(object.mime_type) ? globalThis.String(object.mime_type) : "",
      totalBytes: isSet2(object.totalBytes) ? globalThis.Number(object.totalBytes) : isSet2(object.total_bytes) ? globalThis.Number(object.total_bytes) : 0,
      kind: isSet2(object.kind) ? companionKindFromJSON(object.kind) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.totalBytes !== 0) {
      obj.totalBytes = Math.round(message.totalBytes);
    }
    if (message.kind !== 0) {
      obj.kind = companionKindToJSON(message.kind);
    }
    return obj;
  },
  create(base) {
    return CompanionInfo.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCompanionInfo();
    message.fileName = object.fileName ?? "";
    message.mimeType = object.mimeType ?? "";
    message.totalBytes = object.totalBytes ?? 0;
    message.kind = object.kind ?? 0;
    return message;
  }
};
function createBaseCompanion() {
  return { data: new Uint8Array(0), kind: 0 };
}
var Companion = {
  encode(message, writer = new BinaryWriter()) {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.kind !== 0) {
      writer.uint32(16).int32(message.kind);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCompanion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.kind = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      data: isSet2(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      kind: isSet2(object.kind) ? companionKindFromJSON(object.kind) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.kind !== 0) {
      obj.kind = companionKindToJSON(message.kind);
    }
    return obj;
  },
  create(base) {
    return Companion.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCompanion();
    message.data = object.data ?? new Uint8Array(0);
    message.kind = object.kind ?? 0;
    return message;
  }
};
function bytesFromBase64(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function longToNumber(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet2(value) {
  return value !== null && value !== void 0;
}
function createBaseTimestamp() {
  return { seconds: 0, nanos: 0 };
}
var Timestamp = {
  encode(message, writer = new BinaryWriter()) {
    if (message.seconds !== 0) {
      writer.uint32(8).int64(message.seconds);
    }
    if (message.nanos !== 0) {
      writer.uint32(16).int32(message.nanos);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTimestamp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.seconds = longToNumber2(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.nanos = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      seconds: isSet3(object.seconds) ? globalThis.Number(object.seconds) : 0,
      nanos: isSet3(object.nanos) ? globalThis.Number(object.nanos) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.seconds !== 0) {
      obj.seconds = Math.round(message.seconds);
    }
    if (message.nanos !== 0) {
      obj.nanos = Math.round(message.nanos);
    }
    return obj;
  },
  create(base) {
    return Timestamp.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTimestamp();
    message.seconds = object.seconds ?? 0;
    message.nanos = object.nanos ?? 0;
    return message;
  }
};
function longToNumber2(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet3(value) {
  return value !== null && value !== void 0;
}

// src/generated/photon/imessage/v1/location_types.ts
function friendLocationTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "FRIEND_LOCATION_TYPE_UNSPECIFIED":
      return 0 /* FRIEND_LOCATION_TYPE_UNSPECIFIED */;
    case 1:
    case "FRIEND_LOCATION_TYPE_LEGACY":
      return 1 /* FRIEND_LOCATION_TYPE_LEGACY */;
    case 2:
    case "FRIEND_LOCATION_TYPE_LIVE":
      return 2 /* FRIEND_LOCATION_TYPE_LIVE */;
    case 3:
    case "FRIEND_LOCATION_TYPE_SHALLOW":
      return 3 /* FRIEND_LOCATION_TYPE_SHALLOW */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function friendLocationTypeToJSON(object) {
  switch (object) {
    case 0 /* FRIEND_LOCATION_TYPE_UNSPECIFIED */:
      return "FRIEND_LOCATION_TYPE_UNSPECIFIED";
    case 1 /* FRIEND_LOCATION_TYPE_LEGACY */:
      return "FRIEND_LOCATION_TYPE_LEGACY";
    case 2 /* FRIEND_LOCATION_TYPE_LIVE */:
      return "FRIEND_LOCATION_TYPE_LIVE";
    case 3 /* FRIEND_LOCATION_TYPE_SHALLOW */:
      return "FRIEND_LOCATION_TYPE_SHALLOW";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseSharedFriendLocation() {
  return {
    address: "",
    name: void 0,
    latitude: void 0,
    longitude: void 0,
    accuracy: void 0,
    longAddress: void 0,
    shortAddress: void 0,
    isLocatingInProgress: false,
    locationType: 0,
    locationTimestamp: void 0,
    expiresAt: void 0
  };
}
var SharedFriendLocation = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.name !== void 0) {
      writer.uint32(18).string(message.name);
    }
    if (message.latitude !== void 0) {
      writer.uint32(25).double(message.latitude);
    }
    if (message.longitude !== void 0) {
      writer.uint32(33).double(message.longitude);
    }
    if (message.accuracy !== void 0) {
      writer.uint32(41).double(message.accuracy);
    }
    if (message.longAddress !== void 0) {
      writer.uint32(50).string(message.longAddress);
    }
    if (message.shortAddress !== void 0) {
      writer.uint32(58).string(message.shortAddress);
    }
    if (message.isLocatingInProgress !== false) {
      writer.uint32(64).bool(message.isLocatingInProgress);
    }
    if (message.locationType !== 0) {
      writer.uint32(72).int32(message.locationType);
    }
    if (message.locationTimestamp !== void 0) {
      Timestamp.encode(toTimestamp(message.locationTimestamp), writer.uint32(82).fork()).join();
    }
    if (message.expiresAt !== void 0) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(90).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSharedFriendLocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }
          message.latitude = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }
          message.longitude = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }
          message.accuracy = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.longAddress = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.shortAddress = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }
          message.isLocatingInProgress = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }
          message.locationType = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.locationTimestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet4(object.address) ? globalThis.String(object.address) : "",
      name: isSet4(object.name) ? globalThis.String(object.name) : void 0,
      latitude: isSet4(object.latitude) ? globalThis.Number(object.latitude) : void 0,
      longitude: isSet4(object.longitude) ? globalThis.Number(object.longitude) : void 0,
      accuracy: isSet4(object.accuracy) ? globalThis.Number(object.accuracy) : void 0,
      longAddress: isSet4(object.longAddress) ? globalThis.String(object.longAddress) : isSet4(object.long_address) ? globalThis.String(object.long_address) : void 0,
      shortAddress: isSet4(object.shortAddress) ? globalThis.String(object.shortAddress) : isSet4(object.short_address) ? globalThis.String(object.short_address) : void 0,
      isLocatingInProgress: isSet4(object.isLocatingInProgress) ? globalThis.Boolean(object.isLocatingInProgress) : isSet4(object.is_locating_in_progress) ? globalThis.Boolean(object.is_locating_in_progress) : false,
      locationType: isSet4(object.locationType) ? friendLocationTypeFromJSON(object.locationType) : isSet4(object.location_type) ? friendLocationTypeFromJSON(object.location_type) : 0,
      locationTimestamp: isSet4(object.locationTimestamp) ? fromJsonTimestamp(object.locationTimestamp) : isSet4(object.location_timestamp) ? fromJsonTimestamp(object.location_timestamp) : void 0,
      expiresAt: isSet4(object.expiresAt) ? fromJsonTimestamp(object.expiresAt) : isSet4(object.expires_at) ? fromJsonTimestamp(object.expires_at) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.name !== void 0) {
      obj.name = message.name;
    }
    if (message.latitude !== void 0) {
      obj.latitude = message.latitude;
    }
    if (message.longitude !== void 0) {
      obj.longitude = message.longitude;
    }
    if (message.accuracy !== void 0) {
      obj.accuracy = message.accuracy;
    }
    if (message.longAddress !== void 0) {
      obj.longAddress = message.longAddress;
    }
    if (message.shortAddress !== void 0) {
      obj.shortAddress = message.shortAddress;
    }
    if (message.isLocatingInProgress !== false) {
      obj.isLocatingInProgress = message.isLocatingInProgress;
    }
    if (message.locationType !== 0) {
      obj.locationType = friendLocationTypeToJSON(message.locationType);
    }
    if (message.locationTimestamp !== void 0) {
      obj.locationTimestamp = message.locationTimestamp.toISOString();
    }
    if (message.expiresAt !== void 0) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },
  create(base) {
    return SharedFriendLocation.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSharedFriendLocation();
    message.address = object.address ?? "";
    message.name = object.name ?? void 0;
    message.latitude = object.latitude ?? void 0;
    message.longitude = object.longitude ?? void 0;
    message.accuracy = object.accuracy ?? void 0;
    message.longAddress = object.longAddress ?? void 0;
    message.shortAddress = object.shortAddress ?? void 0;
    message.isLocatingInProgress = object.isLocatingInProgress ?? false;
    message.locationType = object.locationType ?? 0;
    message.locationTimestamp = object.locationTimestamp ?? void 0;
    message.expiresAt = object.expiresAt ?? void 0;
    return message;
  }
};
function createBaseSharedFriendLocationUpdated() {
  return { sourceSequence: 0, location: void 0 };
}
var SharedFriendLocationUpdated = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sourceSequence !== 0) {
      writer.uint32(8).uint64(message.sourceSequence);
    }
    if (message.location !== void 0) {
      SharedFriendLocation.encode(message.location, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSharedFriendLocationUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sourceSequence = longToNumber3(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.location = SharedFriendLocation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sourceSequence: isSet4(object.sourceSequence) ? globalThis.Number(object.sourceSequence) : isSet4(object.source_sequence) ? globalThis.Number(object.source_sequence) : 0,
      location: isSet4(object.location) ? SharedFriendLocation.fromJSON(object.location) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sourceSequence !== 0) {
      obj.sourceSequence = Math.round(message.sourceSequence);
    }
    if (message.location !== void 0) {
      obj.location = SharedFriendLocation.toJSON(message.location);
    }
    return obj;
  },
  create(base) {
    return SharedFriendLocationUpdated.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSharedFriendLocationUpdated();
    message.sourceSequence = object.sourceSequence ?? 0;
    message.location = object.location !== void 0 && object.location !== null ? SharedFriendLocation.fromPartial(object.location) : void 0;
    return message;
  }
};
function toTimestamp(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}
function longToNumber3(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet4(value) {
  return value !== null && value !== void 0;
}
function messageItemTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "MESSAGE_ITEM_TYPE_NORMAL":
      return 0 /* MESSAGE_ITEM_TYPE_NORMAL */;
    case 1:
    case "MESSAGE_ITEM_TYPE_PARTICIPANT_CHANGE":
      return 1 /* MESSAGE_ITEM_TYPE_PARTICIPANT_CHANGE */;
    case 2:
    case "MESSAGE_ITEM_TYPE_GROUP_NAME_CHANGE":
      return 2 /* MESSAGE_ITEM_TYPE_GROUP_NAME_CHANGE */;
    case 3:
    case "MESSAGE_ITEM_TYPE_CHAT_ACTION":
      return 3 /* MESSAGE_ITEM_TYPE_CHAT_ACTION */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function messageItemTypeToJSON(object) {
  switch (object) {
    case 0 /* MESSAGE_ITEM_TYPE_NORMAL */:
      return "MESSAGE_ITEM_TYPE_NORMAL";
    case 1 /* MESSAGE_ITEM_TYPE_PARTICIPANT_CHANGE */:
      return "MESSAGE_ITEM_TYPE_PARTICIPANT_CHANGE";
    case 2 /* MESSAGE_ITEM_TYPE_GROUP_NAME_CHANGE */:
      return "MESSAGE_ITEM_TYPE_GROUP_NAME_CHANGE";
    case 3 /* MESSAGE_ITEM_TYPE_CHAT_ACTION */:
      return "MESSAGE_ITEM_TYPE_CHAT_ACTION";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function messageReactionKindFromJSON(object) {
  switch (object) {
    case 0:
    case "MESSAGE_REACTION_KIND_LOVE":
      return 0 /* MESSAGE_REACTION_KIND_LOVE */;
    case 1:
    case "MESSAGE_REACTION_KIND_LIKE":
      return 1 /* MESSAGE_REACTION_KIND_LIKE */;
    case 2:
    case "MESSAGE_REACTION_KIND_DISLIKE":
      return 2 /* MESSAGE_REACTION_KIND_DISLIKE */;
    case 3:
    case "MESSAGE_REACTION_KIND_LAUGH":
      return 3 /* MESSAGE_REACTION_KIND_LAUGH */;
    case 4:
    case "MESSAGE_REACTION_KIND_EMPHASIZE":
      return 4 /* MESSAGE_REACTION_KIND_EMPHASIZE */;
    case 5:
    case "MESSAGE_REACTION_KIND_QUESTION":
      return 5 /* MESSAGE_REACTION_KIND_QUESTION */;
    case 6:
    case "MESSAGE_REACTION_KIND_EMOJI":
      return 6 /* MESSAGE_REACTION_KIND_EMOJI */;
    case 7:
    case "MESSAGE_REACTION_KIND_STICKER":
      return 7 /* MESSAGE_REACTION_KIND_STICKER */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function messageReactionKindToJSON(object) {
  switch (object) {
    case 0 /* MESSAGE_REACTION_KIND_LOVE */:
      return "MESSAGE_REACTION_KIND_LOVE";
    case 1 /* MESSAGE_REACTION_KIND_LIKE */:
      return "MESSAGE_REACTION_KIND_LIKE";
    case 2 /* MESSAGE_REACTION_KIND_DISLIKE */:
      return "MESSAGE_REACTION_KIND_DISLIKE";
    case 3 /* MESSAGE_REACTION_KIND_LAUGH */:
      return "MESSAGE_REACTION_KIND_LAUGH";
    case 4 /* MESSAGE_REACTION_KIND_EMPHASIZE */:
      return "MESSAGE_REACTION_KIND_EMPHASIZE";
    case 5 /* MESSAGE_REACTION_KIND_QUESTION */:
      return "MESSAGE_REACTION_KIND_QUESTION";
    case 6 /* MESSAGE_REACTION_KIND_EMOJI */:
      return "MESSAGE_REACTION_KIND_EMOJI";
    case 7 /* MESSAGE_REACTION_KIND_STICKER */:
      return "MESSAGE_REACTION_KIND_STICKER";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseMessageReaction() {
  return { kind: 0, emoji: void 0 };
}
var MessageReaction = {
  encode(message, writer = new BinaryWriter()) {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.emoji !== void 0) {
      writer.uint32(18).string(message.emoji);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageReaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.kind = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.emoji = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      kind: isSet5(object.kind) ? messageReactionKindFromJSON(object.kind) : 0,
      emoji: isSet5(object.emoji) ? globalThis.String(object.emoji) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.kind !== 0) {
      obj.kind = messageReactionKindToJSON(message.kind);
    }
    if (message.emoji !== void 0) {
      obj.emoji = message.emoji;
    }
    return obj;
  },
  create(base) {
    return MessageReaction.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageReaction();
    message.kind = object.kind ?? 0;
    message.emoji = object.emoji ?? void 0;
    return message;
  }
};
function createBaseMessageMention() {
  return { address: "", start: 0, length: 0 };
}
var MessageMention = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.start !== 0) {
      writer.uint32(16).int32(message.start);
    }
    if (message.length !== 0) {
      writer.uint32(24).int32(message.length);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageMention();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.start = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.length = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet5(object.address) ? globalThis.String(object.address) : "",
      start: isSet5(object.start) ? globalThis.Number(object.start) : 0,
      length: isSet5(object.length) ? globalThis.Number(object.length) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.start !== 0) {
      obj.start = Math.round(message.start);
    }
    if (message.length !== 0) {
      obj.length = Math.round(message.length);
    }
    return obj;
  },
  create(base) {
    return MessageMention.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageMention();
    message.address = object.address ?? "";
    message.start = object.start ?? 0;
    message.length = object.length ?? 0;
    return message;
  }
};
function createBaseTextFormat() {
  return { type: "", start: 0, length: 0, effectName: void 0 };
}
var TextFormat = {
  encode(message, writer = new BinaryWriter()) {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.start !== 0) {
      writer.uint32(16).int32(message.start);
    }
    if (message.length !== 0) {
      writer.uint32(24).int32(message.length);
    }
    if (message.effectName !== void 0) {
      writer.uint32(34).string(message.effectName);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTextFormat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.start = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.length = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.effectName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      type: isSet5(object.type) ? globalThis.String(object.type) : "",
      start: isSet5(object.start) ? globalThis.Number(object.start) : 0,
      length: isSet5(object.length) ? globalThis.Number(object.length) : 0,
      effectName: isSet5(object.effectName) ? globalThis.String(object.effectName) : isSet5(object.effect_name) ? globalThis.String(object.effect_name) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.start !== 0) {
      obj.start = Math.round(message.start);
    }
    if (message.length !== 0) {
      obj.length = Math.round(message.length);
    }
    if (message.effectName !== void 0) {
      obj.effectName = message.effectName;
    }
    return obj;
  },
  create(base) {
    return TextFormat.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTextFormat();
    message.type = object.type ?? "";
    message.start = object.start ?? 0;
    message.length = object.length ?? 0;
    message.effectName = object.effectName ?? void 0;
    return message;
  }
};
function createBaseAttachmentRef() {
  return { attachmentPath: void 0, attachmentGuid: void 0, attachmentName: void 0 };
}
var AttachmentRef = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachmentPath !== void 0) {
      writer.uint32(10).string(message.attachmentPath);
    }
    if (message.attachmentGuid !== void 0) {
      writer.uint32(18).string(message.attachmentGuid);
    }
    if (message.attachmentName !== void 0) {
      writer.uint32(26).string(message.attachmentName);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAttachmentRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachmentPath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attachmentGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.attachmentName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachmentPath: isSet5(object.attachmentPath) ? globalThis.String(object.attachmentPath) : isSet5(object.attachment_path) ? globalThis.String(object.attachment_path) : void 0,
      attachmentGuid: isSet5(object.attachmentGuid) ? globalThis.String(object.attachmentGuid) : isSet5(object.attachment_guid) ? globalThis.String(object.attachment_guid) : void 0,
      attachmentName: isSet5(object.attachmentName) ? globalThis.String(object.attachmentName) : isSet5(object.attachment_name) ? globalThis.String(object.attachment_name) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachmentPath !== void 0) {
      obj.attachmentPath = message.attachmentPath;
    }
    if (message.attachmentGuid !== void 0) {
      obj.attachmentGuid = message.attachmentGuid;
    }
    if (message.attachmentName !== void 0) {
      obj.attachmentName = message.attachmentName;
    }
    return obj;
  },
  create(base) {
    return AttachmentRef.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAttachmentRef();
    message.attachmentPath = object.attachmentPath ?? void 0;
    message.attachmentGuid = object.attachmentGuid ?? void 0;
    message.attachmentName = object.attachmentName ?? void 0;
    return message;
  }
};
function createBaseReplyTarget() {
  return { messageGuid: "", targetPartIndex: void 0 };
}
var ReplyTarget = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(16).int32(message.targetPartIndex);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseReplyTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet5(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet5(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    return obj;
  },
  create(base) {
    return ReplyTarget.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseReplyTarget();
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    return message;
  }
};
function createBaseStickerPlacement() {
  return { x: 0, y: 0, scale: void 0, rotation: void 0, width: void 0 };
}
var StickerPlacement = {
  encode(message, writer = new BinaryWriter()) {
    if (message.x !== 0) {
      writer.uint32(9).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(17).double(message.y);
    }
    if (message.scale !== void 0) {
      writer.uint32(25).double(message.scale);
    }
    if (message.rotation !== void 0) {
      writer.uint32(33).double(message.rotation);
    }
    if (message.width !== void 0) {
      writer.uint32(41).double(message.width);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStickerPlacement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }
          message.x = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }
          message.y = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }
          message.scale = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }
          message.rotation = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }
          message.width = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      x: isSet5(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet5(object.y) ? globalThis.Number(object.y) : 0,
      scale: isSet5(object.scale) ? globalThis.Number(object.scale) : void 0,
      rotation: isSet5(object.rotation) ? globalThis.Number(object.rotation) : void 0,
      width: isSet5(object.width) ? globalThis.Number(object.width) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.scale !== void 0) {
      obj.scale = message.scale;
    }
    if (message.rotation !== void 0) {
      obj.rotation = message.rotation;
    }
    if (message.width !== void 0) {
      obj.width = message.width;
    }
    return obj;
  },
  create(base) {
    return StickerPlacement.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStickerPlacement();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.scale = object.scale ?? void 0;
    message.rotation = object.rotation ?? void 0;
    message.width = object.width ?? void 0;
    return message;
  }
};
function createBaseMessagePart() {
  return {
    text: void 0,
    attachment: void 0,
    mentionedAddress: void 0,
    bubbleIndex: void 0,
    formatting: []
  };
}
var MessagePart = {
  encode(message, writer = new BinaryWriter()) {
    if (message.text !== void 0) {
      writer.uint32(10).string(message.text);
    }
    if (message.attachment !== void 0) {
      AttachmentRef.encode(message.attachment, writer.uint32(18).fork()).join();
    }
    if (message.mentionedAddress !== void 0) {
      writer.uint32(26).string(message.mentionedAddress);
    }
    if (message.bubbleIndex !== void 0) {
      writer.uint32(32).int32(message.bubbleIndex);
    }
    for (const v of message.formatting) {
      TextFormat.encode(v, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessagePart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attachment = AttachmentRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.mentionedAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.bubbleIndex = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.formatting.push(TextFormat.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      text: isSet5(object.text) ? globalThis.String(object.text) : void 0,
      attachment: isSet5(object.attachment) ? AttachmentRef.fromJSON(object.attachment) : void 0,
      mentionedAddress: isSet5(object.mentionedAddress) ? globalThis.String(object.mentionedAddress) : isSet5(object.mentioned_address) ? globalThis.String(object.mentioned_address) : void 0,
      bubbleIndex: isSet5(object.bubbleIndex) ? globalThis.Number(object.bubbleIndex) : isSet5(object.bubble_index) ? globalThis.Number(object.bubble_index) : void 0,
      formatting: globalThis.Array.isArray(object?.formatting) ? object.formatting.map((e) => TextFormat.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.text !== void 0) {
      obj.text = message.text;
    }
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentRef.toJSON(message.attachment);
    }
    if (message.mentionedAddress !== void 0) {
      obj.mentionedAddress = message.mentionedAddress;
    }
    if (message.bubbleIndex !== void 0) {
      obj.bubbleIndex = Math.round(message.bubbleIndex);
    }
    if (message.formatting?.length) {
      obj.formatting = message.formatting.map((e) => TextFormat.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return MessagePart.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessagePart();
    message.text = object.text ?? void 0;
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentRef.fromPartial(object.attachment) : void 0;
    message.mentionedAddress = object.mentionedAddress ?? void 0;
    message.bubbleIndex = object.bubbleIndex ?? void 0;
    message.formatting = object.formatting?.map((e) => TextFormat.fromPartial(e)) || [];
    return message;
  }
};
function createBaseMessageContent() {
  return {
    text: void 0,
    attachments: [],
    formatting: [],
    mentions: [],
    balloonBundleId: void 0,
    expressiveSendStyleId: void 0
  };
}
var MessageContent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.text !== void 0) {
      writer.uint32(10).string(message.text);
    }
    for (const v of message.attachments) {
      AttachmentInfo.encode(v, writer.uint32(18).fork()).join();
    }
    for (const v of message.formatting) {
      TextFormat.encode(v, writer.uint32(26).fork()).join();
    }
    for (const v of message.mentions) {
      MessageMention.encode(v, writer.uint32(34).fork()).join();
    }
    if (message.balloonBundleId !== void 0) {
      writer.uint32(42).string(message.balloonBundleId);
    }
    if (message.expressiveSendStyleId !== void 0) {
      writer.uint32(50).string(message.expressiveSendStyleId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attachments.push(AttachmentInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.formatting.push(TextFormat.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.mentions.push(MessageMention.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.balloonBundleId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.expressiveSendStyleId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      text: isSet5(object.text) ? globalThis.String(object.text) : void 0,
      attachments: globalThis.Array.isArray(object?.attachments) ? object.attachments.map((e) => AttachmentInfo.fromJSON(e)) : [],
      formatting: globalThis.Array.isArray(object?.formatting) ? object.formatting.map((e) => TextFormat.fromJSON(e)) : [],
      mentions: globalThis.Array.isArray(object?.mentions) ? object.mentions.map((e) => MessageMention.fromJSON(e)) : [],
      balloonBundleId: isSet5(object.balloonBundleId) ? globalThis.String(object.balloonBundleId) : isSet5(object.balloon_bundle_id) ? globalThis.String(object.balloon_bundle_id) : void 0,
      expressiveSendStyleId: isSet5(object.expressiveSendStyleId) ? globalThis.String(object.expressiveSendStyleId) : isSet5(object.expressive_send_style_id) ? globalThis.String(object.expressive_send_style_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.text !== void 0) {
      obj.text = message.text;
    }
    if (message.attachments?.length) {
      obj.attachments = message.attachments.map((e) => AttachmentInfo.toJSON(e));
    }
    if (message.formatting?.length) {
      obj.formatting = message.formatting.map((e) => TextFormat.toJSON(e));
    }
    if (message.mentions?.length) {
      obj.mentions = message.mentions.map((e) => MessageMention.toJSON(e));
    }
    if (message.balloonBundleId !== void 0) {
      obj.balloonBundleId = message.balloonBundleId;
    }
    if (message.expressiveSendStyleId !== void 0) {
      obj.expressiveSendStyleId = message.expressiveSendStyleId;
    }
    return obj;
  },
  create(base) {
    return MessageContent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageContent();
    message.text = object.text ?? void 0;
    message.attachments = object.attachments?.map((e) => AttachmentInfo.fromPartial(e)) || [];
    message.formatting = object.formatting?.map((e) => TextFormat.fromPartial(e)) || [];
    message.mentions = object.mentions?.map((e) => MessageMention.fromPartial(e)) || [];
    message.balloonBundleId = object.balloonBundleId ?? void 0;
    message.expressiveSendStyleId = object.expressiveSendStyleId ?? void 0;
    return message;
  }
};
function createBaseMessageAppliedReaction() {
  return {
    messageGuid: "",
    targetPartIndex: void 0,
    reaction: void 0,
    sender: void 0,
    isFromMe: false,
    dateCreated: void 0
  };
}
var MessageAppliedReaction = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(16).int32(message.targetPartIndex);
    }
    if (message.reaction !== void 0) {
      MessageReaction.encode(message.reaction, writer.uint32(26).fork()).join();
    }
    if (message.sender !== void 0) {
      SingleServiceAddressInfo.encode(message.sender, writer.uint32(34).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(40).bool(message.isFromMe);
    }
    if (message.dateCreated !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateCreated), writer.uint32(50).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageAppliedReaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.reaction = MessageReaction.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.sender = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.dateCreated = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet5(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet5(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0,
      reaction: isSet5(object.reaction) ? MessageReaction.fromJSON(object.reaction) : void 0,
      sender: isSet5(object.sender) ? SingleServiceAddressInfo.fromJSON(object.sender) : void 0,
      isFromMe: isSet5(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet5(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      dateCreated: isSet5(object.dateCreated) ? fromJsonTimestamp2(object.dateCreated) : isSet5(object.date_created) ? fromJsonTimestamp2(object.date_created) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    if (message.reaction !== void 0) {
      obj.reaction = MessageReaction.toJSON(message.reaction);
    }
    if (message.sender !== void 0) {
      obj.sender = SingleServiceAddressInfo.toJSON(message.sender);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.dateCreated !== void 0) {
      obj.dateCreated = message.dateCreated.toISOString();
    }
    return obj;
  },
  create(base) {
    return MessageAppliedReaction.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageAppliedReaction();
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    message.reaction = object.reaction !== void 0 && object.reaction !== null ? MessageReaction.fromPartial(object.reaction) : void 0;
    message.sender = object.sender !== void 0 && object.sender !== null ? SingleServiceAddressInfo.fromPartial(object.sender) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.dateCreated = object.dateCreated ?? void 0;
    return message;
  }
};
function createBaseMessagePlacedSticker() {
  return {
    messageGuid: "",
    targetPartIndex: void 0,
    sender: void 0,
    isFromMe: false,
    dateCreated: void 0,
    sticker: void 0,
    placement: void 0
  };
}
var MessagePlacedSticker = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(16).int32(message.targetPartIndex);
    }
    if (message.sender !== void 0) {
      SingleServiceAddressInfo.encode(message.sender, writer.uint32(26).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.dateCreated !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateCreated), writer.uint32(42).fork()).join();
    }
    if (message.sticker !== void 0) {
      AttachmentInfo.encode(message.sticker, writer.uint32(50).fork()).join();
    }
    if (message.placement !== void 0) {
      StickerPlacement.encode(message.placement, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessagePlacedSticker();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.sender = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.dateCreated = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.sticker = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.placement = StickerPlacement.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet5(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet5(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0,
      sender: isSet5(object.sender) ? SingleServiceAddressInfo.fromJSON(object.sender) : void 0,
      isFromMe: isSet5(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet5(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      dateCreated: isSet5(object.dateCreated) ? fromJsonTimestamp2(object.dateCreated) : isSet5(object.date_created) ? fromJsonTimestamp2(object.date_created) : void 0,
      sticker: isSet5(object.sticker) ? AttachmentInfo.fromJSON(object.sticker) : void 0,
      placement: isSet5(object.placement) ? StickerPlacement.fromJSON(object.placement) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    if (message.sender !== void 0) {
      obj.sender = SingleServiceAddressInfo.toJSON(message.sender);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.dateCreated !== void 0) {
      obj.dateCreated = message.dateCreated.toISOString();
    }
    if (message.sticker !== void 0) {
      obj.sticker = AttachmentInfo.toJSON(message.sticker);
    }
    if (message.placement !== void 0) {
      obj.placement = StickerPlacement.toJSON(message.placement);
    }
    return obj;
  },
  create(base) {
    return MessagePlacedSticker.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessagePlacedSticker();
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    message.sender = object.sender !== void 0 && object.sender !== null ? SingleServiceAddressInfo.fromPartial(object.sender) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.dateCreated = object.dateCreated ?? void 0;
    message.sticker = object.sticker !== void 0 && object.sticker !== null ? AttachmentInfo.fromPartial(object.sticker) : void 0;
    message.placement = object.placement !== void 0 && object.placement !== null ? StickerPlacement.fromPartial(object.placement) : void 0;
    return message;
  }
};
function createBaseMessage() {
  return {
    guid: "",
    content: void 0,
    subject: void 0,
    dateCreated: void 0,
    dateRead: void 0,
    dateDelivered: void 0,
    dateEdited: void 0,
    dateRetracted: void 0,
    datePlayed: void 0,
    dateExpressiveSendPlayed: void 0,
    sender: void 0,
    isFromMe: false,
    isSent: false,
    isDelivered: false,
    isDeliveredQuietly: false,
    didNotifyRecipient: false,
    sendErrorCode: 0,
    isAudioMessage: false,
    isAutoReply: false,
    isSystemMessage: false,
    isForward: false,
    isDelayed: false,
    isSpam: false,
    dataDetectorResultsPresent: false,
    isArchived: false,
    isServiceMessage: false,
    isCorrupt: false,
    isExpirable: false,
    shareStatus: void 0,
    shareDirection: void 0,
    itemType: 0,
    groupTitle: void 0,
    chatActionType: void 0,
    threadOriginatorGuid: void 0,
    threadOriginatorPart: void 0,
    replyToGuid: void 0,
    replyTargetGuid: void 0,
    destinationCallerId: void 0,
    partCount: void 0,
    cachedRoomNames: void 0,
    reactionTargetGuid: void 0,
    reactionTargetPartIndex: void 0,
    reaction: void 0,
    reactionSelected: void 0,
    appliedReactions: [],
    placedStickers: [],
    chatGuids: []
  };
}
var Message = {
  encode(message, writer = new BinaryWriter()) {
    if (message.guid !== "") {
      writer.uint32(10).string(message.guid);
    }
    if (message.content !== void 0) {
      MessageContent.encode(message.content, writer.uint32(18).fork()).join();
    }
    if (message.subject !== void 0) {
      writer.uint32(26).string(message.subject);
    }
    if (message.dateCreated !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateCreated), writer.uint32(82).fork()).join();
    }
    if (message.dateRead !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateRead), writer.uint32(90).fork()).join();
    }
    if (message.dateDelivered !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateDelivered), writer.uint32(98).fork()).join();
    }
    if (message.dateEdited !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateEdited), writer.uint32(106).fork()).join();
    }
    if (message.dateRetracted !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateRetracted), writer.uint32(114).fork()).join();
    }
    if (message.datePlayed !== void 0) {
      Timestamp.encode(toTimestamp2(message.datePlayed), writer.uint32(122).fork()).join();
    }
    if (message.dateExpressiveSendPlayed !== void 0) {
      Timestamp.encode(toTimestamp2(message.dateExpressiveSendPlayed), writer.uint32(130).fork()).join();
    }
    if (message.sender !== void 0) {
      SingleServiceAddressInfo.encode(message.sender, writer.uint32(162).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(168).bool(message.isFromMe);
    }
    if (message.isSent !== false) {
      writer.uint32(240).bool(message.isSent);
    }
    if (message.isDelivered !== false) {
      writer.uint32(248).bool(message.isDelivered);
    }
    if (message.isDeliveredQuietly !== false) {
      writer.uint32(256).bool(message.isDeliveredQuietly);
    }
    if (message.didNotifyRecipient !== false) {
      writer.uint32(264).bool(message.didNotifyRecipient);
    }
    if (message.sendErrorCode !== 0) {
      writer.uint32(272).int32(message.sendErrorCode);
    }
    if (message.isAudioMessage !== false) {
      writer.uint32(320).bool(message.isAudioMessage);
    }
    if (message.isAutoReply !== false) {
      writer.uint32(328).bool(message.isAutoReply);
    }
    if (message.isSystemMessage !== false) {
      writer.uint32(336).bool(message.isSystemMessage);
    }
    if (message.isForward !== false) {
      writer.uint32(344).bool(message.isForward);
    }
    if (message.isDelayed !== false) {
      writer.uint32(352).bool(message.isDelayed);
    }
    if (message.isSpam !== false) {
      writer.uint32(360).bool(message.isSpam);
    }
    if (message.dataDetectorResultsPresent !== false) {
      writer.uint32(368).bool(message.dataDetectorResultsPresent);
    }
    if (message.isArchived !== false) {
      writer.uint32(376).bool(message.isArchived);
    }
    if (message.isServiceMessage !== false) {
      writer.uint32(384).bool(message.isServiceMessage);
    }
    if (message.isCorrupt !== false) {
      writer.uint32(392).bool(message.isCorrupt);
    }
    if (message.isExpirable !== false) {
      writer.uint32(400).bool(message.isExpirable);
    }
    if (message.shareStatus !== void 0) {
      writer.uint32(408).int32(message.shareStatus);
    }
    if (message.shareDirection !== void 0) {
      writer.uint32(416).int32(message.shareDirection);
    }
    if (message.itemType !== 0) {
      writer.uint32(480).int32(message.itemType);
    }
    if (message.groupTitle !== void 0) {
      writer.uint32(490).string(message.groupTitle);
    }
    if (message.chatActionType !== void 0) {
      writer.uint32(496).int32(message.chatActionType);
    }
    if (message.threadOriginatorGuid !== void 0) {
      writer.uint32(570).string(message.threadOriginatorGuid);
    }
    if (message.threadOriginatorPart !== void 0) {
      writer.uint32(578).string(message.threadOriginatorPart);
    }
    if (message.replyToGuid !== void 0) {
      writer.uint32(586).string(message.replyToGuid);
    }
    if (message.replyTargetGuid !== void 0) {
      writer.uint32(594).string(message.replyTargetGuid);
    }
    if (message.destinationCallerId !== void 0) {
      writer.uint32(602).string(message.destinationCallerId);
    }
    if (message.partCount !== void 0) {
      writer.uint32(608).int32(message.partCount);
    }
    if (message.cachedRoomNames !== void 0) {
      writer.uint32(618).string(message.cachedRoomNames);
    }
    if (message.reactionTargetGuid !== void 0) {
      writer.uint32(642).string(message.reactionTargetGuid);
    }
    if (message.reactionTargetPartIndex !== void 0) {
      writer.uint32(648).int32(message.reactionTargetPartIndex);
    }
    if (message.reaction !== void 0) {
      MessageReaction.encode(message.reaction, writer.uint32(658).fork()).join();
    }
    if (message.reactionSelected !== void 0) {
      writer.uint32(664).bool(message.reactionSelected);
    }
    for (const v of message.appliedReactions) {
      MessageAppliedReaction.encode(v, writer.uint32(722).fork()).join();
    }
    for (const v of message.placedStickers) {
      MessagePlacedSticker.encode(v, writer.uint32(730).fork()).join();
    }
    for (const v of message.chatGuids) {
      writer.uint32(738).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.content = MessageContent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.dateCreated = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.dateRead = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }
          message.dateDelivered = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.dateEdited = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }
          message.dateRetracted = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }
          message.datePlayed = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }
          message.dateExpressiveSendPlayed = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }
          message.sender = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }
          message.isSent = reader.bool();
          continue;
        }
        case 31: {
          if (tag !== 248) {
            break;
          }
          message.isDelivered = reader.bool();
          continue;
        }
        case 32: {
          if (tag !== 256) {
            break;
          }
          message.isDeliveredQuietly = reader.bool();
          continue;
        }
        case 33: {
          if (tag !== 264) {
            break;
          }
          message.didNotifyRecipient = reader.bool();
          continue;
        }
        case 34: {
          if (tag !== 272) {
            break;
          }
          message.sendErrorCode = reader.int32();
          continue;
        }
        case 40: {
          if (tag !== 320) {
            break;
          }
          message.isAudioMessage = reader.bool();
          continue;
        }
        case 41: {
          if (tag !== 328) {
            break;
          }
          message.isAutoReply = reader.bool();
          continue;
        }
        case 42: {
          if (tag !== 336) {
            break;
          }
          message.isSystemMessage = reader.bool();
          continue;
        }
        case 43: {
          if (tag !== 344) {
            break;
          }
          message.isForward = reader.bool();
          continue;
        }
        case 44: {
          if (tag !== 352) {
            break;
          }
          message.isDelayed = reader.bool();
          continue;
        }
        case 45: {
          if (tag !== 360) {
            break;
          }
          message.isSpam = reader.bool();
          continue;
        }
        case 46: {
          if (tag !== 368) {
            break;
          }
          message.dataDetectorResultsPresent = reader.bool();
          continue;
        }
        case 47: {
          if (tag !== 376) {
            break;
          }
          message.isArchived = reader.bool();
          continue;
        }
        case 48: {
          if (tag !== 384) {
            break;
          }
          message.isServiceMessage = reader.bool();
          continue;
        }
        case 49: {
          if (tag !== 392) {
            break;
          }
          message.isCorrupt = reader.bool();
          continue;
        }
        case 50: {
          if (tag !== 400) {
            break;
          }
          message.isExpirable = reader.bool();
          continue;
        }
        case 51: {
          if (tag !== 408) {
            break;
          }
          message.shareStatus = reader.int32();
          continue;
        }
        case 52: {
          if (tag !== 416) {
            break;
          }
          message.shareDirection = reader.int32();
          continue;
        }
        case 60: {
          if (tag !== 480) {
            break;
          }
          message.itemType = reader.int32();
          continue;
        }
        case 61: {
          if (tag !== 490) {
            break;
          }
          message.groupTitle = reader.string();
          continue;
        }
        case 62: {
          if (tag !== 496) {
            break;
          }
          message.chatActionType = reader.int32();
          continue;
        }
        case 71: {
          if (tag !== 570) {
            break;
          }
          message.threadOriginatorGuid = reader.string();
          continue;
        }
        case 72: {
          if (tag !== 578) {
            break;
          }
          message.threadOriginatorPart = reader.string();
          continue;
        }
        case 73: {
          if (tag !== 586) {
            break;
          }
          message.replyToGuid = reader.string();
          continue;
        }
        case 74: {
          if (tag !== 594) {
            break;
          }
          message.replyTargetGuid = reader.string();
          continue;
        }
        case 75: {
          if (tag !== 602) {
            break;
          }
          message.destinationCallerId = reader.string();
          continue;
        }
        case 76: {
          if (tag !== 608) {
            break;
          }
          message.partCount = reader.int32();
          continue;
        }
        case 77: {
          if (tag !== 618) {
            break;
          }
          message.cachedRoomNames = reader.string();
          continue;
        }
        case 80: {
          if (tag !== 642) {
            break;
          }
          message.reactionTargetGuid = reader.string();
          continue;
        }
        case 81: {
          if (tag !== 648) {
            break;
          }
          message.reactionTargetPartIndex = reader.int32();
          continue;
        }
        case 82: {
          if (tag !== 658) {
            break;
          }
          message.reaction = MessageReaction.decode(reader, reader.uint32());
          continue;
        }
        case 83: {
          if (tag !== 664) {
            break;
          }
          message.reactionSelected = reader.bool();
          continue;
        }
        case 90: {
          if (tag !== 722) {
            break;
          }
          message.appliedReactions.push(MessageAppliedReaction.decode(reader, reader.uint32()));
          continue;
        }
        case 91: {
          if (tag !== 730) {
            break;
          }
          message.placedStickers.push(MessagePlacedSticker.decode(reader, reader.uint32()));
          continue;
        }
        case 92: {
          if (tag !== 738) {
            break;
          }
          message.chatGuids.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      guid: isSet5(object.guid) ? globalThis.String(object.guid) : "",
      content: isSet5(object.content) ? MessageContent.fromJSON(object.content) : void 0,
      subject: isSet5(object.subject) ? globalThis.String(object.subject) : void 0,
      dateCreated: isSet5(object.dateCreated) ? fromJsonTimestamp2(object.dateCreated) : isSet5(object.date_created) ? fromJsonTimestamp2(object.date_created) : void 0,
      dateRead: isSet5(object.dateRead) ? fromJsonTimestamp2(object.dateRead) : isSet5(object.date_read) ? fromJsonTimestamp2(object.date_read) : void 0,
      dateDelivered: isSet5(object.dateDelivered) ? fromJsonTimestamp2(object.dateDelivered) : isSet5(object.date_delivered) ? fromJsonTimestamp2(object.date_delivered) : void 0,
      dateEdited: isSet5(object.dateEdited) ? fromJsonTimestamp2(object.dateEdited) : isSet5(object.date_edited) ? fromJsonTimestamp2(object.date_edited) : void 0,
      dateRetracted: isSet5(object.dateRetracted) ? fromJsonTimestamp2(object.dateRetracted) : isSet5(object.date_retracted) ? fromJsonTimestamp2(object.date_retracted) : void 0,
      datePlayed: isSet5(object.datePlayed) ? fromJsonTimestamp2(object.datePlayed) : isSet5(object.date_played) ? fromJsonTimestamp2(object.date_played) : void 0,
      dateExpressiveSendPlayed: isSet5(object.dateExpressiveSendPlayed) ? fromJsonTimestamp2(object.dateExpressiveSendPlayed) : isSet5(object.date_expressive_send_played) ? fromJsonTimestamp2(object.date_expressive_send_played) : void 0,
      sender: isSet5(object.sender) ? SingleServiceAddressInfo.fromJSON(object.sender) : void 0,
      isFromMe: isSet5(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet5(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      isSent: isSet5(object.isSent) ? globalThis.Boolean(object.isSent) : isSet5(object.is_sent) ? globalThis.Boolean(object.is_sent) : false,
      isDelivered: isSet5(object.isDelivered) ? globalThis.Boolean(object.isDelivered) : isSet5(object.is_delivered) ? globalThis.Boolean(object.is_delivered) : false,
      isDeliveredQuietly: isSet5(object.isDeliveredQuietly) ? globalThis.Boolean(object.isDeliveredQuietly) : isSet5(object.is_delivered_quietly) ? globalThis.Boolean(object.is_delivered_quietly) : false,
      didNotifyRecipient: isSet5(object.didNotifyRecipient) ? globalThis.Boolean(object.didNotifyRecipient) : isSet5(object.did_notify_recipient) ? globalThis.Boolean(object.did_notify_recipient) : false,
      sendErrorCode: isSet5(object.sendErrorCode) ? globalThis.Number(object.sendErrorCode) : isSet5(object.send_error_code) ? globalThis.Number(object.send_error_code) : 0,
      isAudioMessage: isSet5(object.isAudioMessage) ? globalThis.Boolean(object.isAudioMessage) : isSet5(object.is_audio_message) ? globalThis.Boolean(object.is_audio_message) : false,
      isAutoReply: isSet5(object.isAutoReply) ? globalThis.Boolean(object.isAutoReply) : isSet5(object.is_auto_reply) ? globalThis.Boolean(object.is_auto_reply) : false,
      isSystemMessage: isSet5(object.isSystemMessage) ? globalThis.Boolean(object.isSystemMessage) : isSet5(object.is_system_message) ? globalThis.Boolean(object.is_system_message) : false,
      isForward: isSet5(object.isForward) ? globalThis.Boolean(object.isForward) : isSet5(object.is_forward) ? globalThis.Boolean(object.is_forward) : false,
      isDelayed: isSet5(object.isDelayed) ? globalThis.Boolean(object.isDelayed) : isSet5(object.is_delayed) ? globalThis.Boolean(object.is_delayed) : false,
      isSpam: isSet5(object.isSpam) ? globalThis.Boolean(object.isSpam) : isSet5(object.is_spam) ? globalThis.Boolean(object.is_spam) : false,
      dataDetectorResultsPresent: isSet5(object.dataDetectorResultsPresent) ? globalThis.Boolean(object.dataDetectorResultsPresent) : isSet5(object.data_detector_results_present) ? globalThis.Boolean(object.data_detector_results_present) : false,
      isArchived: isSet5(object.isArchived) ? globalThis.Boolean(object.isArchived) : isSet5(object.is_archived) ? globalThis.Boolean(object.is_archived) : false,
      isServiceMessage: isSet5(object.isServiceMessage) ? globalThis.Boolean(object.isServiceMessage) : isSet5(object.is_service_message) ? globalThis.Boolean(object.is_service_message) : false,
      isCorrupt: isSet5(object.isCorrupt) ? globalThis.Boolean(object.isCorrupt) : isSet5(object.is_corrupt) ? globalThis.Boolean(object.is_corrupt) : false,
      isExpirable: isSet5(object.isExpirable) ? globalThis.Boolean(object.isExpirable) : isSet5(object.is_expirable) ? globalThis.Boolean(object.is_expirable) : false,
      shareStatus: isSet5(object.shareStatus) ? globalThis.Number(object.shareStatus) : isSet5(object.share_status) ? globalThis.Number(object.share_status) : void 0,
      shareDirection: isSet5(object.shareDirection) ? globalThis.Number(object.shareDirection) : isSet5(object.share_direction) ? globalThis.Number(object.share_direction) : void 0,
      itemType: isSet5(object.itemType) ? messageItemTypeFromJSON(object.itemType) : isSet5(object.item_type) ? messageItemTypeFromJSON(object.item_type) : 0,
      groupTitle: isSet5(object.groupTitle) ? globalThis.String(object.groupTitle) : isSet5(object.group_title) ? globalThis.String(object.group_title) : void 0,
      chatActionType: isSet5(object.chatActionType) ? globalThis.Number(object.chatActionType) : isSet5(object.chat_action_type) ? globalThis.Number(object.chat_action_type) : void 0,
      threadOriginatorGuid: isSet5(object.threadOriginatorGuid) ? globalThis.String(object.threadOriginatorGuid) : isSet5(object.thread_originator_guid) ? globalThis.String(object.thread_originator_guid) : void 0,
      threadOriginatorPart: isSet5(object.threadOriginatorPart) ? globalThis.String(object.threadOriginatorPart) : isSet5(object.thread_originator_part) ? globalThis.String(object.thread_originator_part) : void 0,
      replyToGuid: isSet5(object.replyToGuid) ? globalThis.String(object.replyToGuid) : isSet5(object.reply_to_guid) ? globalThis.String(object.reply_to_guid) : void 0,
      replyTargetGuid: isSet5(object.replyTargetGuid) ? globalThis.String(object.replyTargetGuid) : isSet5(object.reply_target_guid) ? globalThis.String(object.reply_target_guid) : void 0,
      destinationCallerId: isSet5(object.destinationCallerId) ? globalThis.String(object.destinationCallerId) : isSet5(object.destination_caller_id) ? globalThis.String(object.destination_caller_id) : void 0,
      partCount: isSet5(object.partCount) ? globalThis.Number(object.partCount) : isSet5(object.part_count) ? globalThis.Number(object.part_count) : void 0,
      cachedRoomNames: isSet5(object.cachedRoomNames) ? globalThis.String(object.cachedRoomNames) : isSet5(object.cached_room_names) ? globalThis.String(object.cached_room_names) : void 0,
      reactionTargetGuid: isSet5(object.reactionTargetGuid) ? globalThis.String(object.reactionTargetGuid) : isSet5(object.reaction_target_guid) ? globalThis.String(object.reaction_target_guid) : void 0,
      reactionTargetPartIndex: isSet5(object.reactionTargetPartIndex) ? globalThis.Number(object.reactionTargetPartIndex) : isSet5(object.reaction_target_part_index) ? globalThis.Number(object.reaction_target_part_index) : void 0,
      reaction: isSet5(object.reaction) ? MessageReaction.fromJSON(object.reaction) : void 0,
      reactionSelected: isSet5(object.reactionSelected) ? globalThis.Boolean(object.reactionSelected) : isSet5(object.reaction_selected) ? globalThis.Boolean(object.reaction_selected) : void 0,
      appliedReactions: globalThis.Array.isArray(object?.appliedReactions) ? object.appliedReactions.map((e) => MessageAppliedReaction.fromJSON(e)) : globalThis.Array.isArray(object?.applied_reactions) ? object.applied_reactions.map((e) => MessageAppliedReaction.fromJSON(e)) : [],
      placedStickers: globalThis.Array.isArray(object?.placedStickers) ? object.placedStickers.map((e) => MessagePlacedSticker.fromJSON(e)) : globalThis.Array.isArray(object?.placed_stickers) ? object.placed_stickers.map((e) => MessagePlacedSticker.fromJSON(e)) : [],
      chatGuids: globalThis.Array.isArray(object?.chatGuids) ? object.chatGuids.map((e) => globalThis.String(e)) : globalThis.Array.isArray(object?.chat_guids) ? object.chat_guids.map((e) => globalThis.String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.guid !== "") {
      obj.guid = message.guid;
    }
    if (message.content !== void 0) {
      obj.content = MessageContent.toJSON(message.content);
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    if (message.dateCreated !== void 0) {
      obj.dateCreated = message.dateCreated.toISOString();
    }
    if (message.dateRead !== void 0) {
      obj.dateRead = message.dateRead.toISOString();
    }
    if (message.dateDelivered !== void 0) {
      obj.dateDelivered = message.dateDelivered.toISOString();
    }
    if (message.dateEdited !== void 0) {
      obj.dateEdited = message.dateEdited.toISOString();
    }
    if (message.dateRetracted !== void 0) {
      obj.dateRetracted = message.dateRetracted.toISOString();
    }
    if (message.datePlayed !== void 0) {
      obj.datePlayed = message.datePlayed.toISOString();
    }
    if (message.dateExpressiveSendPlayed !== void 0) {
      obj.dateExpressiveSendPlayed = message.dateExpressiveSendPlayed.toISOString();
    }
    if (message.sender !== void 0) {
      obj.sender = SingleServiceAddressInfo.toJSON(message.sender);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.isSent !== false) {
      obj.isSent = message.isSent;
    }
    if (message.isDelivered !== false) {
      obj.isDelivered = message.isDelivered;
    }
    if (message.isDeliveredQuietly !== false) {
      obj.isDeliveredQuietly = message.isDeliveredQuietly;
    }
    if (message.didNotifyRecipient !== false) {
      obj.didNotifyRecipient = message.didNotifyRecipient;
    }
    if (message.sendErrorCode !== 0) {
      obj.sendErrorCode = Math.round(message.sendErrorCode);
    }
    if (message.isAudioMessage !== false) {
      obj.isAudioMessage = message.isAudioMessage;
    }
    if (message.isAutoReply !== false) {
      obj.isAutoReply = message.isAutoReply;
    }
    if (message.isSystemMessage !== false) {
      obj.isSystemMessage = message.isSystemMessage;
    }
    if (message.isForward !== false) {
      obj.isForward = message.isForward;
    }
    if (message.isDelayed !== false) {
      obj.isDelayed = message.isDelayed;
    }
    if (message.isSpam !== false) {
      obj.isSpam = message.isSpam;
    }
    if (message.dataDetectorResultsPresent !== false) {
      obj.dataDetectorResultsPresent = message.dataDetectorResultsPresent;
    }
    if (message.isArchived !== false) {
      obj.isArchived = message.isArchived;
    }
    if (message.isServiceMessage !== false) {
      obj.isServiceMessage = message.isServiceMessage;
    }
    if (message.isCorrupt !== false) {
      obj.isCorrupt = message.isCorrupt;
    }
    if (message.isExpirable !== false) {
      obj.isExpirable = message.isExpirable;
    }
    if (message.shareStatus !== void 0) {
      obj.shareStatus = Math.round(message.shareStatus);
    }
    if (message.shareDirection !== void 0) {
      obj.shareDirection = Math.round(message.shareDirection);
    }
    if (message.itemType !== 0) {
      obj.itemType = messageItemTypeToJSON(message.itemType);
    }
    if (message.groupTitle !== void 0) {
      obj.groupTitle = message.groupTitle;
    }
    if (message.chatActionType !== void 0) {
      obj.chatActionType = Math.round(message.chatActionType);
    }
    if (message.threadOriginatorGuid !== void 0) {
      obj.threadOriginatorGuid = message.threadOriginatorGuid;
    }
    if (message.threadOriginatorPart !== void 0) {
      obj.threadOriginatorPart = message.threadOriginatorPart;
    }
    if (message.replyToGuid !== void 0) {
      obj.replyToGuid = message.replyToGuid;
    }
    if (message.replyTargetGuid !== void 0) {
      obj.replyTargetGuid = message.replyTargetGuid;
    }
    if (message.destinationCallerId !== void 0) {
      obj.destinationCallerId = message.destinationCallerId;
    }
    if (message.partCount !== void 0) {
      obj.partCount = Math.round(message.partCount);
    }
    if (message.cachedRoomNames !== void 0) {
      obj.cachedRoomNames = message.cachedRoomNames;
    }
    if (message.reactionTargetGuid !== void 0) {
      obj.reactionTargetGuid = message.reactionTargetGuid;
    }
    if (message.reactionTargetPartIndex !== void 0) {
      obj.reactionTargetPartIndex = Math.round(message.reactionTargetPartIndex);
    }
    if (message.reaction !== void 0) {
      obj.reaction = MessageReaction.toJSON(message.reaction);
    }
    if (message.reactionSelected !== void 0) {
      obj.reactionSelected = message.reactionSelected;
    }
    if (message.appliedReactions?.length) {
      obj.appliedReactions = message.appliedReactions.map((e) => MessageAppliedReaction.toJSON(e));
    }
    if (message.placedStickers?.length) {
      obj.placedStickers = message.placedStickers.map((e) => MessagePlacedSticker.toJSON(e));
    }
    if (message.chatGuids?.length) {
      obj.chatGuids = message.chatGuids;
    }
    return obj;
  },
  create(base) {
    return Message.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessage();
    message.guid = object.guid ?? "";
    message.content = object.content !== void 0 && object.content !== null ? MessageContent.fromPartial(object.content) : void 0;
    message.subject = object.subject ?? void 0;
    message.dateCreated = object.dateCreated ?? void 0;
    message.dateRead = object.dateRead ?? void 0;
    message.dateDelivered = object.dateDelivered ?? void 0;
    message.dateEdited = object.dateEdited ?? void 0;
    message.dateRetracted = object.dateRetracted ?? void 0;
    message.datePlayed = object.datePlayed ?? void 0;
    message.dateExpressiveSendPlayed = object.dateExpressiveSendPlayed ?? void 0;
    message.sender = object.sender !== void 0 && object.sender !== null ? SingleServiceAddressInfo.fromPartial(object.sender) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.isSent = object.isSent ?? false;
    message.isDelivered = object.isDelivered ?? false;
    message.isDeliveredQuietly = object.isDeliveredQuietly ?? false;
    message.didNotifyRecipient = object.didNotifyRecipient ?? false;
    message.sendErrorCode = object.sendErrorCode ?? 0;
    message.isAudioMessage = object.isAudioMessage ?? false;
    message.isAutoReply = object.isAutoReply ?? false;
    message.isSystemMessage = object.isSystemMessage ?? false;
    message.isForward = object.isForward ?? false;
    message.isDelayed = object.isDelayed ?? false;
    message.isSpam = object.isSpam ?? false;
    message.dataDetectorResultsPresent = object.dataDetectorResultsPresent ?? false;
    message.isArchived = object.isArchived ?? false;
    message.isServiceMessage = object.isServiceMessage ?? false;
    message.isCorrupt = object.isCorrupt ?? false;
    message.isExpirable = object.isExpirable ?? false;
    message.shareStatus = object.shareStatus ?? void 0;
    message.shareDirection = object.shareDirection ?? void 0;
    message.itemType = object.itemType ?? 0;
    message.groupTitle = object.groupTitle ?? void 0;
    message.chatActionType = object.chatActionType ?? void 0;
    message.threadOriginatorGuid = object.threadOriginatorGuid ?? void 0;
    message.threadOriginatorPart = object.threadOriginatorPart ?? void 0;
    message.replyToGuid = object.replyToGuid ?? void 0;
    message.replyTargetGuid = object.replyTargetGuid ?? void 0;
    message.destinationCallerId = object.destinationCallerId ?? void 0;
    message.partCount = object.partCount ?? void 0;
    message.cachedRoomNames = object.cachedRoomNames ?? void 0;
    message.reactionTargetGuid = object.reactionTargetGuid ?? void 0;
    message.reactionTargetPartIndex = object.reactionTargetPartIndex ?? void 0;
    message.reaction = object.reaction !== void 0 && object.reaction !== null ? MessageReaction.fromPartial(object.reaction) : void 0;
    message.reactionSelected = object.reactionSelected ?? void 0;
    message.appliedReactions = object.appliedReactions?.map((e) => MessageAppliedReaction.fromPartial(e)) || [];
    message.placedStickers = object.placedStickers?.map((e) => MessagePlacedSticker.fromPartial(e)) || [];
    message.chatGuids = object.chatGuids?.map((e) => e) || [];
    return message;
  }
};
function createBaseEmbeddedMedia() {
  return { data: new Uint8Array(0), mimeType: "" };
}
var EmbeddedMedia = {
  encode(message, writer = new BinaryWriter()) {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEmbeddedMedia();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      data: isSet5(object.data) ? bytesFromBase642(object.data) : new Uint8Array(0),
      mimeType: isSet5(object.mimeType) ? globalThis.String(object.mimeType) : isSet5(object.mime_type) ? globalThis.String(object.mime_type) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes2(message.data);
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },
  create(base) {
    return EmbeddedMedia.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseEmbeddedMedia();
    message.data = object.data ?? new Uint8Array(0);
    message.mimeType = object.mimeType ?? "";
    return message;
  }
};
function createBaseMessageReceived() {
  return { message: void 0 };
}
var MessageReceived = {
  encode(message, writer = new BinaryWriter()) {
    if (message.message !== void 0) {
      Message.encode(message.message, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageReceived();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.message = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { message: isSet5(object.message) ? Message.fromJSON(object.message) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.message !== void 0) {
      obj.message = Message.toJSON(message.message);
    }
    return obj;
  },
  create(base) {
    return MessageReceived.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageReceived();
    message.message = object.message !== void 0 && object.message !== null ? Message.fromPartial(object.message) : void 0;
    return message;
  }
};
function createBaseMessageEdited() {
  return { messageGuid: "", content: void 0, editedAt: void 0 };
}
var MessageEdited = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.content !== void 0) {
      MessageContent.encode(message.content, writer.uint32(18).fork()).join();
    }
    if (message.editedAt !== void 0) {
      Timestamp.encode(toTimestamp2(message.editedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageEdited();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.content = MessageContent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.editedAt = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      content: isSet5(object.content) ? MessageContent.fromJSON(object.content) : void 0,
      editedAt: isSet5(object.editedAt) ? fromJsonTimestamp2(object.editedAt) : isSet5(object.edited_at) ? fromJsonTimestamp2(object.edited_at) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.content !== void 0) {
      obj.content = MessageContent.toJSON(message.content);
    }
    if (message.editedAt !== void 0) {
      obj.editedAt = message.editedAt.toISOString();
    }
    return obj;
  },
  create(base) {
    return MessageEdited.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageEdited();
    message.messageGuid = object.messageGuid ?? "";
    message.content = object.content !== void 0 && object.content !== null ? MessageContent.fromPartial(object.content) : void 0;
    message.editedAt = object.editedAt ?? void 0;
    return message;
  }
};
function createBaseMessageRead() {
  return { messageGuid: "", readAt: void 0 };
}
var MessageRead = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.readAt !== void 0) {
      Timestamp.encode(toTimestamp2(message.readAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageRead();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.readAt = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      readAt: isSet5(object.readAt) ? fromJsonTimestamp2(object.readAt) : isSet5(object.read_at) ? fromJsonTimestamp2(object.read_at) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.readAt !== void 0) {
      obj.readAt = message.readAt.toISOString();
    }
    return obj;
  },
  create(base) {
    return MessageRead.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageRead();
    message.messageGuid = object.messageGuid ?? "";
    message.readAt = object.readAt ?? void 0;
    return message;
  }
};
function createBaseMessageUnsent() {
  return { messageGuid: "", retractedAt: void 0 };
}
var MessageUnsent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.retractedAt !== void 0) {
      Timestamp.encode(toTimestamp2(message.retractedAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageUnsent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.retractedAt = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      retractedAt: isSet5(object.retractedAt) ? fromJsonTimestamp2(object.retractedAt) : isSet5(object.retracted_at) ? fromJsonTimestamp2(object.retracted_at) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.retractedAt !== void 0) {
      obj.retractedAt = message.retractedAt.toISOString();
    }
    return obj;
  },
  create(base) {
    return MessageUnsent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageUnsent();
    message.messageGuid = object.messageGuid ?? "";
    message.retractedAt = object.retractedAt ?? void 0;
    return message;
  }
};
function createBaseMessageReactionAdded() {
  return { messageGuid: "", targetPartIndex: void 0, reaction: void 0 };
}
var MessageReactionAdded = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(16).int32(message.targetPartIndex);
    }
    if (message.reaction !== void 0) {
      MessageReaction.encode(message.reaction, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageReactionAdded();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.reaction = MessageReaction.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet5(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet5(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0,
      reaction: isSet5(object.reaction) ? MessageReaction.fromJSON(object.reaction) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    if (message.reaction !== void 0) {
      obj.reaction = MessageReaction.toJSON(message.reaction);
    }
    return obj;
  },
  create(base) {
    return MessageReactionAdded.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageReactionAdded();
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    message.reaction = object.reaction !== void 0 && object.reaction !== null ? MessageReaction.fromPartial(object.reaction) : void 0;
    return message;
  }
};
function createBaseMessageReactionRemoved() {
  return { messageGuid: "", targetPartIndex: void 0, reaction: void 0 };
}
var MessageReactionRemoved = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(16).int32(message.targetPartIndex);
    }
    if (message.reaction !== void 0) {
      MessageReaction.encode(message.reaction, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageReactionRemoved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.reaction = MessageReaction.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet5(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet5(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0,
      reaction: isSet5(object.reaction) ? MessageReaction.fromJSON(object.reaction) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    if (message.reaction !== void 0) {
      obj.reaction = MessageReaction.toJSON(message.reaction);
    }
    return obj;
  },
  create(base) {
    return MessageReactionRemoved.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageReactionRemoved();
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    message.reaction = object.reaction !== void 0 && object.reaction !== null ? MessageReaction.fromPartial(object.reaction) : void 0;
    return message;
  }
};
function createBaseStickerPlaced() {
  return { messageGuid: "", targetPartIndex: void 0, sticker: void 0, placement: void 0 };
}
var StickerPlaced = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(16).int32(message.targetPartIndex);
    }
    if (message.sticker !== void 0) {
      AttachmentInfo.encode(message.sticker, writer.uint32(26).fork()).join();
    }
    if (message.placement !== void 0) {
      StickerPlacement.encode(message.placement, writer.uint32(34).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStickerPlaced();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.sticker = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.placement = StickerPlacement.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet5(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet5(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0,
      sticker: isSet5(object.sticker) ? AttachmentInfo.fromJSON(object.sticker) : void 0,
      placement: isSet5(object.placement) ? StickerPlacement.fromJSON(object.placement) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    if (message.sticker !== void 0) {
      obj.sticker = AttachmentInfo.toJSON(message.sticker);
    }
    if (message.placement !== void 0) {
      obj.placement = StickerPlacement.toJSON(message.placement);
    }
    return obj;
  },
  create(base) {
    return StickerPlaced.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStickerPlaced();
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    message.sticker = object.sticker !== void 0 && object.sticker !== null ? AttachmentInfo.fromPartial(object.sticker) : void 0;
    message.placement = object.placement !== void 0 && object.placement !== null ? StickerPlacement.fromPartial(object.placement) : void 0;
    return message;
  }
};
function createBaseMessageChangeEvent() {
  return {
    chatGuid: "",
    occurredAt: void 0,
    actor: void 0,
    isFromMe: false,
    messageReceived: void 0,
    messageEdited: void 0,
    messageRead: void 0,
    messageUnsent: void 0,
    reactionAdded: void 0,
    reactionRemoved: void 0,
    stickerPlaced: void 0
  };
}
var MessageChangeEvent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.occurredAt !== void 0) {
      Timestamp.encode(toTimestamp2(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.actor !== void 0) {
      SingleServiceAddressInfo.encode(message.actor, writer.uint32(26).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.messageReceived !== void 0) {
      MessageReceived.encode(message.messageReceived, writer.uint32(82).fork()).join();
    }
    if (message.messageEdited !== void 0) {
      MessageEdited.encode(message.messageEdited, writer.uint32(90).fork()).join();
    }
    if (message.messageRead !== void 0) {
      MessageRead.encode(message.messageRead, writer.uint32(98).fork()).join();
    }
    if (message.messageUnsent !== void 0) {
      MessageUnsent.encode(message.messageUnsent, writer.uint32(106).fork()).join();
    }
    if (message.reactionAdded !== void 0) {
      MessageReactionAdded.encode(message.reactionAdded, writer.uint32(114).fork()).join();
    }
    if (message.reactionRemoved !== void 0) {
      MessageReactionRemoved.encode(message.reactionRemoved, writer.uint32(122).fork()).join();
    }
    if (message.stickerPlaced !== void 0) {
      StickerPlaced.encode(message.stickerPlaced, writer.uint32(130).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.occurredAt = fromTimestamp2(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.actor = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.messageReceived = MessageReceived.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.messageEdited = MessageEdited.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }
          message.messageRead = MessageRead.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.messageUnsent = MessageUnsent.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }
          message.reactionAdded = MessageReactionAdded.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }
          message.reactionRemoved = MessageReactionRemoved.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }
          message.stickerPlaced = StickerPlaced.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet5(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet5(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      occurredAt: isSet5(object.occurredAt) ? fromJsonTimestamp2(object.occurredAt) : isSet5(object.occurred_at) ? fromJsonTimestamp2(object.occurred_at) : void 0,
      actor: isSet5(object.actor) ? SingleServiceAddressInfo.fromJSON(object.actor) : void 0,
      isFromMe: isSet5(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet5(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      messageReceived: isSet5(object.messageReceived) ? MessageReceived.fromJSON(object.messageReceived) : isSet5(object.message_received) ? MessageReceived.fromJSON(object.message_received) : void 0,
      messageEdited: isSet5(object.messageEdited) ? MessageEdited.fromJSON(object.messageEdited) : isSet5(object.message_edited) ? MessageEdited.fromJSON(object.message_edited) : void 0,
      messageRead: isSet5(object.messageRead) ? MessageRead.fromJSON(object.messageRead) : isSet5(object.message_read) ? MessageRead.fromJSON(object.message_read) : void 0,
      messageUnsent: isSet5(object.messageUnsent) ? MessageUnsent.fromJSON(object.messageUnsent) : isSet5(object.message_unsent) ? MessageUnsent.fromJSON(object.message_unsent) : void 0,
      reactionAdded: isSet5(object.reactionAdded) ? MessageReactionAdded.fromJSON(object.reactionAdded) : isSet5(object.reaction_added) ? MessageReactionAdded.fromJSON(object.reaction_added) : void 0,
      reactionRemoved: isSet5(object.reactionRemoved) ? MessageReactionRemoved.fromJSON(object.reactionRemoved) : isSet5(object.reaction_removed) ? MessageReactionRemoved.fromJSON(object.reaction_removed) : void 0,
      stickerPlaced: isSet5(object.stickerPlaced) ? StickerPlaced.fromJSON(object.stickerPlaced) : isSet5(object.sticker_placed) ? StickerPlaced.fromJSON(object.sticker_placed) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.occurredAt !== void 0) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.actor !== void 0) {
      obj.actor = SingleServiceAddressInfo.toJSON(message.actor);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.messageReceived !== void 0) {
      obj.messageReceived = MessageReceived.toJSON(message.messageReceived);
    }
    if (message.messageEdited !== void 0) {
      obj.messageEdited = MessageEdited.toJSON(message.messageEdited);
    }
    if (message.messageRead !== void 0) {
      obj.messageRead = MessageRead.toJSON(message.messageRead);
    }
    if (message.messageUnsent !== void 0) {
      obj.messageUnsent = MessageUnsent.toJSON(message.messageUnsent);
    }
    if (message.reactionAdded !== void 0) {
      obj.reactionAdded = MessageReactionAdded.toJSON(message.reactionAdded);
    }
    if (message.reactionRemoved !== void 0) {
      obj.reactionRemoved = MessageReactionRemoved.toJSON(message.reactionRemoved);
    }
    if (message.stickerPlaced !== void 0) {
      obj.stickerPlaced = StickerPlaced.toJSON(message.stickerPlaced);
    }
    return obj;
  },
  create(base) {
    return MessageChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageChangeEvent();
    message.chatGuid = object.chatGuid ?? "";
    message.occurredAt = object.occurredAt ?? void 0;
    message.actor = object.actor !== void 0 && object.actor !== null ? SingleServiceAddressInfo.fromPartial(object.actor) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.messageReceived = object.messageReceived !== void 0 && object.messageReceived !== null ? MessageReceived.fromPartial(object.messageReceived) : void 0;
    message.messageEdited = object.messageEdited !== void 0 && object.messageEdited !== null ? MessageEdited.fromPartial(object.messageEdited) : void 0;
    message.messageRead = object.messageRead !== void 0 && object.messageRead !== null ? MessageRead.fromPartial(object.messageRead) : void 0;
    message.messageUnsent = object.messageUnsent !== void 0 && object.messageUnsent !== null ? MessageUnsent.fromPartial(object.messageUnsent) : void 0;
    message.reactionAdded = object.reactionAdded !== void 0 && object.reactionAdded !== null ? MessageReactionAdded.fromPartial(object.reactionAdded) : void 0;
    message.reactionRemoved = object.reactionRemoved !== void 0 && object.reactionRemoved !== null ? MessageReactionRemoved.fromPartial(object.reactionRemoved) : void 0;
    message.stickerPlaced = object.stickerPlaced !== void 0 && object.stickerPlaced !== null ? StickerPlaced.fromPartial(object.stickerPlaced) : void 0;
    return message;
  }
};
function bytesFromBase642(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes2(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function toTimestamp2(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp2(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp2(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp2(Timestamp.fromJSON(o));
  }
}
function isSet5(value) {
  return value !== null && value !== void 0;
}

// src/utils/unwrap.ts
function unwrap(value, field) {
  if (value == null) {
    throw new Error(`Expected ${field} in response`);
  }
  return value;
}

// src/transport/mapper.ts
function mapTransferState(proto) {
  switch (proto) {
    case 0 /* TRANSFER_STATE_PENDING */:
      return "pending";
    case 1 /* TRANSFER_STATE_TRANSFERRING */:
      return "transferring";
    case 2 /* TRANSFER_STATE_FAILED */:
      return "failed";
    case 5 /* TRANSFER_STATE_FINISHED */:
      return "finished";
    default:
      return "unknown";
  }
}
function mapCompanionKind(proto) {
  switch (proto) {
    case 1 /* COMPANION_KIND_LIVE_PHOTO_VIDEO */:
      return "live-photo-video";
    default:
      return "unknown";
  }
}
function mapMessageItemType(proto) {
  switch (proto) {
    case 0 /* MESSAGE_ITEM_TYPE_NORMAL */:
      return "normal";
    case 1 /* MESSAGE_ITEM_TYPE_PARTICIPANT_CHANGE */:
      return "participantChange";
    case 2 /* MESSAGE_ITEM_TYPE_GROUP_NAME_CHANGE */:
      return "groupNameChange";
    case 3 /* MESSAGE_ITEM_TYPE_CHAT_ACTION */:
      return "chatAction";
    default:
      return "unknown";
  }
}
function mapChatServiceType(proto) {
  switch (proto) {
    case 1 /* CHAT_SERVICE_TYPE_IMESSAGE */:
      return "iMessage";
    case 2 /* CHAT_SERVICE_TYPE_SMS */:
      return "SMS";
    case 3 /* CHAT_SERVICE_TYPE_RCS */:
      return "RCS";
    default:
      return "unknown";
  }
}
function mapFriendLocationType(proto) {
  switch (proto) {
    case 1 /* FRIEND_LOCATION_TYPE_LEGACY */:
      return "legacy";
    case 2 /* FRIEND_LOCATION_TYPE_LIVE */:
      return "live";
    case 3 /* FRIEND_LOCATION_TYPE_SHALLOW */:
      return "shallow";
    default:
      return "unknown";
  }
}
function mapSingleServiceAddressInfo(proto) {
  return {
    address: proto.address,
    country: proto.country,
    service: mapChatServiceType(proto.service)
  };
}
function mapMultiServiceAddressInfo(proto) {
  return {
    address: proto.address,
    country: proto.country ?? null,
    services: proto.services.map(mapChatServiceType)
  };
}
function mapCompanionInfo(proto) {
  return {
    fileName: proto.fileName,
    kind: mapCompanionKind(proto.kind),
    mimeType: proto.mimeType,
    totalBytes: proto.totalBytes
  };
}
function mapAttachmentInfo(proto) {
  return {
    companionKind: proto.companionKind ? mapCompanionKind(proto.companionKind) : void 0,
    fileName: proto.fileName,
    guid: proto.guid,
    isHidden: proto.isHidden,
    isOutgoing: proto.isOutgoing,
    isSticker: proto.isSticker,
    mimeType: proto.mimeType,
    originalGuid: proto.originalGuid ? proto.originalGuid : void 0,
    totalBytes: proto.totalBytes,
    transferState: mapTransferState(proto.transferState),
    uti: proto.uti
  };
}
function mapTextFormat(proto) {
  return {
    effectName: proto.effectName,
    length: proto.length,
    start: proto.start,
    type: proto.type
  };
}
function mapTextFormatInput(input) {
  return {
    effectName: input.type === "effect" ? input.effect : void 0,
    length: input.length,
    start: input.start,
    type: input.type
  };
}
function mapMessageMention(proto) {
  return {
    address: proto.address,
    length: proto.length,
    start: proto.start
  };
}
function mapMessageReaction(proto) {
  switch (proto.kind) {
    case 0 /* MESSAGE_REACTION_KIND_LOVE */:
      return { emoji: proto.emoji, kind: "love" };
    case 1 /* MESSAGE_REACTION_KIND_LIKE */:
      return { emoji: proto.emoji, kind: "like" };
    case 2 /* MESSAGE_REACTION_KIND_DISLIKE */:
      return { emoji: proto.emoji, kind: "dislike" };
    case 3 /* MESSAGE_REACTION_KIND_LAUGH */:
      return { emoji: proto.emoji, kind: "laugh" };
    case 4 /* MESSAGE_REACTION_KIND_EMPHASIZE */:
      return { emoji: proto.emoji, kind: "emphasize" };
    case 5 /* MESSAGE_REACTION_KIND_QUESTION */:
      return { emoji: proto.emoji, kind: "question" };
    case 6 /* MESSAGE_REACTION_KIND_EMOJI */:
      return { emoji: proto.emoji, kind: "emoji" };
    case 7 /* MESSAGE_REACTION_KIND_STICKER */:
      return { emoji: proto.emoji, kind: "sticker" };
    default:
      return { emoji: proto.emoji, kind: "unknown" };
  }
}
function mapStickerPlacement(proto) {
  return {
    rotation: proto.rotation,
    scale: proto.scale,
    width: proto.width,
    x: proto.x,
    y: proto.y
  };
}
function mapMessageContent(proto) {
  return {
    attachments: proto.attachments.map(mapAttachmentInfo),
    balloonBundleId: proto.balloonBundleId,
    expressiveSendStyleId: proto.expressiveSendStyleId,
    formatting: proto.formatting.map(mapTextFormat),
    mentions: proto.mentions.map(mapMessageMention),
    text: proto.text
  };
}
function mapAppliedReaction(proto) {
  return {
    dateCreated: unwrap(proto.dateCreated, "dateCreated"),
    isFromMe: proto.isFromMe,
    messageGuid: proto.messageGuid,
    reaction: mapMessageReaction(unwrap(proto.reaction, "reaction")),
    sender: proto.sender ? mapSingleServiceAddressInfo(proto.sender) : void 0,
    targetPartIndex: proto.targetPartIndex
  };
}
function mapPlacedSticker(proto) {
  return {
    dateCreated: unwrap(proto.dateCreated, "dateCreated"),
    isFromMe: proto.isFromMe,
    messageGuid: proto.messageGuid,
    placement: proto.placement ? mapStickerPlacement(proto.placement) : void 0,
    sender: proto.sender ? mapSingleServiceAddressInfo(proto.sender) : void 0,
    sticker: proto.sticker ? mapAttachmentInfo(proto.sticker) : void 0,
    targetPartIndex: proto.targetPartIndex
  };
}
function mapMessage(proto) {
  return {
    appliedReactions: proto.appliedReactions.map(mapAppliedReaction),
    cachedRoomNames: proto.cachedRoomNames,
    chatActionType: proto.chatActionType,
    chatGuids: proto.chatGuids,
    content: mapMessageContent(unwrap(proto.content, "content")),
    dataDetectorResultsPresent: proto.dataDetectorResultsPresent,
    dateCreated: unwrap(proto.dateCreated, "dateCreated"),
    dateDelivered: proto.dateDelivered,
    dateEdited: proto.dateEdited,
    dateExpressiveSendPlayed: proto.dateExpressiveSendPlayed,
    datePlayed: proto.datePlayed,
    dateRead: proto.dateRead,
    dateRetracted: proto.dateRetracted,
    destinationCallerId: proto.destinationCallerId,
    didNotifyRecipient: proto.didNotifyRecipient,
    groupTitle: proto.groupTitle,
    guid: proto.guid,
    isArchived: proto.isArchived,
    isAudioMessage: proto.isAudioMessage,
    isAutoReply: proto.isAutoReply,
    isCorrupt: proto.isCorrupt,
    isDelayed: proto.isDelayed,
    isDelivered: proto.isDelivered,
    isDeliveredQuietly: proto.isDeliveredQuietly,
    isExpirable: proto.isExpirable,
    isForward: proto.isForward,
    isFromMe: proto.isFromMe,
    isSent: proto.isSent,
    isServiceMessage: proto.isServiceMessage,
    isSpam: proto.isSpam,
    isSystemMessage: proto.isSystemMessage,
    itemType: mapMessageItemType(proto.itemType),
    partCount: proto.partCount,
    placedStickers: proto.placedStickers.map(mapPlacedSticker),
    reaction: proto.reaction ? mapMessageReaction(proto.reaction) : void 0,
    reactionSelected: proto.reactionSelected,
    reactionTargetGuid: proto.reactionTargetGuid,
    reactionTargetPartIndex: proto.reactionTargetPartIndex,
    replyTargetGuid: proto.replyTargetGuid,
    sendErrorCode: proto.sendErrorCode,
    sender: proto.sender ? mapSingleServiceAddressInfo(proto.sender) : void 0,
    shareDirection: proto.shareDirection,
    shareStatus: proto.shareStatus,
    subject: proto.subject,
    threadOriginatorGuid: proto.threadOriginatorGuid,
    threadOriginatorPart: proto.threadOriginatorPart
  };
}
function mapChat(proto) {
  return {
    chatIdentifier: proto.chatIdentifier,
    displayName: proto.displayName ?? "",
    groupId: proto.groupId,
    guid: proto.guid,
    isArchived: proto.isArchived,
    isFiltered: proto.isFiltered,
    isGroup: proto.isGroup,
    lastMessage: proto.lastMessage ? mapMessage(proto.lastMessage) : void 0,
    participants: proto.participants.map(mapSingleServiceAddressInfo),
    service: mapChatServiceType(proto.service),
    unreadCount: proto.unreadCount
  };
}
function mapPollOption(proto) {
  return {
    creatorHandle: proto.creatorHandle,
    optionIdentifier: proto.optionIdentifier,
    text: proto.text
  };
}
function mapPollParticipantVote(proto) {
  return {
    optionIdentifier: proto.optionIdentifier,
    participant: mapSingleServiceAddressInfo(
      unwrap(proto.participant, "participant")
    )
  };
}
function mapPoll(proto) {
  return {
    chatGuid: proto.chatGuid,
    options: proto.options.map(mapPollOption),
    pollMessageGuid: proto.pollMessageGuid,
    title: proto.title,
    votes: proto.votes.map(mapPollParticipantVote)
  };
}
function mapPollDelta(proto) {
  if (proto.created) {
    return {
      type: "created",
      title: proto.created.title,
      options: proto.created.options.map(mapPollOption)
    };
  }
  if (proto.optionAdded) {
    return {
      type: "optionAdded",
      title: proto.optionAdded.title,
      options: proto.optionAdded.options.map(mapPollOption)
    };
  }
  if (proto.voted) {
    return {
      type: "voted",
      optionIdentifier: proto.voted.optionIdentifier
    };
  }
  if (proto.unvoted) {
    return {
      type: "unvoted",
      optionIdentifier: proto.unvoted.optionIdentifier
    };
  }
  return void 0;
}
function mapSharedFriendLocation(proto) {
  return {
    accuracy: proto.accuracy,
    address: proto.address,
    expiresAt: proto.expiresAt,
    isLocatingInProgress: proto.isLocatingInProgress,
    latitude: proto.latitude,
    locationTimestamp: proto.locationTimestamp,
    locationType: mapFriendLocationType(proto.locationType),
    longAddress: proto.longAddress,
    longitude: proto.longitude,
    name: proto.name,
    shortAddress: proto.shortAddress
  };
}
function mapSharedFriendLocationUpdated(proto) {
  return {
    location: mapSharedFriendLocation(unwrap(proto.location, "location")),
    sourceSequence: proto.sourceSequence
  };
}
function mapLocationRequestReceipt(proto) {
  return {
    address: proto.address,
    messageGuid: proto.messageGuid,
    reason: proto.reason,
    status: proto.status
  };
}
function mapEmbeddedMedia(proto) {
  return {
    data: proto.data,
    mimeType: proto.mimeType
  };
}
function mapEventContext(chatGuidValue, occurredAt, isFromMe, actor) {
  return {
    actor: actor ? mapSingleServiceAddressInfo(actor) : void 0,
    chatGuid: chatGuidValue,
    isFromMe,
    occurredAt
  };
}
function mapChatEvent(sequence, proto) {
  const context = mapEventContext(
    proto.chatGuid,
    unwrap(proto.occurredAt, "occurredAt"),
    proto.isFromMe,
    proto.actor
  );
  if (proto.backgroundChanged) {
    return { type: "chat.backgroundChanged", sequence, ...context };
  }
  if (proto.backgroundRemoved) {
    return { type: "chat.backgroundRemoved", sequence, ...context };
  }
  if (proto.markedRead) {
    return { type: "chat.markedRead", sequence, ...context };
  }
  if (proto.archived) {
    return { type: "chat.archived", sequence, ...context };
  }
  if (proto.unarchived) {
    return { type: "chat.unarchived", sequence, ...context };
  }
  return void 0;
}
function mapGroupChange(proto) {
  if (proto.displayNameChanged) {
    return {
      type: "displayNameChanged",
      displayName: proto.displayNameChanged.displayName
    };
  }
  if (proto.participantAdded) {
    return {
      type: "participantAdded",
      participant: mapSingleServiceAddressInfo(
        unwrap(proto.participantAdded.participant, "participant")
      )
    };
  }
  if (proto.participantRemoved) {
    return {
      type: "participantRemoved",
      participant: mapSingleServiceAddressInfo(
        unwrap(proto.participantRemoved.participant, "participant")
      )
    };
  }
  if (proto.participantLeft) {
    return {
      type: "participantLeft",
      participant: mapSingleServiceAddressInfo(
        unwrap(proto.participantLeft.participant, "participant")
      )
    };
  }
  if (proto.iconChanged) {
    return { type: "iconChanged" };
  }
  if (proto.iconRemoved) {
    return { type: "iconRemoved" };
  }
  return void 0;
}
function mapGroupEvent(sequence, proto) {
  const change = mapGroupChange(proto);
  if (!change) {
    return void 0;
  }
  return {
    type: "group.changed",
    sequence,
    ...mapEventContext(
      proto.chatGuid,
      unwrap(proto.occurredAt, "occurredAt"),
      proto.isFromMe,
      proto.actor
    ),
    change
  };
}
function mapReceivedMessage(sequence, context, proto) {
  return {
    type: "message.received",
    sequence,
    ...context,
    message: mapMessage(unwrap(proto.message, "message"))
  };
}
function mapEditedMessage(sequence, context, proto) {
  return {
    type: "message.edited",
    sequence,
    ...context,
    messageGuid: proto.messageGuid,
    content: mapMessageContent(unwrap(proto.content, "content")),
    editedAt: unwrap(proto.editedAt, "editedAt")
  };
}
function mapReadMessage(sequence, context, proto) {
  return {
    type: "message.read",
    sequence,
    ...context,
    messageGuid: proto.messageGuid,
    readAt: unwrap(proto.readAt, "readAt")
  };
}
function mapUnsentMessage(sequence, context, proto) {
  return {
    type: "message.unsent",
    sequence,
    ...context,
    messageGuid: proto.messageGuid,
    retractedAt: unwrap(proto.retractedAt, "retractedAt")
  };
}
function mapReactionAdded(sequence, context, proto) {
  return {
    type: "message.reactionAdded",
    sequence,
    ...context,
    messageGuid: proto.messageGuid,
    reaction: mapMessageReaction(unwrap(proto.reaction, "reaction")),
    targetPartIndex: proto.targetPartIndex
  };
}
function mapReactionRemoved(sequence, context, proto) {
  return {
    type: "message.reactionRemoved",
    sequence,
    ...context,
    messageGuid: proto.messageGuid,
    reaction: mapMessageReaction(unwrap(proto.reaction, "reaction")),
    targetPartIndex: proto.targetPartIndex
  };
}
function mapStickerPlacedEvent(sequence, context, proto) {
  return {
    type: "message.stickerPlaced",
    sequence,
    ...context,
    messageGuid: proto.messageGuid,
    placement: proto.placement ? mapStickerPlacement(proto.placement) : void 0,
    sticker: proto.sticker ? mapAttachmentInfo(proto.sticker) : void 0,
    targetPartIndex: proto.targetPartIndex
  };
}
function mapMessageEvent(sequence, proto) {
  const context = mapEventContext(
    proto.chatGuid,
    unwrap(proto.occurredAt, "occurredAt"),
    proto.isFromMe,
    proto.actor
  );
  if (proto.messageReceived) {
    return mapReceivedMessage(sequence, context, proto.messageReceived);
  }
  if (proto.messageEdited) {
    return mapEditedMessage(sequence, context, proto.messageEdited);
  }
  if (proto.messageRead) {
    return mapReadMessage(sequence, context, proto.messageRead);
  }
  if (proto.messageUnsent) {
    return mapUnsentMessage(sequence, context, proto.messageUnsent);
  }
  if (proto.reactionAdded) {
    return mapReactionAdded(sequence, context, proto.reactionAdded);
  }
  if (proto.reactionRemoved) {
    return mapReactionRemoved(sequence, context, proto.reactionRemoved);
  }
  if (proto.stickerPlaced) {
    return mapStickerPlacedEvent(sequence, context, proto.stickerPlaced);
  }
  return void 0;
}
function mapPollEvent(sequence, proto) {
  const delta = mapPollDelta(proto);
  if (!delta) {
    return void 0;
  }
  return {
    type: "poll.changed",
    sequence,
    ...mapEventContext(
      proto.chatGuid,
      unwrap(proto.occurredAt, "occurredAt"),
      proto.isFromMe,
      proto.actor
    ),
    delta,
    pollMessageGuid: proto.pollMessageGuid
  };
}
function mapReplyTarget(replyTo) {
  if (!replyTo) {
    return void 0;
  }
  if (typeof replyTo === "string") {
    return { messageGuid: replyTo, targetPartIndex: void 0 };
  }
  return {
    messageGuid: replyTo.guid,
    targetPartIndex: replyTo.partIndex
  };
}
function mapOutgoingMessagePart(part) {
  return {
    attachment: part.attachmentGuid ? {
      attachmentGuid: part.attachmentGuid,
      attachmentName: part.attachmentName
    } : void 0,
    bubbleIndex: part.bubbleIndex,
    formatting: (part.formatting ?? []).map(mapTextFormatInput),
    mentionedAddress: part.mentionedAddress,
    text: part.text
  };
}

// src/resources/addresses.ts
var AddressesResource = class {
  _client;
  constructor(client) {
    this._client = client;
  }
  /**
   * Look up the server's address record for a peer.
   *
   * Throws `NotFoundError` when the server has no record for the address.
   */
  async get(address) {
    try {
      const response = await this._client.getAddressInfo({ address });
      return mapMultiServiceAddressInfo(unwrap(response.info, "info"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Whether this device's current Focus configuration would silence
   * notifications from the given address.
   *
   * @example
   * ```ts
   * const silenced = await im.addresses.isFocusSilenced("alice@example.com");
   * ```
   */
  async isFocusSilenced(address) {
    try {
      const response = await this._client.getFocusStatus({ address });
      return response.isSilencedByFocus;
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Whether Apple currently reports the address as reachable on iMessage.
   *
   * @example
   * ```ts
   * const available = await im.addresses.isIMessageAvailable(
   *   "alice@example.com"
   * );
   * ```
   */
  async isIMessageAvailable(address) {
    try {
      const response = await this._client.getIMessageAvailability({ address });
      return response.isAvailable;
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
};

// src/streaming/event-stream.ts
var TypedEventStream = class _TypedEventStream {
  /** The underlying async iterable source. */
  _source;
  /** Optional cleanup callback invoked when the stream is closed. */
  _cleanup;
  /** Whether this stream has been closed. */
  _closed = false;
  /** Whether a consumer has already attached to this stream. */
  _consumed = false;
  /**
   * When a consumer is blocked on `next()`, this resolver lets `.close()`
   * interrupt it immediately.
   */
  _cancelResolve;
  constructor(source, cleanup) {
    this._source = source;
    this._cleanup = cleanup;
  }
  // -------------------------------------------------------------------------
  // AsyncIterable
  // -------------------------------------------------------------------------
  [Symbol.asyncIterator]() {
    this._claimConsumer();
    return this._iterate();
  }
  // -------------------------------------------------------------------------
  // Callback style
  // -------------------------------------------------------------------------
  /**
   * Subscribe to events with a callback. Returns an unsubscribe function that
   * stops the internal consumption loop.
   *
   * The callback may be synchronous or async. If async, back-pressure is
   * applied -- the next event is not delivered until the previous callback
   * resolves. Pass `onError` to handle callback or source errors; otherwise
   * errors are rethrown on the next microtask.
   */
  on(callback, onError) {
    this._claimConsumer();
    let stopped = false;
    const run = async () => {
      const iter = this._iterate();
      try {
        for (; ; ) {
          const result = await iter.next();
          if (result.done || stopped) {
            break;
          }
          await callback(result.value);
        }
      } finally {
        await iter.return?.(void 0);
      }
    };
    run().catch((error) => {
      if (onError) {
        onError(error);
        return;
      }
      queueMicrotask(() => {
        throw error;
      });
    });
    return () => {
      stopped = true;
      this.close();
    };
  }
  filter(predicate) {
    const parent = this;
    async function* filtered() {
      for await (const event of parent) {
        if (predicate(event)) {
          yield event;
        }
      }
    }
    return new _TypedEventStream(filtered(), () => parent.close());
  }
  /** Transform each event into a different shape. */
  map(transform) {
    const parent = this;
    async function* mapped() {
      for await (const event of parent) {
        yield transform(event);
      }
    }
    return new _TypedEventStream(mapped(), () => parent.close());
  }
  /**
   * Yield the first `count` events, then close the parent stream.
   *
   * `count <= 0` returns an empty stream without consuming the parent.
   */
  take(count) {
    const parent = this;
    async function* taken() {
      if (count <= 0) {
        return;
      }
      let remaining = count;
      for await (const event of parent) {
        yield event;
        remaining--;
        if (remaining <= 0) {
          break;
        }
      }
    }
    return new _TypedEventStream(taken(), () => parent.close());
  }
  // -------------------------------------------------------------------------
  // Lifecycle
  // -------------------------------------------------------------------------
  /** Signal the stream is done. Interrupts any pending iteration. */
  async close() {
    if (this._closed) {
      return;
    }
    this._closed = true;
    this._cancelResolve?.();
    if (this._cleanup) {
      await this._cleanup();
    }
  }
  /** `Symbol.asyncDispose` -- enables `await using`. */
  async [Symbol.asyncDispose]() {
    await this.close();
  }
  // -------------------------------------------------------------------------
  // Internals
  // -------------------------------------------------------------------------
  /**
   * Claim sole-consumer rights. Throws if the stream has already been
   * consumed or closed.
   */
  _claimConsumer() {
    if (this._closed) {
      throw new Error("Cannot consume a closed TypedEventStream.");
    }
    if (this._consumed) {
      throw new Error(
        "TypedEventStream already has a consumer. Each stream instance supports only one consumer. Use .filter() / .map() / .take() to derive a new stream before consuming."
      );
    }
    this._consumed = true;
  }
  /**
   * Internal async generator that pulls from the source and respects the
   * closed flag.
   */
  async *_iterate() {
    const iterator = this._source[Symbol.asyncIterator]();
    try {
      while (!this._closed) {
        const nextPromise = iterator.next();
        const result = await Promise.race([nextPromise, this._cancelPromise()]);
        if (result === void 0 || this._closed) {
          nextPromise.catch(() => void 0);
          break;
        }
        if (result.done) {
          break;
        }
        yield result.value;
      }
    } finally {
      const cleanup = iterator.return?.(void 0);
      if (cleanup && this._closed) {
        cleanup.catch(() => void 0);
      } else if (cleanup) {
        await cleanup;
      }
    }
  }
  /**
   * Returns a promise that resolves to `undefined` when `.close()` is called.
   * If the stream is already closed, resolves immediately.
   */
  _cancelPromise() {
    if (this._closed) {
      return Promise.resolve(void 0);
    }
    return new Promise((resolve) => {
      this._cancelResolve = () => resolve(void 0);
    });
  }
};

// src/resources/attachments.ts
function mapDownloadFrame(frame) {
  if (frame.header) {
    return {
      type: "header",
      info: mapAttachmentInfo(unwrap(frame.header.attachment, "attachment")),
      companionInfo: frame.header.companion ? mapCompanionInfo(frame.header.companion) : void 0
    };
  }
  if (frame.primaryChunk) {
    return {
      type: "primaryChunk",
      data: frame.primaryChunk
    };
  }
  if (frame.companionChunk) {
    return {
      type: "companionChunk",
      data: frame.companionChunk
    };
  }
  return void 0;
}
var AttachmentsResource = class {
  _client;
  _streamClient;
  constructor(client, streamClient = client) {
    this._client = client;
    this._streamClient = streamClient;
  }
  /**
   * Fetch metadata for a previously uploaded or received attachment.
   *
   * @param attachment - Attachment guid (from `attachments.upload(...)`,
   *                     `message.content.attachments[i].guid`, etc.).
   */
  async get(attachment) {
    try {
      const response = await this._client.getAttachmentInfo({
        attachmentGuid: attachment
      });
      return mapAttachmentInfo(unwrap(response.attachment, "attachment"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Upload a file (and optional Live Photo companion video) to the server.
   */
  async upload(input) {
    try {
      const response = await this._client.uploadAttachment({
        fileName: input.fileName,
        data: input.data,
        companion: input.companion ? {
          data: input.companion.data,
          kind: 1 /* COMPANION_KIND_LIVE_PHOTO_VIDEO */
        } : void 0
      });
      return {
        attachment: mapAttachmentInfo(
          unwrap(response.attachment, "attachment")
        ),
        companion: response.companion ? mapCompanionInfo(response.companion) : void 0
      };
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /** Stream the bytes of an attachment as the server emits them. */
  downloadStream(attachment) {
    const abort = new AbortController();
    const rpcStream = this._streamClient.downloadAttachment(
      {
        attachmentGuid: attachment
      },
      { signal: abort.signal }
    );
    async function* mapFrames() {
      try {
        for await (const frame of rpcStream) {
          const mapped = mapDownloadFrame(frame);
          if (mapped) {
            yield mapped;
          }
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapFrames(), async () => abort.abort());
  }
};

// src/types/chat-guid.ts
var RAW_CHAT_GUID = /^any;[-+];.+$/;
var STRUCTURED_CHAT_GUID_HELP = 'chat must start with "any;-;" for direct chats or "any;+;" for group chats, for example "any;-;alice@example.com" or "any;+;chat123". Pass `chat.guid` from a prior SDK call.';
function normalizeChatGuid(chat) {
  if (typeof chat !== "string") {
    throw new ValidationError(STRUCTURED_CHAT_GUID_HELP, {
      code: "invalidArgument",
      context: { field: "chat", value: String(chat) },
      grpcCode: 3,
      retryable: false
    });
  }
  const normalized = chat.trim();
  if (!normalized) {
    throw new ValidationError(STRUCTURED_CHAT_GUID_HELP, {
      code: "invalidArgument",
      context: { field: "chat", value: chat },
      grpcCode: 3,
      retryable: false
    });
  }
  if (!RAW_CHAT_GUID.test(normalized)) {
    throw new ValidationError(STRUCTURED_CHAT_GUID_HELP, {
      code: "invalidArgument",
      context: { field: "chat", value: normalized },
      grpcCode: 3,
      retryable: false
    });
  }
  return normalized;
}

// src/resources/chats.ts
function normalizeInitialMessageText(message) {
  if (message === void 0) {
    return void 0;
  }
  if (typeof message !== "string") {
    throw new ValidationError("message must be a string.", {
      code: "invalidArgument",
      context: { field: "message", value: String(message) },
      grpcCode: 3,
      retryable: false
    });
  }
  return message;
}
var ChatsResource = class {
  _client;
  _streamClient;
  _onHeartbeat;
  constructor(client, streamClient = client, onHeartbeat) {
    this._client = client;
    this._streamClient = streamClient;
    this._onHeartbeat = onHeartbeat;
  }
  /**
   * Create a chat with one or more peers, optionally sending an opening
   * message in the same round-trip.
   *
   * Pass `options.message` to send the initial message in the same request.
   *
   * @param addresses - One or more peer addresses (email or phone). One
   *                    address creates a 1:1 chat; two or more create a
   *                    group.
   */
  async create(addresses, options) {
    try {
      const message = normalizeInitialMessageText(options?.message);
      const response = await this._client.createChat({
        addresses,
        clientMessageId: options?.clientMessageId,
        service: 1 /* CHAT_SERVICE_TYPE_IMESSAGE */,
        initialMessage: message === void 0 ? void 0 : {
          attributedBody: options?.attributedBody,
          text: message,
          effectId: options?.effect,
          subject: options?.subject
        }
      });
      return {
        chat: mapChat(unwrap(response.chat, "chat")),
        initialMessage: response.initialMessage ? mapMessage(response.initialMessage) : void 0
      };
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Fetch a chat by reference.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid` from a previous SDK call.
   */
  async get(chat) {
    try {
      const response = await this._client.getChat({
        chatGuid: normalizeChatGuid(chat)
      });
      return mapChat(unwrap(response.chat, "chat"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /** Count chats accessible to the current account. */
  async count(options) {
    try {
      const response = await this._client.getChatCount({
        includeArchived: options?.includeArchived ?? false
      });
      return response.count;
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Whether `chat` has a custom background set.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async hasBackground(chat) {
    try {
      const response = await this._client.hasBackground({
        chatGuid: normalizeChatGuid(chat)
      });
      return response.backgroundPresent;
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Replace `chat`'s background with the supplied image bytes.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async setBackground(chat, data) {
    try {
      await this._client.setBackground({
        chatGuid: normalizeChatGuid(chat),
        data
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Clear `chat`'s custom background.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async removeBackground(chat) {
    try {
      await this._client.removeBackground({
        chatGuid: normalizeChatGuid(chat)
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Mark every unread message in `chat` as read.
   *
   * Resolves once the server has accepted and executed the command.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async markRead(chat) {
    try {
      await this._client.markChatRead({ chatGuid: normalizeChatGuid(chat) });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Send your contact card to `chat`.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async shareContactInfo(chat) {
    try {
      await this._client.shareContactInfo({
        chatGuid: normalizeChatGuid(chat)
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Set the transient typing indicator in `chat`.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async setTyping(chat, isTyping) {
    try {
      await this._client.setTyping({
        chatGuid: normalizeChatGuid(chat),
        isTyping
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Subscribe to chat-level durable change events (background changed,
   * marked-read, archive state).
   *
   * Pass `filter.chat` to scope the stream to a single chat.
   */
  subscribeEvents(filter) {
    const abort = new AbortController();
    const onHeartbeat = this._onHeartbeat;
    const rpcStream = this._streamClient.subscribeChatEvents(
      {
        chatGuid: filter?.chat ? normalizeChatGuid(filter.chat) : void 0
      },
      { signal: abort.signal }
    );
    async function* mapEvents() {
      try {
        for await (const frame of rpcStream) {
          if (frame.sequence === void 0 || !frame.chatChanged) {
            if (frame.heartbeat) {
              onHeartbeat?.();
            }
            continue;
          }
          const event = mapChatEvent(frame.sequence, frame.chatChanged);
          if (event) {
            yield event;
          }
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapEvents(), async () => abort.abort());
  }
};

// src/resources/events.ts
function normalizeSequenceCursor(cursor, field) {
  if (cursor === void 0) {
    return void 0;
  }
  if (!Number.isSafeInteger(cursor) || cursor < 0) {
    throw new ValidationError(`${field} must be a non-negative safe integer.`, {
      code: "invalidArgument",
      context: { field, value: String(cursor) },
      grpcCode: 3,
      retryable: false
    });
  }
  return cursor;
}
function mapCatchUpFrame(frame) {
  if (frame.complete) {
    return {
      type: "catchup.complete",
      headSequence: frame.complete.headSequence
    };
  }
  if (frame.sequence === void 0) {
    return void 0;
  }
  if (frame.messageChanged) {
    return mapMessageEvent(frame.sequence, frame.messageChanged);
  }
  if (frame.chatChanged) {
    return mapChatEvent(frame.sequence, frame.chatChanged);
  }
  if (frame.groupChanged) {
    return mapGroupEvent(frame.sequence, frame.groupChanged);
  }
  if (frame.pollChanged) {
    return mapPollEvent(frame.sequence, frame.pollChanged);
  }
  return void 0;
}
var EventsResource = class {
  _client;
  constructor(client) {
    this._client = client;
  }
  /**
   * Replay durable events after `since`, then end with `catchup.complete`.
   *
   * Pass the last fully processed sequence to recover missed events after a
   * disconnect. Omit `since` to replay from the beginning of the retained log.
   */
  catchUp(since) {
    const afterSequence = normalizeSequenceCursor(since, "since");
    const abort = new AbortController();
    const rpcStream = this._client.catchUpEvents(
      { afterSequence },
      { signal: abort.signal }
    );
    async function* mapEvents() {
      try {
        for await (const frame of rpcStream) {
          const mapped = mapCatchUpFrame(frame);
          if (mapped) {
            yield mapped;
          }
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapEvents(), async () => abort.abort());
  }
};

// src/resources/groups.ts
function normalizeDisplayName(displayName) {
  if (typeof displayName !== "string") {
    throw new ValidationError("display_name must be a string.", {
      code: "invalidArgument",
      context: { field: "display_name", value: String(displayName) },
      grpcCode: 3,
      retryable: false
    });
  }
  const normalized = displayName.trim();
  if (!normalized) {
    throw new ValidationError("display_name must not be empty", {
      code: "invalidArgument",
      context: { field: "display_name" },
      grpcCode: 3,
      retryable: false
    });
  }
  return normalized;
}
function normalizeParticipantAddresses(addresses) {
  if (!Array.isArray(addresses)) {
    throw new ValidationError("addresses must be an array.", {
      code: "invalidArgument",
      context: { field: "addresses", value: String(addresses) },
      grpcCode: 3,
      retryable: false
    });
  }
  if (addresses.length === 0) {
    throw new ValidationError("addresses must not be empty", {
      code: "invalidArgument",
      context: { field: "addresses" },
      grpcCode: 3,
      retryable: false
    });
  }
  return Array.from(addresses, (address, index) => {
    if (typeof address !== "string") {
      throw new ValidationError(`addresses[${index}] must be a string.`, {
        code: "invalidArgument",
        context: { field: `addresses[${index}]`, value: String(address) },
        grpcCode: 3,
        retryable: false
      });
    }
    const normalized = address.trim();
    if (!normalized) {
      throw new ValidationError(`addresses[${index}] must not be empty`, {
        code: "invalidArgument",
        context: { field: `addresses[${index}]` },
        grpcCode: 3,
        retryable: false
      });
    }
    return normalized;
  });
}
function normalizeIconData(data) {
  if (!(data instanceof Uint8Array)) {
    throw new ValidationError("data must be a Uint8Array.", {
      code: "invalidArgument",
      context: { field: "data", value: String(data) },
      grpcCode: 3,
      retryable: false
    });
  }
  return data;
}
var GroupsResource = class {
  _client;
  _streamClient;
  _onHeartbeat;
  constructor(client, streamClient = client, onHeartbeat) {
    this._client = client;
    this._streamClient = streamClient;
    this._onHeartbeat = onHeartbeat;
  }
  /**
   * Rename a group chat.
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid` from a prior SDK call.
   *               Direct chats reject this operation server-side.
   */
  async setDisplayName(chat, displayName, options) {
    try {
      const response = await this._client.setDisplayName({
        chatGuid: normalizeChatGuid(chat),
        displayName: normalizeDisplayName(displayName),
        clientMessageId: options?.clientMessageId
      });
      return mapChat(unwrap(response.chat, "chat"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Add one or more participants to a group chat.
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid`.
   * @param addresses - Email addresses or phone numbers to invite.
   */
  async addParticipants(chat, addresses, options) {
    try {
      const response = await this._client.addParticipants({
        chatGuid: normalizeChatGuid(chat),
        addresses: normalizeParticipantAddresses(addresses),
        clientMessageId: options?.clientMessageId
      });
      return mapChat(unwrap(response.chat, "chat"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Remove participants from a group chat (only the chat creator can do this).
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid`.
   * @param addresses - Email addresses or phone numbers to remove.
   */
  async removeParticipants(chat, addresses, options) {
    try {
      const response = await this._client.removeParticipants({
        chatGuid: normalizeChatGuid(chat),
        addresses: normalizeParticipantAddresses(addresses),
        clientMessageId: options?.clientMessageId
      });
      return mapChat(unwrap(response.chat, "chat"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Leave a group chat.
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid`.
   */
  async leave(chat, options) {
    try {
      await this._client.leaveGroup({
        chatGuid: normalizeChatGuid(chat),
        clientMessageId: options?.clientMessageId
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Set the group photo to the supplied image bytes.
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid`.
   */
  async setIcon(chat, data, options) {
    try {
      await this._client.setIcon({
        chatGuid: normalizeChatGuid(chat),
        data: normalizeIconData(data),
        clientMessageId: options?.clientMessageId
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Fetch the current group photo bytes.
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid`.
   */
  async getIcon(chat) {
    try {
      const response = await this._client.getIcon({
        chatGuid: normalizeChatGuid(chat)
      });
      return {
        data: response.data,
        mimeType: response.mimeType
      };
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Clear the group photo.
   *
   * @param chat - An `any;+;...` group chat guid. In practice, pass
   *               `chat.guid`.
   */
  async removeIcon(chat, options) {
    try {
      await this._client.removeIcon({
        chatGuid: normalizeChatGuid(chat),
        clientMessageId: options?.clientMessageId
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Subscribe to group-membership / metadata events.
   *
   * Pass `filter.chat` to scope the stream to a single group.
   */
  subscribeEvents(filter) {
    const abort = new AbortController();
    const onHeartbeat = this._onHeartbeat;
    const rpcStream = this._streamClient.subscribeGroupEvents(
      {
        chatGuid: filter?.chat ? normalizeChatGuid(filter.chat) : void 0
      },
      { signal: abort.signal }
    );
    async function* mapEvents() {
      try {
        for await (const frame of rpcStream) {
          if (frame.sequence === void 0 || !frame.groupChanged) {
            if (frame.heartbeat) {
              onHeartbeat?.();
            }
            continue;
          }
          const event = mapGroupEvent(frame.sequence, frame.groupChanged);
          if (event) {
            yield event;
          }
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapEvents(), async () => abort.abort());
  }
};

// src/resources/locations.ts
var LocationsResource = class {
  _client;
  _streamClient;
  _onHeartbeat;
  constructor(client, streamClient = client, onHeartbeat) {
    this._client = client;
    this._streamClient = streamClient;
    this._onHeartbeat = onHeartbeat;
  }
  /**
   * List every friend currently sharing a location with the local account.
   *
   * Returns an empty array when no active shared-location sessions exist.
   */
  async list() {
    try {
      const response = await this._client.listSharedFriendLocations({});
      return response.locations.map(mapSharedFriendLocation);
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Fetch the latest shared-location snapshot for one address.
   *
   * Throws `NotFoundError` when that address is not currently sharing a
   * location with the local account.
   */
  async get(address) {
    try {
      const response = await this._client.getSharedFriendLocation({ address });
      return mapSharedFriendLocation(unwrap(response.location, "location"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Request location sharing from one participant in a chat.
   *
   * This sends a visible Find My request card. It does not grant access by
   * itself; the other person must accept or start sharing before `get`, `list`,
   * or `watch` can return their location.
   */
  async request(chat, address, options = {}) {
    try {
      const response = await this._client.requestFriendLocationSharing({
        address,
        chatGuid: normalizeChatGuid(chat),
        clientMessageId: options.clientMessageId
      });
      return mapLocationRequestReceipt(response);
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /** Watch updates for every shared friend, or one friend when `address` is set. */
  watch(address) {
    const abort = new AbortController();
    const onHeartbeat = this._onHeartbeat;
    const rpcStream = this._streamClient.watchSharedFriendLocations(
      { address },
      { signal: abort.signal }
    );
    async function* mapUpdates() {
      try {
        for await (const frame of rpcStream) {
          if (!frame.locationUpdated) {
            if (frame.heartbeat) {
              onHeartbeat?.();
            }
            continue;
          }
          yield mapSharedFriendLocationUpdated(frame.locationUpdated);
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapUpdates(), async () => abort.abort());
  }
};

// src/resources/messages.ts
function toReactionKind(reaction) {
  switch (reaction) {
    case "love":
      return 0 /* MESSAGE_REACTION_KIND_LOVE */;
    case "like":
      return 1 /* MESSAGE_REACTION_KIND_LIKE */;
    case "dislike":
      return 2 /* MESSAGE_REACTION_KIND_DISLIKE */;
    case "laugh":
      return 3 /* MESSAGE_REACTION_KIND_LAUGH */;
    case "emphasize":
      return 4 /* MESSAGE_REACTION_KIND_EMPHASIZE */;
    case "question":
      return 5 /* MESSAGE_REACTION_KIND_QUESTION */;
    case "emoji":
      return 6 /* MESSAGE_REACTION_KIND_EMOJI */;
    default:
      throw new TypeError(`Unsupported reaction kind: ${String(reaction)}`);
  }
}
var MessagesResource = class {
  _client;
  _streamClient;
  _onHeartbeat;
  constructor(client, streamClient = client, onHeartbeat) {
    this._client = client;
    this._streamClient = streamClient;
    this._onHeartbeat = onHeartbeat;
  }
  /**
   * Send a plain-text message.
   *
   * The returned `Message` is the server's immediate snapshot.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param text - The message body.
   */
  async sendText(chat, text, options) {
    try {
      const response = await this._client.sendTextMessage({
        chatGuid: normalizeChatGuid(chat),
        text,
        replyTo: mapReplyTarget(options?.replyTo),
        subject: options?.subject,
        effectId: options?.effect,
        enableDataDetection: options?.enableDataDetection,
        enableLinkPreview: options?.enableLinkPreview,
        formatting: (options?.formatting ?? []).map(mapTextFormatInput),
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Send a previously uploaded attachment as a message.
   *
   * The returned `Message` is the server's immediate snapshot.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param attachment - Attachment guid returned by `attachments.upload(...)`.
   */
  async sendAttachment(chat, attachment, options) {
    try {
      const response = await this._client.sendAttachmentMessage({
        chatGuid: normalizeChatGuid(chat),
        attachment: { attachmentGuid: attachment },
        replyTo: mapReplyTarget(options?.replyTo),
        effectId: options?.effect,
        isAudioMessage: options?.isAudioMessage,
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Send a multi-part message (interleaved text, attachments, mentions).
   *
   * The returned `Message` is the server's immediate snapshot.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async sendMultipart(chat, parts, options) {
    try {
      const response = await this._client.sendMultipartMessage({
        chatGuid: normalizeChatGuid(chat),
        parts: parts.map(mapOutgoingMessagePart),
        replyTo: mapReplyTarget(options?.replyTo),
        subject: options?.subject,
        effectId: options?.effect,
        enableDataDetection: options?.enableDataDetection,
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Send an iMessage mini-app card backed by the caller's own iMessage
   * extension.
   *
   * The server assembles Apple's balloon plugin id from `teamId` and
   * `extensionBundleId`. Recipients with the extension installed open it on
   * tap; others see the App Store entry referenced by `appStoreId` when it is
   * set.
   *
   * The `layout` field mirrors Apple's `MSMessageTemplateLayout`. At least
   * one of `caption`, `subcaption`, `trailingCaption`, `trailingSubcaption`,
   * or `image` must be set, otherwise the card renders as an empty bubble
   * and is rejected with INVALID_ARGUMENT. `imageTitle` and `imageSubtitle`
   * are overlay slots that render only when `image` is also set.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message.teamId - 10-character uppercase alphanumeric Apple Team ID.
   * @param message.extensionBundleId - Bundle id of the iMessage extension
   *   target. Must not contain `:`.
   * @param message.appName - App display name used by Messages fallback UI.
   * @param message.appStoreId - Optional positive Apple App Store id of the
   *   owning app.
   * @param message.url - Absolute URL delivered to the extension on tap.
   * @param message.layout - Visible card layout. See `MiniAppLayout` for the
   *   rendering map of each slot.
   */
  async sendCustomizedMiniApp(chat, message, options) {
    try {
      const response = await this._client.sendCustomizedMiniAppMessage({
        chatGuid: normalizeChatGuid(chat),
        teamId: message.teamId,
        extensionBundleId: message.extensionBundleId,
        appName: message.appName,
        url: message.url,
        layout: message.layout,
        appStoreId: message.appStoreId,
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Edit an already-sent message (within Apple's 15-minute edit window).
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message - Guid of the message to edit (`message.guid`).
   * @param options.backwardCompatText - Server-defined plain-text edit body
   *   used when rich edit rendering is unavailable.
   */
  async edit(chat, message, newText, options) {
    try {
      const response = await this._client.editMessage({
        target: {
          chatGuid: normalizeChatGuid(chat),
          messageGuid: message,
          targetPartIndex: options?.partIndex
        },
        newText,
        backwardCompatText: options?.backwardCompatText,
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Retract (un-send) a message, within Apple's 2-minute window.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message - Guid of the message to retract (`message.guid`).
   */
  async unsend(chat, message, options) {
    try {
      await this._client.unsendMessage({
        target: {
          chatGuid: normalizeChatGuid(chat),
          messageGuid: message,
          targetPartIndex: options?.partIndex
        },
        clientMessageId: options?.clientMessageId
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Add or remove a tapback reaction on a message.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message - Guid of the target message (`message.guid`).
   */
  async setReaction(chat, message, reaction, isSet17, options) {
    const kind = toReactionKind(reaction.kind);
    try {
      const response = await this._client.setReaction({
        target: {
          chatGuid: normalizeChatGuid(chat),
          messageGuid: message,
          targetPartIndex: options?.partIndex
        },
        reaction: {
          kind,
          emoji: reaction.emoji
        },
        isSet: isSet17,
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Place a sticker on a message.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message - Guid of the target message.
   * @param sticker - Attachment guid of an uploaded sticker image.
   */
  async placeSticker(chat, message, sticker, placement, options) {
    try {
      const response = await this._client.placeSticker({
        target: {
          chatGuid: normalizeChatGuid(chat),
          messageGuid: message,
          targetPartIndex: options?.partIndex
        },
        sticker: { attachmentGuid: sticker },
        placement,
        clientMessageId: options?.clientMessageId
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Tell the recipient that a notify-silenced (Focus / Do Not Disturb) bypass
   * was attempted for `message`.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message - Guid of the message that triggered the silence.
   */
  async notifySilenced(chat, message, options) {
    try {
      await this._client.notifySilencedMessage({
        chatGuid: normalizeChatGuid(chat),
        messageGuid: message,
        clientMessageId: options?.clientMessageId
      });
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Fetch a single message by its guid.
   *
   * @param message - Guid of the message to fetch (`message.guid`).
   */
  async get(message) {
    try {
      const response = await this._client.getMessage({
        messageGuid: message
      });
      return mapMessage(unwrap(response.message, "message"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * List recent messages across all chats.
   *
   * `options.pageSize`, when provided, must be between `1` and `100`
   * inclusive.
   */
  async listRecent(options) {
    try {
      const response = await this._client.listRecentMessages({
        pageSize: options?.pageSize,
        pageToken: options?.pageToken,
        isFromMe: options?.isFromMe,
        isRead: options?.isRead,
        before: options?.before,
        after: options?.after
      });
      return {
        messages: response.messages.map(mapMessage),
        nextPageToken: response.nextPageToken
      };
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * List recent messages within a single chat.
   *
   * `options.pageSize`, when provided, must be between `1` and `100`
   * inclusive.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   */
  async listInChat(chat, options) {
    try {
      const response = await this._client.listChatMessages({
        chatGuid: normalizeChatGuid(chat),
        pageSize: options?.pageSize,
        pageToken: options?.pageToken,
        isFromMe: options?.isFromMe,
        isRead: options?.isRead,
        before: options?.before,
        after: options?.after
      });
      return {
        messages: response.messages.map(mapMessage),
        nextPageToken: response.nextPageToken
      };
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Fetch embedded media bytes for a supported Digital Touch or handwritten
   * message.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param message - Guid of the bubble whose media you want.
   */
  async getEmbeddedMedia(chat, message) {
    try {
      const response = await this._client.getEmbeddedMedia({
        chatGuid: normalizeChatGuid(chat),
        messageGuid: message
      });
      return mapEmbeddedMedia(unwrap(response.media, "media"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Subscribe to message-level events (received / edited / read / unsent /
   * reaction / sticker).
   *
   * Use the write response as the authoritative result for the mutation you
   * just issued.
   *
   * Pass `filter.chat` to scope the stream to a single chat.
   */
  subscribeEvents(filter) {
    const abort = new AbortController();
    const onHeartbeat = this._onHeartbeat;
    const rpcStream = this._streamClient.subscribeMessageEvents(
      {
        chatGuid: filter?.chat ? normalizeChatGuid(filter.chat) : void 0
      },
      { signal: abort.signal }
    );
    async function* mapEvents() {
      try {
        for await (const frame of rpcStream) {
          if (frame.sequence === void 0 || !frame.messageChanged) {
            if (frame.heartbeat) {
              onHeartbeat?.();
            }
            continue;
          }
          const event = mapMessageEvent(frame.sequence, frame.messageChanged);
          if (event) {
            yield event;
          }
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapEvents(), async () => abort.abort());
  }
};

// src/resources/polls.ts
var PollsResource = class {
  _client;
  _streamClient;
  _onHeartbeat;
  constructor(client, streamClient = client, onHeartbeat) {
    this._client = client;
    this._streamClient = streamClient;
    this._onHeartbeat = onHeartbeat;
  }
  /**
   * Create a poll in `chat` with `title` and an initial set of `choices`.
   *
   * @param chat - An `any;-;...` or `any;+;...` chat guid. In practice, pass
   *               `chat.guid`.
   * @param title - Poll title shown above the choices.
   * @param choices - Initial choice texts.
   */
  async create(chat, title, choices, options) {
    try {
      const response = await this._client.createPoll({
        chatGuid: normalizeChatGuid(chat),
        title,
        options: choices,
        clientMessageId: options?.clientMessageId
      });
      return mapPoll(unwrap(response.poll, "poll"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Fetch the current state of a poll (title, choices, votes).
   *
   * @param pollMessage - The guid of the poll message
   *                      (`poll.pollMessageGuid` or any prior result).
   */
  async get(pollMessage) {
    try {
      const response = await this._client.getPoll({
        pollMessageGuid: pollMessage
      });
      return mapPoll(unwrap(response.poll, "poll"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Cast (or re-cast) the current account's vote on a poll.
   *
   * @param pollMessage - The guid of the poll message.
   * @param optionId - The `optionIdentifier` of the chosen choice.
   */
  async vote(pollMessage, optionId, options) {
    try {
      const response = await this._client.votePoll({
        pollMessageGuid: pollMessage,
        optionIdentifier: optionId,
        clientMessageId: options?.clientMessageId
      });
      return mapPoll(unwrap(response.poll, "poll"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Withdraw the current account's vote on a poll.
   *
   * @param pollMessage - The guid of the poll message.
   */
  async unvote(pollMessage, options) {
    try {
      const response = await this._client.unvotePoll({
        pollMessageGuid: pollMessage,
        clientMessageId: options?.clientMessageId
      });
      return mapPoll(unwrap(response.poll, "poll"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Append a new choice to an existing poll.
   *
   * @param pollMessage - The guid of the poll message.
   * @param text - The choice's display text.
   */
  async addOption(pollMessage, text, options) {
    try {
      const response = await this._client.addPollOption({
        pollMessageGuid: pollMessage,
        optionText: text,
        clientMessageId: options?.clientMessageId
      });
      return mapPoll(unwrap(response.poll, "poll"));
    } catch (err) {
      throw fromGrpcError(err);
    }
  }
  /**
   * Subscribe to poll lifecycle events (created / option added / voted /
   * unvoted). Vote and unvote events include the affected option identifier.
   *
   * Pass `filter.pollMessage` to scope the stream to a single poll.
   */
  subscribeEvents(filter) {
    const abort = new AbortController();
    const onHeartbeat = this._onHeartbeat;
    const rpcStream = this._streamClient.subscribePollEvents(
      {
        pollMessageGuid: filter?.pollMessage
      },
      { signal: abort.signal }
    );
    async function* mapEvents() {
      try {
        for await (const frame of rpcStream) {
          if (frame.sequence === void 0 || !frame.pollChanged) {
            if (frame.heartbeat) {
              onHeartbeat?.();
            }
            continue;
          }
          const event = mapPollEvent(frame.sequence, frame.pollChanged);
          if (event) {
            yield event;
          }
        }
      } catch (err) {
        throw fromGrpcError(err);
      }
    }
    return new TypedEventStream(mapEvents(), async () => abort.abort());
  }
};
function createBaseGetAddressInfoRequest() {
  return { address: "" };
}
var GetAddressInfoRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAddressInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet6(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetAddressInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAddressInfoRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetAddressInfoResponse() {
  return { info: void 0 };
}
var GetAddressInfoResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.info !== void 0) {
      MultiServiceAddressInfo.encode(message.info, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAddressInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.info = MultiServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { info: isSet6(object.info) ? MultiServiceAddressInfo.fromJSON(object.info) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.info !== void 0) {
      obj.info = MultiServiceAddressInfo.toJSON(message.info);
    }
    return obj;
  },
  create(base) {
    return GetAddressInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAddressInfoResponse();
    message.info = object.info !== void 0 && object.info !== null ? MultiServiceAddressInfo.fromPartial(object.info) : void 0;
    return message;
  }
};
function createBaseGetFocusStatusRequest() {
  return { address: "" };
}
var GetFocusStatusRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetFocusStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet6(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetFocusStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetFocusStatusRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetFocusStatusResponse() {
  return { isSilencedByFocus: false };
}
var GetFocusStatusResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.isSilencedByFocus !== false) {
      writer.uint32(8).bool(message.isSilencedByFocus);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetFocusStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.isSilencedByFocus = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      isSilencedByFocus: isSet6(object.isSilencedByFocus) ? globalThis.Boolean(object.isSilencedByFocus) : isSet6(object.is_silenced_by_focus) ? globalThis.Boolean(object.is_silenced_by_focus) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.isSilencedByFocus !== false) {
      obj.isSilencedByFocus = message.isSilencedByFocus;
    }
    return obj;
  },
  create(base) {
    return GetFocusStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetFocusStatusResponse();
    message.isSilencedByFocus = object.isSilencedByFocus ?? false;
    return message;
  }
};
function createBaseGetIMessageAvailabilityRequest() {
  return { address: "" };
}
var GetIMessageAvailabilityRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIMessageAvailabilityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet6(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetIMessageAvailabilityRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIMessageAvailabilityRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetIMessageAvailabilityResponse() {
  return { isAvailable: false };
}
var GetIMessageAvailabilityResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.isAvailable !== false) {
      writer.uint32(8).bool(message.isAvailable);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIMessageAvailabilityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.isAvailable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      isAvailable: isSet6(object.isAvailable) ? globalThis.Boolean(object.isAvailable) : isSet6(object.is_available) ? globalThis.Boolean(object.is_available) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.isAvailable !== false) {
      obj.isAvailable = message.isAvailable;
    }
    return obj;
  },
  create(base) {
    return GetIMessageAvailabilityResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIMessageAvailabilityResponse();
    message.isAvailable = object.isAvailable ?? false;
    return message;
  }
};
var AddressServiceDefinition = {
  name: "AddressService",
  fullName: "photon.imessage.v1.AddressService",
  methods: {
    /**
     * Returns services seen and country for the address.
     *
     * Errors: NOT_FOUND when the address has never been seen in this device's
     * iMessage history.
     */
    getAddressInfo: {
      name: "GetAddressInfo",
      requestType: GetAddressInfoRequest,
      requestStream: false,
      responseType: GetAddressInfoResponse,
      responseStream: false,
      options: {}
    },
    /** Returns whether Messages currently reports this address as Focus-silenced. */
    getFocusStatus: {
      name: "GetFocusStatus",
      requestType: GetFocusStatusRequest,
      requestStream: false,
      responseType: GetFocusStatusResponse,
      responseStream: false,
      options: {}
    },
    /** Returns whether the address is reachable via iMessage at query time. */
    getIMessageAvailability: {
      name: "GetIMessageAvailability",
      requestType: GetIMessageAvailabilityRequest,
      requestStream: false,
      responseType: GetIMessageAvailabilityResponse,
      responseStream: false,
      options: {}
    }
  }
};
function isSet6(value) {
  return value !== null && value !== void 0;
}
function createBaseGetAttachmentInfoRequest() {
  return { attachmentGuid: "" };
}
var GetAttachmentInfoRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachmentGuid !== "") {
      writer.uint32(10).string(message.attachmentGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAttachmentInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachmentGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachmentGuid: isSet7(object.attachmentGuid) ? globalThis.String(object.attachmentGuid) : isSet7(object.attachment_guid) ? globalThis.String(object.attachment_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachmentGuid !== "") {
      obj.attachmentGuid = message.attachmentGuid;
    }
    return obj;
  },
  create(base) {
    return GetAttachmentInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAttachmentInfoRequest();
    message.attachmentGuid = object.attachmentGuid ?? "";
    return message;
  }
};
function createBaseGetAttachmentInfoResponse() {
  return { attachment: void 0 };
}
var GetAttachmentInfoResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachment !== void 0) {
      AttachmentInfo.encode(message.attachment, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAttachmentInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachment = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { attachment: isSet7(object.attachment) ? AttachmentInfo.fromJSON(object.attachment) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentInfo.toJSON(message.attachment);
    }
    return obj;
  },
  create(base) {
    return GetAttachmentInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAttachmentInfoResponse();
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentInfo.fromPartial(object.attachment) : void 0;
    return message;
  }
};
function createBaseUploadAttachmentRequest() {
  return { fileName: "", data: new Uint8Array(0), companion: void 0 };
}
var UploadAttachmentRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.fileName !== "") {
      writer.uint32(10).string(message.fileName);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.companion !== void 0) {
      Companion.encode(message.companion, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadAttachmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.fileName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.companion = Companion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      fileName: isSet7(object.fileName) ? globalThis.String(object.fileName) : isSet7(object.file_name) ? globalThis.String(object.file_name) : "",
      data: isSet7(object.data) ? bytesFromBase643(object.data) : new Uint8Array(0),
      companion: isSet7(object.companion) ? Companion.fromJSON(object.companion) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes3(message.data);
    }
    if (message.companion !== void 0) {
      obj.companion = Companion.toJSON(message.companion);
    }
    return obj;
  },
  create(base) {
    return UploadAttachmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUploadAttachmentRequest();
    message.fileName = object.fileName ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.companion = object.companion !== void 0 && object.companion !== null ? Companion.fromPartial(object.companion) : void 0;
    return message;
  }
};
function createBaseUploadAttachmentResponse() {
  return { attachment: void 0, companion: void 0 };
}
var UploadAttachmentResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachment !== void 0) {
      AttachmentInfo.encode(message.attachment, writer.uint32(10).fork()).join();
    }
    if (message.companion !== void 0) {
      CompanionInfo.encode(message.companion, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadAttachmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachment = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.companion = CompanionInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachment: isSet7(object.attachment) ? AttachmentInfo.fromJSON(object.attachment) : void 0,
      companion: isSet7(object.companion) ? CompanionInfo.fromJSON(object.companion) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentInfo.toJSON(message.attachment);
    }
    if (message.companion !== void 0) {
      obj.companion = CompanionInfo.toJSON(message.companion);
    }
    return obj;
  },
  create(base) {
    return UploadAttachmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUploadAttachmentResponse();
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentInfo.fromPartial(object.attachment) : void 0;
    message.companion = object.companion !== void 0 && object.companion !== null ? CompanionInfo.fromPartial(object.companion) : void 0;
    return message;
  }
};
function createBaseDownloadAttachmentRequest() {
  return { attachmentGuid: "" };
}
var DownloadAttachmentRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachmentGuid !== "") {
      writer.uint32(10).string(message.attachmentGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDownloadAttachmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachmentGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachmentGuid: isSet7(object.attachmentGuid) ? globalThis.String(object.attachmentGuid) : isSet7(object.attachment_guid) ? globalThis.String(object.attachment_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachmentGuid !== "") {
      obj.attachmentGuid = message.attachmentGuid;
    }
    return obj;
  },
  create(base) {
    return DownloadAttachmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDownloadAttachmentRequest();
    message.attachmentGuid = object.attachmentGuid ?? "";
    return message;
  }
};
function createBaseDownloadHeader() {
  return { attachment: void 0, companion: void 0 };
}
var DownloadHeader = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachment !== void 0) {
      AttachmentInfo.encode(message.attachment, writer.uint32(10).fork()).join();
    }
    if (message.companion !== void 0) {
      CompanionInfo.encode(message.companion, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDownloadHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachment = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.companion = CompanionInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachment: isSet7(object.attachment) ? AttachmentInfo.fromJSON(object.attachment) : void 0,
      companion: isSet7(object.companion) ? CompanionInfo.fromJSON(object.companion) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentInfo.toJSON(message.attachment);
    }
    if (message.companion !== void 0) {
      obj.companion = CompanionInfo.toJSON(message.companion);
    }
    return obj;
  },
  create(base) {
    return DownloadHeader.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDownloadHeader();
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentInfo.fromPartial(object.attachment) : void 0;
    message.companion = object.companion !== void 0 && object.companion !== null ? CompanionInfo.fromPartial(object.companion) : void 0;
    return message;
  }
};
function createBaseDownloadAttachmentResponse() {
  return { header: void 0, primaryChunk: void 0, companionChunk: void 0 };
}
var DownloadAttachmentResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.header !== void 0) {
      DownloadHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.primaryChunk !== void 0) {
      writer.uint32(18).bytes(message.primaryChunk);
    }
    if (message.companionChunk !== void 0) {
      writer.uint32(26).bytes(message.companionChunk);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDownloadAttachmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.header = DownloadHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.primaryChunk = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.companionChunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      header: isSet7(object.header) ? DownloadHeader.fromJSON(object.header) : void 0,
      primaryChunk: isSet7(object.primaryChunk) ? bytesFromBase643(object.primaryChunk) : isSet7(object.primary_chunk) ? bytesFromBase643(object.primary_chunk) : void 0,
      companionChunk: isSet7(object.companionChunk) ? bytesFromBase643(object.companionChunk) : isSet7(object.companion_chunk) ? bytesFromBase643(object.companion_chunk) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.header !== void 0) {
      obj.header = DownloadHeader.toJSON(message.header);
    }
    if (message.primaryChunk !== void 0) {
      obj.primaryChunk = base64FromBytes3(message.primaryChunk);
    }
    if (message.companionChunk !== void 0) {
      obj.companionChunk = base64FromBytes3(message.companionChunk);
    }
    return obj;
  },
  create(base) {
    return DownloadAttachmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDownloadAttachmentResponse();
    message.header = object.header !== void 0 && object.header !== null ? DownloadHeader.fromPartial(object.header) : void 0;
    message.primaryChunk = object.primaryChunk ?? void 0;
    message.companionChunk = object.companionChunk ?? void 0;
    return message;
  }
};
var AttachmentServiceDefinition = {
  name: "AttachmentService",
  fullName: "photon.imessage.v1.AttachmentService",
  methods: {
    /** Metadata-only lookup. Cheap; never touches attachment bytes. */
    getAttachmentInfo: {
      name: "GetAttachmentInfo",
      requestType: GetAttachmentInfoRequest,
      requestStream: false,
      responseType: GetAttachmentInfoResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Atomic upload of one primary file plus an optional sidecar. The
     * server persists both before responding; on failure neither is visible
     * to subsequent reads.
     */
    uploadAttachment: {
      name: "UploadAttachment",
      requestType: UploadAttachmentRequest,
      requestStream: false,
      responseType: UploadAttachmentResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Server-streaming bytes. See `DownloadAttachmentResponse` for the
     * frame contract.
     */
    downloadAttachment: {
      name: "DownloadAttachment",
      requestType: DownloadAttachmentRequest,
      requestStream: false,
      responseType: DownloadAttachmentResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase643(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes3(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function isSet7(value) {
  return value !== null && value !== void 0;
}
function createBaseEmpty() {
  return {};
}
var Empty = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return Empty.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseEmpty();
    return message;
  }
};
function createBaseChat() {
  return {
    guid: "",
    chatIdentifier: void 0,
    groupId: void 0,
    displayName: void 0,
    isGroup: false,
    service: 0,
    isArchived: false,
    isFiltered: false,
    unreadCount: void 0,
    participants: [],
    lastMessage: void 0
  };
}
var Chat = {
  encode(message, writer = new BinaryWriter()) {
    if (message.guid !== "") {
      writer.uint32(10).string(message.guid);
    }
    if (message.chatIdentifier !== void 0) {
      writer.uint32(18).string(message.chatIdentifier);
    }
    if (message.groupId !== void 0) {
      writer.uint32(26).string(message.groupId);
    }
    if (message.displayName !== void 0) {
      writer.uint32(34).string(message.displayName);
    }
    if (message.isGroup !== false) {
      writer.uint32(40).bool(message.isGroup);
    }
    if (message.service !== 0) {
      writer.uint32(48).int32(message.service);
    }
    if (message.isArchived !== false) {
      writer.uint32(56).bool(message.isArchived);
    }
    if (message.isFiltered !== false) {
      writer.uint32(64).bool(message.isFiltered);
    }
    if (message.unreadCount !== void 0) {
      writer.uint32(72).int32(message.unreadCount);
    }
    for (const v of message.participants) {
      SingleServiceAddressInfo.encode(v, writer.uint32(82).fork()).join();
    }
    if (message.lastMessage !== void 0) {
      Message.encode(message.lastMessage, writer.uint32(90).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.guid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.chatIdentifier = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.groupId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.displayName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isGroup = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.service = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.isArchived = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }
          message.isFiltered = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }
          message.unreadCount = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.participants.push(SingleServiceAddressInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.lastMessage = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      guid: isSet8(object.guid) ? globalThis.String(object.guid) : "",
      chatIdentifier: isSet8(object.chatIdentifier) ? globalThis.String(object.chatIdentifier) : isSet8(object.chat_identifier) ? globalThis.String(object.chat_identifier) : void 0,
      groupId: isSet8(object.groupId) ? globalThis.String(object.groupId) : isSet8(object.group_id) ? globalThis.String(object.group_id) : void 0,
      displayName: isSet8(object.displayName) ? globalThis.String(object.displayName) : isSet8(object.display_name) ? globalThis.String(object.display_name) : void 0,
      isGroup: isSet8(object.isGroup) ? globalThis.Boolean(object.isGroup) : isSet8(object.is_group) ? globalThis.Boolean(object.is_group) : false,
      service: isSet8(object.service) ? chatServiceTypeFromJSON(object.service) : 0,
      isArchived: isSet8(object.isArchived) ? globalThis.Boolean(object.isArchived) : isSet8(object.is_archived) ? globalThis.Boolean(object.is_archived) : false,
      isFiltered: isSet8(object.isFiltered) ? globalThis.Boolean(object.isFiltered) : isSet8(object.is_filtered) ? globalThis.Boolean(object.is_filtered) : false,
      unreadCount: isSet8(object.unreadCount) ? globalThis.Number(object.unreadCount) : isSet8(object.unread_count) ? globalThis.Number(object.unread_count) : void 0,
      participants: globalThis.Array.isArray(object?.participants) ? object.participants.map((e) => SingleServiceAddressInfo.fromJSON(e)) : [],
      lastMessage: isSet8(object.lastMessage) ? Message.fromJSON(object.lastMessage) : isSet8(object.last_message) ? Message.fromJSON(object.last_message) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.guid !== "") {
      obj.guid = message.guid;
    }
    if (message.chatIdentifier !== void 0) {
      obj.chatIdentifier = message.chatIdentifier;
    }
    if (message.groupId !== void 0) {
      obj.groupId = message.groupId;
    }
    if (message.displayName !== void 0) {
      obj.displayName = message.displayName;
    }
    if (message.isGroup !== false) {
      obj.isGroup = message.isGroup;
    }
    if (message.service !== 0) {
      obj.service = chatServiceTypeToJSON(message.service);
    }
    if (message.isArchived !== false) {
      obj.isArchived = message.isArchived;
    }
    if (message.isFiltered !== false) {
      obj.isFiltered = message.isFiltered;
    }
    if (message.unreadCount !== void 0) {
      obj.unreadCount = Math.round(message.unreadCount);
    }
    if (message.participants?.length) {
      obj.participants = message.participants.map((e) => SingleServiceAddressInfo.toJSON(e));
    }
    if (message.lastMessage !== void 0) {
      obj.lastMessage = Message.toJSON(message.lastMessage);
    }
    return obj;
  },
  create(base) {
    return Chat.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseChat();
    message.guid = object.guid ?? "";
    message.chatIdentifier = object.chatIdentifier ?? void 0;
    message.groupId = object.groupId ?? void 0;
    message.displayName = object.displayName ?? void 0;
    message.isGroup = object.isGroup ?? false;
    message.service = object.service ?? 0;
    message.isArchived = object.isArchived ?? false;
    message.isFiltered = object.isFiltered ?? false;
    message.unreadCount = object.unreadCount ?? void 0;
    message.participants = object.participants?.map((e) => SingleServiceAddressInfo.fromPartial(e)) || [];
    message.lastMessage = object.lastMessage !== void 0 && object.lastMessage !== null ? Message.fromPartial(object.lastMessage) : void 0;
    return message;
  }
};
function createBaseChatBackgroundChanged() {
  return {};
}
var ChatBackgroundChanged = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChatBackgroundChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ChatBackgroundChanged.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseChatBackgroundChanged();
    return message;
  }
};
function createBaseChatBackgroundRemoved() {
  return {};
}
var ChatBackgroundRemoved = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChatBackgroundRemoved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ChatBackgroundRemoved.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseChatBackgroundRemoved();
    return message;
  }
};
function createBaseChatMarkedRead() {
  return {};
}
var ChatMarkedRead = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChatMarkedRead();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ChatMarkedRead.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseChatMarkedRead();
    return message;
  }
};
function createBaseChatArchived() {
  return {};
}
var ChatArchived = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChatArchived();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ChatArchived.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseChatArchived();
    return message;
  }
};
function createBaseChatUnarchived() {
  return {};
}
var ChatUnarchived = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChatUnarchived();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ChatUnarchived.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseChatUnarchived();
    return message;
  }
};
function createBaseChatChangeEvent() {
  return {
    chatGuid: "",
    occurredAt: void 0,
    actor: void 0,
    isFromMe: false,
    backgroundChanged: void 0,
    backgroundRemoved: void 0,
    markedRead: void 0,
    archived: void 0,
    unarchived: void 0
  };
}
var ChatChangeEvent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.occurredAt !== void 0) {
      Timestamp.encode(toTimestamp3(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.actor !== void 0) {
      SingleServiceAddressInfo.encode(message.actor, writer.uint32(26).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.backgroundChanged !== void 0) {
      ChatBackgroundChanged.encode(message.backgroundChanged, writer.uint32(82).fork()).join();
    }
    if (message.backgroundRemoved !== void 0) {
      ChatBackgroundRemoved.encode(message.backgroundRemoved, writer.uint32(90).fork()).join();
    }
    if (message.markedRead !== void 0) {
      ChatMarkedRead.encode(message.markedRead, writer.uint32(98).fork()).join();
    }
    if (message.archived !== void 0) {
      ChatArchived.encode(message.archived, writer.uint32(106).fork()).join();
    }
    if (message.unarchived !== void 0) {
      ChatUnarchived.encode(message.unarchived, writer.uint32(114).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChatChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.occurredAt = fromTimestamp3(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.actor = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.backgroundChanged = ChatBackgroundChanged.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.backgroundRemoved = ChatBackgroundRemoved.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }
          message.markedRead = ChatMarkedRead.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.archived = ChatArchived.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }
          message.unarchived = ChatUnarchived.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet8(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet8(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      occurredAt: isSet8(object.occurredAt) ? fromJsonTimestamp3(object.occurredAt) : isSet8(object.occurred_at) ? fromJsonTimestamp3(object.occurred_at) : void 0,
      actor: isSet8(object.actor) ? SingleServiceAddressInfo.fromJSON(object.actor) : void 0,
      isFromMe: isSet8(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet8(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      backgroundChanged: isSet8(object.backgroundChanged) ? ChatBackgroundChanged.fromJSON(object.backgroundChanged) : isSet8(object.background_changed) ? ChatBackgroundChanged.fromJSON(object.background_changed) : void 0,
      backgroundRemoved: isSet8(object.backgroundRemoved) ? ChatBackgroundRemoved.fromJSON(object.backgroundRemoved) : isSet8(object.background_removed) ? ChatBackgroundRemoved.fromJSON(object.background_removed) : void 0,
      markedRead: isSet8(object.markedRead) ? ChatMarkedRead.fromJSON(object.markedRead) : isSet8(object.marked_read) ? ChatMarkedRead.fromJSON(object.marked_read) : void 0,
      archived: isSet8(object.archived) ? ChatArchived.fromJSON(object.archived) : void 0,
      unarchived: isSet8(object.unarchived) ? ChatUnarchived.fromJSON(object.unarchived) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.occurredAt !== void 0) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.actor !== void 0) {
      obj.actor = SingleServiceAddressInfo.toJSON(message.actor);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.backgroundChanged !== void 0) {
      obj.backgroundChanged = ChatBackgroundChanged.toJSON(message.backgroundChanged);
    }
    if (message.backgroundRemoved !== void 0) {
      obj.backgroundRemoved = ChatBackgroundRemoved.toJSON(message.backgroundRemoved);
    }
    if (message.markedRead !== void 0) {
      obj.markedRead = ChatMarkedRead.toJSON(message.markedRead);
    }
    if (message.archived !== void 0) {
      obj.archived = ChatArchived.toJSON(message.archived);
    }
    if (message.unarchived !== void 0) {
      obj.unarchived = ChatUnarchived.toJSON(message.unarchived);
    }
    return obj;
  },
  create(base) {
    return ChatChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseChatChangeEvent();
    message.chatGuid = object.chatGuid ?? "";
    message.occurredAt = object.occurredAt ?? void 0;
    message.actor = object.actor !== void 0 && object.actor !== null ? SingleServiceAddressInfo.fromPartial(object.actor) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.backgroundChanged = object.backgroundChanged !== void 0 && object.backgroundChanged !== null ? ChatBackgroundChanged.fromPartial(object.backgroundChanged) : void 0;
    message.backgroundRemoved = object.backgroundRemoved !== void 0 && object.backgroundRemoved !== null ? ChatBackgroundRemoved.fromPartial(object.backgroundRemoved) : void 0;
    message.markedRead = object.markedRead !== void 0 && object.markedRead !== null ? ChatMarkedRead.fromPartial(object.markedRead) : void 0;
    message.archived = object.archived !== void 0 && object.archived !== null ? ChatArchived.fromPartial(object.archived) : void 0;
    message.unarchived = object.unarchived !== void 0 && object.unarchived !== null ? ChatUnarchived.fromPartial(object.unarchived) : void 0;
    return message;
  }
};
function toTimestamp3(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp3(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp3(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp3(Timestamp.fromJSON(o));
  }
}
function isSet8(value) {
  return value !== null && value !== void 0;
}
function createBaseHeartbeat() {
  return {};
}
var Heartbeat = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return Heartbeat.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseHeartbeat();
    return message;
  }
};

// src/generated/photon/imessage/v1/chat_service.ts
function createBaseCreateChatRequest() {
  return { addresses: [], service: 0, initialMessage: void 0, clientMessageId: void 0 };
}
var CreateChatRequest = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.addresses) {
      writer.uint32(10).string(v);
    }
    if (message.service !== 0) {
      writer.uint32(16).int32(message.service);
    }
    if (message.initialMessage !== void 0) {
      CreateChatInitialMessage.encode(message.initialMessage, writer.uint32(26).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateChatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.addresses.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.service = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.initialMessage = CreateChatInitialMessage.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      addresses: globalThis.Array.isArray(object?.addresses) ? object.addresses.map((e) => globalThis.String(e)) : [],
      service: isSet9(object.service) ? chatServiceTypeFromJSON(object.service) : 0,
      initialMessage: isSet9(object.initialMessage) ? CreateChatInitialMessage.fromJSON(object.initialMessage) : isSet9(object.initial_message) ? CreateChatInitialMessage.fromJSON(object.initial_message) : void 0,
      clientMessageId: isSet9(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet9(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.service !== 0) {
      obj.service = chatServiceTypeToJSON(message.service);
    }
    if (message.initialMessage !== void 0) {
      obj.initialMessage = CreateChatInitialMessage.toJSON(message.initialMessage);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return CreateChatRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateChatRequest();
    message.addresses = object.addresses?.map((e) => e) || [];
    message.service = object.service ?? 0;
    message.initialMessage = object.initialMessage !== void 0 && object.initialMessage !== null ? CreateChatInitialMessage.fromPartial(object.initialMessage) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseCreateChatInitialMessage() {
  return { text: "", attributedBody: void 0, effectId: void 0, subject: void 0 };
}
var CreateChatInitialMessage = {
  encode(message, writer = new BinaryWriter()) {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.attributedBody !== void 0) {
      writer.uint32(18).bytes(message.attributedBody);
    }
    if (message.effectId !== void 0) {
      writer.uint32(26).string(message.effectId);
    }
    if (message.subject !== void 0) {
      writer.uint32(34).string(message.subject);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateChatInitialMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attributedBody = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      text: isSet9(object.text) ? globalThis.String(object.text) : "",
      attributedBody: isSet9(object.attributedBody) ? bytesFromBase644(object.attributedBody) : isSet9(object.attributed_body) ? bytesFromBase644(object.attributed_body) : void 0,
      effectId: isSet9(object.effectId) ? globalThis.String(object.effectId) : isSet9(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      subject: isSet9(object.subject) ? globalThis.String(object.subject) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.attributedBody !== void 0) {
      obj.attributedBody = base64FromBytes4(message.attributedBody);
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    return obj;
  },
  create(base) {
    return CreateChatInitialMessage.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateChatInitialMessage();
    message.text = object.text ?? "";
    message.attributedBody = object.attributedBody ?? void 0;
    message.effectId = object.effectId ?? void 0;
    message.subject = object.subject ?? void 0;
    return message;
  }
};
function createBaseCreateChatResponse() {
  return { chat: void 0, initialMessage: void 0 };
}
var CreateChatResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    if (message.initialMessage !== void 0) {
      Message.encode(message.initialMessage, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateChatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.initialMessage = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chat: isSet9(object.chat) ? Chat.fromJSON(object.chat) : void 0,
      initialMessage: isSet9(object.initialMessage) ? Message.fromJSON(object.initialMessage) : isSet9(object.initial_message) ? Message.fromJSON(object.initial_message) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    if (message.initialMessage !== void 0) {
      obj.initialMessage = Message.toJSON(message.initialMessage);
    }
    return obj;
  },
  create(base) {
    return CreateChatResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateChatResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    message.initialMessage = object.initialMessage !== void 0 && object.initialMessage !== null ? Message.fromPartial(object.initialMessage) : void 0;
    return message;
  }
};
function createBaseMarkChatReadRequest() {
  return { chatGuid: "" };
}
var MarkChatReadRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMarkChatReadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return MarkChatReadRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMarkChatReadRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseSetBackgroundRequest() {
  return { chatGuid: "", data: new Uint8Array(0) };
}
var SetBackgroundRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      data: isSet9(object.data) ? bytesFromBase644(object.data) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes4(message.data);
    }
    return obj;
  },
  create(base) {
    return SetBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetBackgroundRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  }
};
function createBaseRemoveBackgroundRequest() {
  return { chatGuid: "" };
}
var RemoveBackgroundRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return RemoveBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveBackgroundRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseShareContactInfoRequest() {
  return { chatGuid: "" };
}
var ShareContactInfoRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseShareContactInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return ShareContactInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseShareContactInfoRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseSetTypingRequest() {
  return { chatGuid: "", isTyping: false };
}
var SetTypingRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.isTyping !== false) {
      writer.uint32(16).bool(message.isTyping);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetTypingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.isTyping = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      isTyping: isSet9(object.isTyping) ? globalThis.Boolean(object.isTyping) : isSet9(object.is_typing) ? globalThis.Boolean(object.is_typing) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.isTyping !== false) {
      obj.isTyping = message.isTyping;
    }
    return obj;
  },
  create(base) {
    return SetTypingRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetTypingRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.isTyping = object.isTyping ?? false;
    return message;
  }
};
function createBaseGetChatRequest() {
  return { chatGuid: "" };
}
var GetChatRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return GetChatRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseGetChatResponse() {
  return { chat: void 0 };
}
var GetChatResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet9(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return GetChatResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseGetChatCountRequest() {
  return { includeArchived: false };
}
var GetChatCountRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.includeArchived !== false) {
      writer.uint32(8).bool(message.includeArchived);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatCountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.includeArchived = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      includeArchived: isSet9(object.includeArchived) ? globalThis.Boolean(object.includeArchived) : isSet9(object.include_archived) ? globalThis.Boolean(object.include_archived) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.includeArchived !== false) {
      obj.includeArchived = message.includeArchived;
    }
    return obj;
  },
  create(base) {
    return GetChatCountRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatCountRequest();
    message.includeArchived = object.includeArchived ?? false;
    return message;
  }
};
function createBaseGetChatCountResponse() {
  return { count: 0 };
}
var GetChatCountResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.count !== 0) {
      writer.uint32(8).int64(message.count);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatCountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.count = longToNumber4(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { count: isSet9(object.count) ? globalThis.Number(object.count) : 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },
  create(base) {
    return GetChatCountResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatCountResponse();
    message.count = object.count ?? 0;
    return message;
  }
};
function createBaseHasBackgroundRequest() {
  return { chatGuid: "" };
}
var HasBackgroundRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseHasBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return HasBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseHasBackgroundRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseHasBackgroundResponse() {
  return { backgroundPresent: false };
}
var HasBackgroundResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.backgroundPresent !== false) {
      writer.uint32(8).bool(message.backgroundPresent);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseHasBackgroundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.backgroundPresent = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      backgroundPresent: isSet9(object.backgroundPresent) ? globalThis.Boolean(object.backgroundPresent) : isSet9(object.background_present) ? globalThis.Boolean(object.background_present) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.backgroundPresent !== false) {
      obj.backgroundPresent = message.backgroundPresent;
    }
    return obj;
  },
  create(base) {
    return HasBackgroundResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseHasBackgroundResponse();
    message.backgroundPresent = object.backgroundPresent ?? false;
    return message;
  }
};
function createBaseSubscribeChatEventsRequest() {
  return { chatGuid: void 0 };
}
var SubscribeChatEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== void 0) {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeChatEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet9(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet9(object.chat_guid) ? globalThis.String(object.chat_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== void 0) {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribeChatEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeChatEventsRequest();
    message.chatGuid = object.chatGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribeChatEventsResponse() {
  return { sequence: void 0, chatChanged: void 0, heartbeat: void 0 };
}
var SubscribeChatEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.chatChanged !== void 0) {
      ChatChangeEvent.encode(message.chatChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeChatEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber4(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.chatChanged = ChatChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet9(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      chatChanged: isSet9(object.chatChanged) ? ChatChangeEvent.fromJSON(object.chatChanged) : isSet9(object.chat_changed) ? ChatChangeEvent.fromJSON(object.chat_changed) : void 0,
      heartbeat: isSet9(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.chatChanged !== void 0) {
      obj.chatChanged = ChatChangeEvent.toJSON(message.chatChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribeChatEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeChatEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.chatChanged = object.chatChanged !== void 0 && object.chatChanged !== null ? ChatChangeEvent.fromPartial(object.chatChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var ChatServiceDefinition = {
  name: "ChatService",
  fullName: "photon.imessage.v1.ChatService",
  methods: {
    /** Writes */
    createChat: {
      name: "CreateChat",
      requestType: CreateChatRequest,
      requestStream: false,
      responseType: CreateChatResponse,
      responseStream: false,
      options: {}
    },
    markChatRead: {
      name: "MarkChatRead",
      requestType: MarkChatReadRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    setBackground: {
      name: "SetBackground",
      requestType: SetBackgroundRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    removeBackground: {
      name: "RemoveBackground",
      requestType: RemoveBackgroundRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    /**
     * Pushes the local user's name-and-photo card into the target chat so
     * the recipient sees it in their contact suggestions. No observable
     * local state change.
     */
    shareContactInfo: {
      name: "ShareContactInfo",
      requestType: ShareContactInfoRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    /** Transient typing-indicator write. No persisted state. */
    setTyping: {
      name: "SetTyping",
      requestType: SetTypingRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    /** Reads */
    getChat: {
      name: "GetChat",
      requestType: GetChatRequest,
      requestStream: false,
      responseType: GetChatResponse,
      responseStream: false,
      options: {}
    },
    getChatCount: {
      name: "GetChatCount",
      requestType: GetChatCountRequest,
      requestStream: false,
      responseType: GetChatCountResponse,
      responseStream: false,
      options: {}
    },
    hasBackground: {
      name: "HasBackground",
      requestType: HasBackgroundRequest,
      requestStream: false,
      responseType: HasBackgroundResponse,
      responseStream: false,
      options: {}
    },
    subscribeChatEvents: {
      name: "SubscribeChatEvents",
      requestType: SubscribeChatEventsRequest,
      requestStream: false,
      responseType: SubscribeChatEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase644(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes4(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function longToNumber4(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet9(value) {
  return value !== null && value !== void 0;
}
function createBaseGroupDisplayNameChanged() {
  return { displayName: "" };
}
var GroupDisplayNameChanged = {
  encode(message, writer = new BinaryWriter()) {
    if (message.displayName !== "") {
      writer.uint32(10).string(message.displayName);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupDisplayNameChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      displayName: isSet10(object.displayName) ? globalThis.String(object.displayName) : isSet10(object.display_name) ? globalThis.String(object.display_name) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    return obj;
  },
  create(base) {
    return GroupDisplayNameChanged.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGroupDisplayNameChanged();
    message.displayName = object.displayName ?? "";
    return message;
  }
};
function createBaseGroupParticipantAdded() {
  return { participant: void 0 };
}
var GroupParticipantAdded = {
  encode(message, writer = new BinaryWriter()) {
    if (message.participant !== void 0) {
      SingleServiceAddressInfo.encode(message.participant, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupParticipantAdded();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.participant = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      participant: isSet10(object.participant) ? SingleServiceAddressInfo.fromJSON(object.participant) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.participant !== void 0) {
      obj.participant = SingleServiceAddressInfo.toJSON(message.participant);
    }
    return obj;
  },
  create(base) {
    return GroupParticipantAdded.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGroupParticipantAdded();
    message.participant = object.participant !== void 0 && object.participant !== null ? SingleServiceAddressInfo.fromPartial(object.participant) : void 0;
    return message;
  }
};
function createBaseGroupParticipantRemoved() {
  return { participant: void 0 };
}
var GroupParticipantRemoved = {
  encode(message, writer = new BinaryWriter()) {
    if (message.participant !== void 0) {
      SingleServiceAddressInfo.encode(message.participant, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupParticipantRemoved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.participant = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      participant: isSet10(object.participant) ? SingleServiceAddressInfo.fromJSON(object.participant) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.participant !== void 0) {
      obj.participant = SingleServiceAddressInfo.toJSON(message.participant);
    }
    return obj;
  },
  create(base) {
    return GroupParticipantRemoved.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGroupParticipantRemoved();
    message.participant = object.participant !== void 0 && object.participant !== null ? SingleServiceAddressInfo.fromPartial(object.participant) : void 0;
    return message;
  }
};
function createBaseGroupParticipantLeft() {
  return { participant: void 0 };
}
var GroupParticipantLeft = {
  encode(message, writer = new BinaryWriter()) {
    if (message.participant !== void 0) {
      SingleServiceAddressInfo.encode(message.participant, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupParticipantLeft();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.participant = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      participant: isSet10(object.participant) ? SingleServiceAddressInfo.fromJSON(object.participant) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.participant !== void 0) {
      obj.participant = SingleServiceAddressInfo.toJSON(message.participant);
    }
    return obj;
  },
  create(base) {
    return GroupParticipantLeft.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGroupParticipantLeft();
    message.participant = object.participant !== void 0 && object.participant !== null ? SingleServiceAddressInfo.fromPartial(object.participant) : void 0;
    return message;
  }
};
function createBaseGroupIconChanged() {
  return {};
}
var GroupIconChanged = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupIconChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return GroupIconChanged.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseGroupIconChanged();
    return message;
  }
};
function createBaseGroupIconRemoved() {
  return {};
}
var GroupIconRemoved = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupIconRemoved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return GroupIconRemoved.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseGroupIconRemoved();
    return message;
  }
};
function createBaseGroupChangeEvent() {
  return {
    chatGuid: "",
    occurredAt: void 0,
    actor: void 0,
    isFromMe: false,
    displayNameChanged: void 0,
    participantAdded: void 0,
    participantRemoved: void 0,
    participantLeft: void 0,
    iconChanged: void 0,
    iconRemoved: void 0
  };
}
var GroupChangeEvent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.occurredAt !== void 0) {
      Timestamp.encode(toTimestamp4(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.actor !== void 0) {
      SingleServiceAddressInfo.encode(message.actor, writer.uint32(26).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.displayNameChanged !== void 0) {
      GroupDisplayNameChanged.encode(message.displayNameChanged, writer.uint32(82).fork()).join();
    }
    if (message.participantAdded !== void 0) {
      GroupParticipantAdded.encode(message.participantAdded, writer.uint32(90).fork()).join();
    }
    if (message.participantRemoved !== void 0) {
      GroupParticipantRemoved.encode(message.participantRemoved, writer.uint32(98).fork()).join();
    }
    if (message.participantLeft !== void 0) {
      GroupParticipantLeft.encode(message.participantLeft, writer.uint32(106).fork()).join();
    }
    if (message.iconChanged !== void 0) {
      GroupIconChanged.encode(message.iconChanged, writer.uint32(114).fork()).join();
    }
    if (message.iconRemoved !== void 0) {
      GroupIconRemoved.encode(message.iconRemoved, writer.uint32(122).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.occurredAt = fromTimestamp4(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.actor = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.displayNameChanged = GroupDisplayNameChanged.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.participantAdded = GroupParticipantAdded.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }
          message.participantRemoved = GroupParticipantRemoved.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.participantLeft = GroupParticipantLeft.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }
          message.iconChanged = GroupIconChanged.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }
          message.iconRemoved = GroupIconRemoved.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet10(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet10(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      occurredAt: isSet10(object.occurredAt) ? fromJsonTimestamp4(object.occurredAt) : isSet10(object.occurred_at) ? fromJsonTimestamp4(object.occurred_at) : void 0,
      actor: isSet10(object.actor) ? SingleServiceAddressInfo.fromJSON(object.actor) : void 0,
      isFromMe: isSet10(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet10(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      displayNameChanged: isSet10(object.displayNameChanged) ? GroupDisplayNameChanged.fromJSON(object.displayNameChanged) : isSet10(object.display_name_changed) ? GroupDisplayNameChanged.fromJSON(object.display_name_changed) : void 0,
      participantAdded: isSet10(object.participantAdded) ? GroupParticipantAdded.fromJSON(object.participantAdded) : isSet10(object.participant_added) ? GroupParticipantAdded.fromJSON(object.participant_added) : void 0,
      participantRemoved: isSet10(object.participantRemoved) ? GroupParticipantRemoved.fromJSON(object.participantRemoved) : isSet10(object.participant_removed) ? GroupParticipantRemoved.fromJSON(object.participant_removed) : void 0,
      participantLeft: isSet10(object.participantLeft) ? GroupParticipantLeft.fromJSON(object.participantLeft) : isSet10(object.participant_left) ? GroupParticipantLeft.fromJSON(object.participant_left) : void 0,
      iconChanged: isSet10(object.iconChanged) ? GroupIconChanged.fromJSON(object.iconChanged) : isSet10(object.icon_changed) ? GroupIconChanged.fromJSON(object.icon_changed) : void 0,
      iconRemoved: isSet10(object.iconRemoved) ? GroupIconRemoved.fromJSON(object.iconRemoved) : isSet10(object.icon_removed) ? GroupIconRemoved.fromJSON(object.icon_removed) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.occurredAt !== void 0) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.actor !== void 0) {
      obj.actor = SingleServiceAddressInfo.toJSON(message.actor);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.displayNameChanged !== void 0) {
      obj.displayNameChanged = GroupDisplayNameChanged.toJSON(message.displayNameChanged);
    }
    if (message.participantAdded !== void 0) {
      obj.participantAdded = GroupParticipantAdded.toJSON(message.participantAdded);
    }
    if (message.participantRemoved !== void 0) {
      obj.participantRemoved = GroupParticipantRemoved.toJSON(message.participantRemoved);
    }
    if (message.participantLeft !== void 0) {
      obj.participantLeft = GroupParticipantLeft.toJSON(message.participantLeft);
    }
    if (message.iconChanged !== void 0) {
      obj.iconChanged = GroupIconChanged.toJSON(message.iconChanged);
    }
    if (message.iconRemoved !== void 0) {
      obj.iconRemoved = GroupIconRemoved.toJSON(message.iconRemoved);
    }
    return obj;
  },
  create(base) {
    return GroupChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGroupChangeEvent();
    message.chatGuid = object.chatGuid ?? "";
    message.occurredAt = object.occurredAt ?? void 0;
    message.actor = object.actor !== void 0 && object.actor !== null ? SingleServiceAddressInfo.fromPartial(object.actor) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.displayNameChanged = object.displayNameChanged !== void 0 && object.displayNameChanged !== null ? GroupDisplayNameChanged.fromPartial(object.displayNameChanged) : void 0;
    message.participantAdded = object.participantAdded !== void 0 && object.participantAdded !== null ? GroupParticipantAdded.fromPartial(object.participantAdded) : void 0;
    message.participantRemoved = object.participantRemoved !== void 0 && object.participantRemoved !== null ? GroupParticipantRemoved.fromPartial(object.participantRemoved) : void 0;
    message.participantLeft = object.participantLeft !== void 0 && object.participantLeft !== null ? GroupParticipantLeft.fromPartial(object.participantLeft) : void 0;
    message.iconChanged = object.iconChanged !== void 0 && object.iconChanged !== null ? GroupIconChanged.fromPartial(object.iconChanged) : void 0;
    message.iconRemoved = object.iconRemoved !== void 0 && object.iconRemoved !== null ? GroupIconRemoved.fromPartial(object.iconRemoved) : void 0;
    return message;
  }
};
function toTimestamp4(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp4(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp4(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp4(Timestamp.fromJSON(o));
  }
}
function isSet10(value) {
  return value !== null && value !== void 0;
}
function createBasePollOption() {
  return { text: "", optionIdentifier: "", creatorHandle: void 0 };
}
var PollOption = {
  encode(message, writer = new BinaryWriter()) {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.optionIdentifier !== "") {
      writer.uint32(18).string(message.optionIdentifier);
    }
    if (message.creatorHandle !== void 0) {
      writer.uint32(26).string(message.creatorHandle);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.optionIdentifier = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.creatorHandle = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      text: isSet11(object.text) ? globalThis.String(object.text) : "",
      optionIdentifier: isSet11(object.optionIdentifier) ? globalThis.String(object.optionIdentifier) : isSet11(object.option_identifier) ? globalThis.String(object.option_identifier) : "",
      creatorHandle: isSet11(object.creatorHandle) ? globalThis.String(object.creatorHandle) : isSet11(object.creator_handle) ? globalThis.String(object.creator_handle) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.optionIdentifier !== "") {
      obj.optionIdentifier = message.optionIdentifier;
    }
    if (message.creatorHandle !== void 0) {
      obj.creatorHandle = message.creatorHandle;
    }
    return obj;
  },
  create(base) {
    return PollOption.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollOption();
    message.text = object.text ?? "";
    message.optionIdentifier = object.optionIdentifier ?? "";
    message.creatorHandle = object.creatorHandle ?? void 0;
    return message;
  }
};
function createBasePollParticipantVote() {
  return { participant: void 0, optionIdentifier: "" };
}
var PollParticipantVote = {
  encode(message, writer = new BinaryWriter()) {
    if (message.participant !== void 0) {
      SingleServiceAddressInfo.encode(message.participant, writer.uint32(10).fork()).join();
    }
    if (message.optionIdentifier !== "") {
      writer.uint32(18).string(message.optionIdentifier);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollParticipantVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.participant = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.optionIdentifier = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      participant: isSet11(object.participant) ? SingleServiceAddressInfo.fromJSON(object.participant) : void 0,
      optionIdentifier: isSet11(object.optionIdentifier) ? globalThis.String(object.optionIdentifier) : isSet11(object.option_identifier) ? globalThis.String(object.option_identifier) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.participant !== void 0) {
      obj.participant = SingleServiceAddressInfo.toJSON(message.participant);
    }
    if (message.optionIdentifier !== "") {
      obj.optionIdentifier = message.optionIdentifier;
    }
    return obj;
  },
  create(base) {
    return PollParticipantVote.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollParticipantVote();
    message.participant = object.participant !== void 0 && object.participant !== null ? SingleServiceAddressInfo.fromPartial(object.participant) : void 0;
    message.optionIdentifier = object.optionIdentifier ?? "";
    return message;
  }
};
function createBasePollInfo() {
  return { pollMessageGuid: "", chatGuid: "", title: "", options: [], votes: [] };
}
var PollInfo = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.chatGuid !== "") {
      writer.uint32(18).string(message.chatGuid);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    for (const v of message.options) {
      PollOption.encode(v, writer.uint32(34).fork()).join();
    }
    for (const v of message.votes) {
      PollParticipantVote.encode(v, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.options.push(PollOption.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.votes.push(PollParticipantVote.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet11(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet11(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      chatGuid: isSet11(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet11(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      title: isSet11(object.title) ? globalThis.String(object.title) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e) => PollOption.fromJSON(e)) : [],
      votes: globalThis.Array.isArray(object?.votes) ? object.votes.map((e) => PollParticipantVote.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.options?.length) {
      obj.options = message.options.map((e) => PollOption.toJSON(e));
    }
    if (message.votes?.length) {
      obj.votes = message.votes.map((e) => PollParticipantVote.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return PollInfo.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollInfo();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.chatGuid = object.chatGuid ?? "";
    message.title = object.title ?? "";
    message.options = object.options?.map((e) => PollOption.fromPartial(e)) || [];
    message.votes = object.votes?.map((e) => PollParticipantVote.fromPartial(e)) || [];
    return message;
  }
};
function createBasePollCreated() {
  return { title: "", options: [] };
}
var PollCreated = {
  encode(message, writer = new BinaryWriter()) {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    for (const v of message.options) {
      PollOption.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.options.push(PollOption.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      title: isSet11(object.title) ? globalThis.String(object.title) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e) => PollOption.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.options?.length) {
      obj.options = message.options.map((e) => PollOption.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return PollCreated.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollCreated();
    message.title = object.title ?? "";
    message.options = object.options?.map((e) => PollOption.fromPartial(e)) || [];
    return message;
  }
};
function createBasePollOptionAdded() {
  return { title: "", options: [] };
}
var PollOptionAdded = {
  encode(message, writer = new BinaryWriter()) {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    for (const v of message.options) {
      PollOption.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollOptionAdded();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.options.push(PollOption.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      title: isSet11(object.title) ? globalThis.String(object.title) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e) => PollOption.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.options?.length) {
      obj.options = message.options.map((e) => PollOption.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return PollOptionAdded.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollOptionAdded();
    message.title = object.title ?? "";
    message.options = object.options?.map((e) => PollOption.fromPartial(e)) || [];
    return message;
  }
};
function createBasePollVoted() {
  return { optionIdentifier: "" };
}
var PollVoted = {
  encode(message, writer = new BinaryWriter()) {
    if (message.optionIdentifier !== "") {
      writer.uint32(10).string(message.optionIdentifier);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollVoted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.optionIdentifier = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      optionIdentifier: isSet11(object.optionIdentifier) ? globalThis.String(object.optionIdentifier) : isSet11(object.option_identifier) ? globalThis.String(object.option_identifier) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.optionIdentifier !== "") {
      obj.optionIdentifier = message.optionIdentifier;
    }
    return obj;
  },
  create(base) {
    return PollVoted.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollVoted();
    message.optionIdentifier = object.optionIdentifier ?? "";
    return message;
  }
};
function createBasePollUnvoted() {
  return { optionIdentifier: "" };
}
var PollUnvoted = {
  encode(message, writer = new BinaryWriter()) {
    if (message.optionIdentifier !== "") {
      writer.uint32(10).string(message.optionIdentifier);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollUnvoted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.optionIdentifier = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      optionIdentifier: isSet11(object.optionIdentifier) ? globalThis.String(object.optionIdentifier) : isSet11(object.option_identifier) ? globalThis.String(object.option_identifier) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.optionIdentifier !== "") {
      obj.optionIdentifier = message.optionIdentifier;
    }
    return obj;
  },
  create(base) {
    return PollUnvoted.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollUnvoted();
    message.optionIdentifier = object.optionIdentifier ?? "";
    return message;
  }
};
function createBasePollChangeEvent() {
  return {
    chatGuid: "",
    pollMessageGuid: "",
    occurredAt: void 0,
    actor: void 0,
    isFromMe: false,
    created: void 0,
    optionAdded: void 0,
    voted: void 0,
    unvoted: void 0
  };
}
var PollChangeEvent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.pollMessageGuid !== "") {
      writer.uint32(18).string(message.pollMessageGuid);
    }
    if (message.occurredAt !== void 0) {
      Timestamp.encode(toTimestamp5(message.occurredAt), writer.uint32(26).fork()).join();
    }
    if (message.actor !== void 0) {
      SingleServiceAddressInfo.encode(message.actor, writer.uint32(34).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(40).bool(message.isFromMe);
    }
    if (message.created !== void 0) {
      PollCreated.encode(message.created, writer.uint32(82).fork()).join();
    }
    if (message.optionAdded !== void 0) {
      PollOptionAdded.encode(message.optionAdded, writer.uint32(90).fork()).join();
    }
    if (message.voted !== void 0) {
      PollVoted.encode(message.voted, writer.uint32(98).fork()).join();
    }
    if (message.unvoted !== void 0) {
      PollUnvoted.encode(message.unvoted, writer.uint32(106).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.occurredAt = fromTimestamp5(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.actor = SingleServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.created = PollCreated.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.optionAdded = PollOptionAdded.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }
          message.voted = PollVoted.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.unvoted = PollUnvoted.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet11(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet11(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      pollMessageGuid: isSet11(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet11(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      occurredAt: isSet11(object.occurredAt) ? fromJsonTimestamp5(object.occurredAt) : isSet11(object.occurred_at) ? fromJsonTimestamp5(object.occurred_at) : void 0,
      actor: isSet11(object.actor) ? SingleServiceAddressInfo.fromJSON(object.actor) : void 0,
      isFromMe: isSet11(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet11(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      created: isSet11(object.created) ? PollCreated.fromJSON(object.created) : void 0,
      optionAdded: isSet11(object.optionAdded) ? PollOptionAdded.fromJSON(object.optionAdded) : isSet11(object.option_added) ? PollOptionAdded.fromJSON(object.option_added) : void 0,
      voted: isSet11(object.voted) ? PollVoted.fromJSON(object.voted) : void 0,
      unvoted: isSet11(object.unvoted) ? PollUnvoted.fromJSON(object.unvoted) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.occurredAt !== void 0) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.actor !== void 0) {
      obj.actor = SingleServiceAddressInfo.toJSON(message.actor);
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.created !== void 0) {
      obj.created = PollCreated.toJSON(message.created);
    }
    if (message.optionAdded !== void 0) {
      obj.optionAdded = PollOptionAdded.toJSON(message.optionAdded);
    }
    if (message.voted !== void 0) {
      obj.voted = PollVoted.toJSON(message.voted);
    }
    if (message.unvoted !== void 0) {
      obj.unvoted = PollUnvoted.toJSON(message.unvoted);
    }
    return obj;
  },
  create(base) {
    return PollChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollChangeEvent();
    message.chatGuid = object.chatGuid ?? "";
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.occurredAt = object.occurredAt ?? void 0;
    message.actor = object.actor !== void 0 && object.actor !== null ? SingleServiceAddressInfo.fromPartial(object.actor) : void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.created = object.created !== void 0 && object.created !== null ? PollCreated.fromPartial(object.created) : void 0;
    message.optionAdded = object.optionAdded !== void 0 && object.optionAdded !== null ? PollOptionAdded.fromPartial(object.optionAdded) : void 0;
    message.voted = object.voted !== void 0 && object.voted !== null ? PollVoted.fromPartial(object.voted) : void 0;
    message.unvoted = object.unvoted !== void 0 && object.unvoted !== null ? PollUnvoted.fromPartial(object.unvoted) : void 0;
    return message;
  }
};
function toTimestamp5(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp5(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp5(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp5(Timestamp.fromJSON(o));
  }
}
function isSet11(value) {
  return value !== null && value !== void 0;
}

// src/generated/photon/imessage/v1/event_service.ts
function createBaseCatchUpEventsRequest() {
  return { afterSequence: void 0 };
}
var CatchUpEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.afterSequence !== void 0) {
      writer.uint32(8).uint64(message.afterSequence);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCatchUpEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.afterSequence = longToNumber5(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      afterSequence: isSet12(object.afterSequence) ? globalThis.Number(object.afterSequence) : isSet12(object.after_sequence) ? globalThis.Number(object.after_sequence) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.afterSequence !== void 0) {
      obj.afterSequence = Math.round(message.afterSequence);
    }
    return obj;
  },
  create(base) {
    return CatchUpEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCatchUpEventsRequest();
    message.afterSequence = object.afterSequence ?? void 0;
    return message;
  }
};
function createBaseCatchUpEventsComplete() {
  return { headSequence: 0 };
}
var CatchUpEventsComplete = {
  encode(message, writer = new BinaryWriter()) {
    if (message.headSequence !== 0) {
      writer.uint32(8).uint64(message.headSequence);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCatchUpEventsComplete();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.headSequence = longToNumber5(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      headSequence: isSet12(object.headSequence) ? globalThis.Number(object.headSequence) : isSet12(object.head_sequence) ? globalThis.Number(object.head_sequence) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.headSequence !== 0) {
      obj.headSequence = Math.round(message.headSequence);
    }
    return obj;
  },
  create(base) {
    return CatchUpEventsComplete.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCatchUpEventsComplete();
    message.headSequence = object.headSequence ?? 0;
    return message;
  }
};
function createBaseCatchUpEventsResponse() {
  return {
    sequence: void 0,
    messageChanged: void 0,
    groupChanged: void 0,
    pollChanged: void 0,
    chatChanged: void 0,
    complete: void 0,
    heartbeat: void 0
  };
}
var CatchUpEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.messageChanged !== void 0) {
      MessageChangeEvent.encode(message.messageChanged, writer.uint32(82).fork()).join();
    }
    if (message.groupChanged !== void 0) {
      GroupChangeEvent.encode(message.groupChanged, writer.uint32(90).fork()).join();
    }
    if (message.pollChanged !== void 0) {
      PollChangeEvent.encode(message.pollChanged, writer.uint32(98).fork()).join();
    }
    if (message.chatChanged !== void 0) {
      ChatChangeEvent.encode(message.chatChanged, writer.uint32(106).fork()).join();
    }
    if (message.complete !== void 0) {
      CatchUpEventsComplete.encode(message.complete, writer.uint32(162).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCatchUpEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber5(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.messageChanged = MessageChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.groupChanged = GroupChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }
          message.pollChanged = PollChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.chatChanged = ChatChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }
          message.complete = CatchUpEventsComplete.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet12(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      messageChanged: isSet12(object.messageChanged) ? MessageChangeEvent.fromJSON(object.messageChanged) : isSet12(object.message_changed) ? MessageChangeEvent.fromJSON(object.message_changed) : void 0,
      groupChanged: isSet12(object.groupChanged) ? GroupChangeEvent.fromJSON(object.groupChanged) : isSet12(object.group_changed) ? GroupChangeEvent.fromJSON(object.group_changed) : void 0,
      pollChanged: isSet12(object.pollChanged) ? PollChangeEvent.fromJSON(object.pollChanged) : isSet12(object.poll_changed) ? PollChangeEvent.fromJSON(object.poll_changed) : void 0,
      chatChanged: isSet12(object.chatChanged) ? ChatChangeEvent.fromJSON(object.chatChanged) : isSet12(object.chat_changed) ? ChatChangeEvent.fromJSON(object.chat_changed) : void 0,
      complete: isSet12(object.complete) ? CatchUpEventsComplete.fromJSON(object.complete) : void 0,
      heartbeat: isSet12(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.messageChanged !== void 0) {
      obj.messageChanged = MessageChangeEvent.toJSON(message.messageChanged);
    }
    if (message.groupChanged !== void 0) {
      obj.groupChanged = GroupChangeEvent.toJSON(message.groupChanged);
    }
    if (message.pollChanged !== void 0) {
      obj.pollChanged = PollChangeEvent.toJSON(message.pollChanged);
    }
    if (message.chatChanged !== void 0) {
      obj.chatChanged = ChatChangeEvent.toJSON(message.chatChanged);
    }
    if (message.complete !== void 0) {
      obj.complete = CatchUpEventsComplete.toJSON(message.complete);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return CatchUpEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCatchUpEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.messageChanged = object.messageChanged !== void 0 && object.messageChanged !== null ? MessageChangeEvent.fromPartial(object.messageChanged) : void 0;
    message.groupChanged = object.groupChanged !== void 0 && object.groupChanged !== null ? GroupChangeEvent.fromPartial(object.groupChanged) : void 0;
    message.pollChanged = object.pollChanged !== void 0 && object.pollChanged !== null ? PollChangeEvent.fromPartial(object.pollChanged) : void 0;
    message.chatChanged = object.chatChanged !== void 0 && object.chatChanged !== null ? ChatChangeEvent.fromPartial(object.chatChanged) : void 0;
    message.complete = object.complete !== void 0 && object.complete !== null ? CatchUpEventsComplete.fromPartial(object.complete) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var EventServiceDefinition = {
  name: "EventService",
  fullName: "photon.imessage.v1.EventService",
  methods: {
    catchUpEvents: {
      name: "CatchUpEvents",
      requestType: CatchUpEventsRequest,
      requestStream: false,
      responseType: CatchUpEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function longToNumber5(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet12(value) {
  return value !== null && value !== void 0;
}
function createBaseSetDisplayNameRequest() {
  return { chatGuid: "", displayName: "", clientMessageId: void 0 };
}
var SetDisplayNameRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetDisplayNameRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.displayName = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      displayName: isSet13(object.displayName) ? globalThis.String(object.displayName) : isSet13(object.display_name) ? globalThis.String(object.display_name) : "",
      clientMessageId: isSet13(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet13(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SetDisplayNameRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetDisplayNameRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.displayName = object.displayName ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSetDisplayNameResponse() {
  return { chat: void 0 };
}
var SetDisplayNameResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetDisplayNameResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet13(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return SetDisplayNameResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetDisplayNameResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseAddParticipantsRequest() {
  return { chatGuid: "", addresses: [], clientMessageId: void 0 };
}
var AddParticipantsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    for (const v of message.addresses) {
      writer.uint32(18).string(v);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddParticipantsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.addresses.push(reader.string());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      addresses: globalThis.Array.isArray(object?.addresses) ? object.addresses.map((e) => globalThis.String(e)) : [],
      clientMessageId: isSet13(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet13(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return AddParticipantsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddParticipantsRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.addresses = object.addresses?.map((e) => e) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseAddParticipantsResponse() {
  return { chat: void 0 };
}
var AddParticipantsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddParticipantsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet13(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return AddParticipantsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddParticipantsResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseRemoveParticipantsRequest() {
  return { chatGuid: "", addresses: [], clientMessageId: void 0 };
}
var RemoveParticipantsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    for (const v of message.addresses) {
      writer.uint32(18).string(v);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveParticipantsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.addresses.push(reader.string());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      addresses: globalThis.Array.isArray(object?.addresses) ? object.addresses.map((e) => globalThis.String(e)) : [],
      clientMessageId: isSet13(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet13(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return RemoveParticipantsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveParticipantsRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.addresses = object.addresses?.map((e) => e) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseRemoveParticipantsResponse() {
  return { chat: void 0 };
}
var RemoveParticipantsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveParticipantsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet13(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return RemoveParticipantsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveParticipantsResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseLeaveGroupRequest() {
  return { chatGuid: "", clientMessageId: void 0 };
}
var LeaveGroupRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseLeaveGroupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      clientMessageId: isSet13(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet13(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return LeaveGroupRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseLeaveGroupRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSetIconRequest() {
  return { chatGuid: "", data: new Uint8Array(0), clientMessageId: void 0 };
}
var SetIconRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetIconRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      data: isSet13(object.data) ? bytesFromBase645(object.data) : new Uint8Array(0),
      clientMessageId: isSet13(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet13(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes5(message.data);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SetIconRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetIconRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseRemoveIconRequest() {
  return { chatGuid: "", clientMessageId: void 0 };
}
var RemoveIconRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveIconRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      clientMessageId: isSet13(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet13(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return RemoveIconRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveIconRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseGetIconRequest() {
  return { chatGuid: "" };
}
var GetIconRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIconRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return GetIconRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIconRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseGetIconResponse() {
  return { data: new Uint8Array(0), mimeType: "" };
}
var GetIconResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIconResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      data: isSet13(object.data) ? bytesFromBase645(object.data) : new Uint8Array(0),
      mimeType: isSet13(object.mimeType) ? globalThis.String(object.mimeType) : isSet13(object.mime_type) ? globalThis.String(object.mime_type) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes5(message.data);
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },
  create(base) {
    return GetIconResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIconResponse();
    message.data = object.data ?? new Uint8Array(0);
    message.mimeType = object.mimeType ?? "";
    return message;
  }
};
function createBaseSubscribeGroupEventsRequest() {
  return { chatGuid: void 0 };
}
var SubscribeGroupEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== void 0) {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeGroupEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet13(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet13(object.chat_guid) ? globalThis.String(object.chat_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== void 0) {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribeGroupEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeGroupEventsRequest();
    message.chatGuid = object.chatGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribeGroupEventsResponse() {
  return { sequence: void 0, groupChanged: void 0, heartbeat: void 0 };
}
var SubscribeGroupEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.groupChanged !== void 0) {
      GroupChangeEvent.encode(message.groupChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeGroupEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber6(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.groupChanged = GroupChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet13(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      groupChanged: isSet13(object.groupChanged) ? GroupChangeEvent.fromJSON(object.groupChanged) : isSet13(object.group_changed) ? GroupChangeEvent.fromJSON(object.group_changed) : void 0,
      heartbeat: isSet13(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.groupChanged !== void 0) {
      obj.groupChanged = GroupChangeEvent.toJSON(message.groupChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribeGroupEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeGroupEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.groupChanged = object.groupChanged !== void 0 && object.groupChanged !== null ? GroupChangeEvent.fromPartial(object.groupChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var GroupServiceDefinition = {
  name: "GroupService",
  fullName: "photon.imessage.v1.GroupService",
  methods: {
    /** Writes */
    setDisplayName: {
      name: "SetDisplayName",
      requestType: SetDisplayNameRequest,
      requestStream: false,
      responseType: SetDisplayNameResponse,
      responseStream: false,
      options: {}
    },
    addParticipants: {
      name: "AddParticipants",
      requestType: AddParticipantsRequest,
      requestStream: false,
      responseType: AddParticipantsResponse,
      responseStream: false,
      options: {}
    },
    removeParticipants: {
      name: "RemoveParticipants",
      requestType: RemoveParticipantsRequest,
      requestStream: false,
      responseType: RemoveParticipantsResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Local user voluntarily leaves the group. The chat ceases to be
     * visible from this device, hence no Chat snapshot.
     */
    leaveGroup: {
      name: "LeaveGroup",
      requestType: LeaveGroupRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    setIcon: {
      name: "SetIcon",
      requestType: SetIconRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    removeIcon: {
      name: "RemoveIcon",
      requestType: RemoveIconRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    /** Reads */
    getIcon: {
      name: "GetIcon",
      requestType: GetIconRequest,
      requestStream: false,
      responseType: GetIconResponse,
      responseStream: false,
      options: {}
    },
    /** Streams */
    subscribeGroupEvents: {
      name: "SubscribeGroupEvents",
      requestType: SubscribeGroupEventsRequest,
      requestStream: false,
      responseType: SubscribeGroupEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase645(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes5(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function longToNumber6(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet13(value) {
  return value !== null && value !== void 0;
}
function createBaseListSharedFriendLocationsRequest() {
  return {};
}
var ListSharedFriendLocationsRequest = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListSharedFriendLocationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ListSharedFriendLocationsRequest.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseListSharedFriendLocationsRequest();
    return message;
  }
};
function createBaseListSharedFriendLocationsResponse() {
  return { locations: [] };
}
var ListSharedFriendLocationsResponse = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.locations) {
      SharedFriendLocation.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListSharedFriendLocationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.locations.push(SharedFriendLocation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      locations: globalThis.Array.isArray(object?.locations) ? object.locations.map((e) => SharedFriendLocation.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.locations?.length) {
      obj.locations = message.locations.map((e) => SharedFriendLocation.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ListSharedFriendLocationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListSharedFriendLocationsResponse();
    message.locations = object.locations?.map((e) => SharedFriendLocation.fromPartial(e)) || [];
    return message;
  }
};
function createBaseGetSharedFriendLocationRequest() {
  return { address: "" };
}
var GetSharedFriendLocationRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSharedFriendLocationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet14(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetSharedFriendLocationRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSharedFriendLocationRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetSharedFriendLocationResponse() {
  return { location: void 0 };
}
var GetSharedFriendLocationResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.location !== void 0) {
      SharedFriendLocation.encode(message.location, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSharedFriendLocationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.location = SharedFriendLocation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { location: isSet14(object.location) ? SharedFriendLocation.fromJSON(object.location) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.location !== void 0) {
      obj.location = SharedFriendLocation.toJSON(message.location);
    }
    return obj;
  },
  create(base) {
    return GetSharedFriendLocationResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSharedFriendLocationResponse();
    message.location = object.location !== void 0 && object.location !== null ? SharedFriendLocation.fromPartial(object.location) : void 0;
    return message;
  }
};
function createBaseRequestFriendLocationSharingRequest() {
  return { chatGuid: "", address: "", clientMessageId: void 0 };
}
var RequestFriendLocationSharingRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRequestFriendLocationSharingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet14(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet14(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      address: isSet14(object.address) ? globalThis.String(object.address) : "",
      clientMessageId: isSet14(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet14(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return RequestFriendLocationSharingRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRequestFriendLocationSharingRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.address = object.address ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseRequestFriendLocationSharingResponse() {
  return { address: "", status: "", reason: void 0, messageGuid: void 0 };
}
var RequestFriendLocationSharingResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.reason !== void 0) {
      writer.uint32(26).string(message.reason);
    }
    if (message.messageGuid !== void 0) {
      writer.uint32(34).string(message.messageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRequestFriendLocationSharingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet14(object.address) ? globalThis.String(object.address) : "",
      status: isSet14(object.status) ? globalThis.String(object.status) : "",
      reason: isSet14(object.reason) ? globalThis.String(object.reason) : void 0,
      messageGuid: isSet14(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet14(object.message_guid) ? globalThis.String(object.message_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.reason !== void 0) {
      obj.reason = message.reason;
    }
    if (message.messageGuid !== void 0) {
      obj.messageGuid = message.messageGuid;
    }
    return obj;
  },
  create(base) {
    return RequestFriendLocationSharingResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRequestFriendLocationSharingResponse();
    message.address = object.address ?? "";
    message.status = object.status ?? "";
    message.reason = object.reason ?? void 0;
    message.messageGuid = object.messageGuid ?? void 0;
    return message;
  }
};
function createBaseWatchSharedFriendLocationsRequest() {
  return { address: void 0 };
}
var WatchSharedFriendLocationsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== void 0) {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWatchSharedFriendLocationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet14(object.address) ? globalThis.String(object.address) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== void 0) {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return WatchSharedFriendLocationsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseWatchSharedFriendLocationsRequest();
    message.address = object.address ?? void 0;
    return message;
  }
};
function createBaseWatchSharedFriendLocationsResponse() {
  return { locationUpdated: void 0, heartbeat: void 0 };
}
var WatchSharedFriendLocationsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.locationUpdated !== void 0) {
      SharedFriendLocationUpdated.encode(message.locationUpdated, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWatchSharedFriendLocationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.locationUpdated = SharedFriendLocationUpdated.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      locationUpdated: isSet14(object.locationUpdated) ? SharedFriendLocationUpdated.fromJSON(object.locationUpdated) : isSet14(object.location_updated) ? SharedFriendLocationUpdated.fromJSON(object.location_updated) : void 0,
      heartbeat: isSet14(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.locationUpdated !== void 0) {
      obj.locationUpdated = SharedFriendLocationUpdated.toJSON(message.locationUpdated);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return WatchSharedFriendLocationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseWatchSharedFriendLocationsResponse();
    message.locationUpdated = object.locationUpdated !== void 0 && object.locationUpdated !== null ? SharedFriendLocationUpdated.fromPartial(object.locationUpdated) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var LocationServiceDefinition = {
  name: "LocationService",
  fullName: "photon.imessage.v1.LocationService",
  methods: {
    listSharedFriendLocations: {
      name: "ListSharedFriendLocations",
      requestType: ListSharedFriendLocationsRequest,
      requestStream: false,
      responseType: ListSharedFriendLocationsResponse,
      responseStream: false,
      options: {}
    },
    getSharedFriendLocation: {
      name: "GetSharedFriendLocation",
      requestType: GetSharedFriendLocationRequest,
      requestStream: false,
      responseType: GetSharedFriendLocationResponse,
      responseStream: false,
      options: {}
    },
    requestFriendLocationSharing: {
      name: "RequestFriendLocationSharing",
      requestType: RequestFriendLocationSharingRequest,
      requestStream: false,
      responseType: RequestFriendLocationSharingResponse,
      responseStream: false,
      options: {}
    },
    watchSharedFriendLocations: {
      name: "WatchSharedFriendLocations",
      requestType: WatchSharedFriendLocationsRequest,
      requestStream: false,
      responseType: WatchSharedFriendLocationsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function isSet14(value) {
  return value !== null && value !== void 0;
}
function createBaseMessageTarget() {
  return { chatGuid: "", messageGuid: "", targetPartIndex: void 0 };
}
var MessageTarget = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(24).int32(message.targetPartIndex);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      messageGuid: isSet15(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet15(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet15(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet15(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    return obj;
  },
  create(base) {
    return MessageTarget.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageTarget();
    message.chatGuid = object.chatGuid ?? "";
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    return message;
  }
};
function createBaseSendTextMessageRequest() {
  return {
    chatGuid: "",
    text: "",
    replyTo: void 0,
    subject: void 0,
    effectId: void 0,
    enableDataDetection: void 0,
    enableLinkPreview: void 0,
    formatting: [],
    clientMessageId: void 0
  };
}
var SendTextMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.replyTo !== void 0) {
      ReplyTarget.encode(message.replyTo, writer.uint32(26).fork()).join();
    }
    if (message.subject !== void 0) {
      writer.uint32(34).string(message.subject);
    }
    if (message.effectId !== void 0) {
      writer.uint32(42).string(message.effectId);
    }
    if (message.enableDataDetection !== void 0) {
      writer.uint32(48).bool(message.enableDataDetection);
    }
    if (message.enableLinkPreview !== void 0) {
      writer.uint32(56).bool(message.enableLinkPreview);
    }
    for (const v of message.formatting) {
      TextFormat.encode(v, writer.uint32(66).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendTextMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.replyTo = ReplyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.enableDataDetection = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.enableLinkPreview = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.formatting.push(TextFormat.decode(reader, reader.uint32()));
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      text: isSet15(object.text) ? globalThis.String(object.text) : "",
      replyTo: isSet15(object.replyTo) ? ReplyTarget.fromJSON(object.replyTo) : isSet15(object.reply_to) ? ReplyTarget.fromJSON(object.reply_to) : void 0,
      subject: isSet15(object.subject) ? globalThis.String(object.subject) : void 0,
      effectId: isSet15(object.effectId) ? globalThis.String(object.effectId) : isSet15(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      enableDataDetection: isSet15(object.enableDataDetection) ? globalThis.Boolean(object.enableDataDetection) : isSet15(object.enable_data_detection) ? globalThis.Boolean(object.enable_data_detection) : void 0,
      enableLinkPreview: isSet15(object.enableLinkPreview) ? globalThis.Boolean(object.enableLinkPreview) : isSet15(object.enable_link_preview) ? globalThis.Boolean(object.enable_link_preview) : void 0,
      formatting: globalThis.Array.isArray(object?.formatting) ? object.formatting.map((e) => TextFormat.fromJSON(e)) : [],
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.replyTo !== void 0) {
      obj.replyTo = ReplyTarget.toJSON(message.replyTo);
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.enableDataDetection !== void 0) {
      obj.enableDataDetection = message.enableDataDetection;
    }
    if (message.enableLinkPreview !== void 0) {
      obj.enableLinkPreview = message.enableLinkPreview;
    }
    if (message.formatting?.length) {
      obj.formatting = message.formatting.map((e) => TextFormat.toJSON(e));
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendTextMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendTextMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.text = object.text ?? "";
    message.replyTo = object.replyTo !== void 0 && object.replyTo !== null ? ReplyTarget.fromPartial(object.replyTo) : void 0;
    message.subject = object.subject ?? void 0;
    message.effectId = object.effectId ?? void 0;
    message.enableDataDetection = object.enableDataDetection ?? void 0;
    message.enableLinkPreview = object.enableLinkPreview ?? void 0;
    message.formatting = object.formatting?.map((e) => TextFormat.fromPartial(e)) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSendAttachmentMessageRequest() {
  return {
    chatGuid: "",
    attachment: void 0,
    replyTo: void 0,
    effectId: void 0,
    isAudioMessage: void 0,
    clientMessageId: void 0
  };
}
var SendAttachmentMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.attachment !== void 0) {
      AttachmentRef.encode(message.attachment, writer.uint32(18).fork()).join();
    }
    if (message.replyTo !== void 0) {
      ReplyTarget.encode(message.replyTo, writer.uint32(26).fork()).join();
    }
    if (message.effectId !== void 0) {
      writer.uint32(34).string(message.effectId);
    }
    if (message.isAudioMessage !== void 0) {
      writer.uint32(40).bool(message.isAudioMessage);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendAttachmentMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attachment = AttachmentRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.replyTo = ReplyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isAudioMessage = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      attachment: isSet15(object.attachment) ? AttachmentRef.fromJSON(object.attachment) : void 0,
      replyTo: isSet15(object.replyTo) ? ReplyTarget.fromJSON(object.replyTo) : isSet15(object.reply_to) ? ReplyTarget.fromJSON(object.reply_to) : void 0,
      effectId: isSet15(object.effectId) ? globalThis.String(object.effectId) : isSet15(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      isAudioMessage: isSet15(object.isAudioMessage) ? globalThis.Boolean(object.isAudioMessage) : isSet15(object.is_audio_message) ? globalThis.Boolean(object.is_audio_message) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentRef.toJSON(message.attachment);
    }
    if (message.replyTo !== void 0) {
      obj.replyTo = ReplyTarget.toJSON(message.replyTo);
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.isAudioMessage !== void 0) {
      obj.isAudioMessage = message.isAudioMessage;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendAttachmentMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendAttachmentMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentRef.fromPartial(object.attachment) : void 0;
    message.replyTo = object.replyTo !== void 0 && object.replyTo !== null ? ReplyTarget.fromPartial(object.replyTo) : void 0;
    message.effectId = object.effectId ?? void 0;
    message.isAudioMessage = object.isAudioMessage ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSendMultipartMessageRequest() {
  return {
    chatGuid: "",
    parts: [],
    replyTo: void 0,
    subject: void 0,
    effectId: void 0,
    enableDataDetection: void 0,
    clientMessageId: void 0
  };
}
var SendMultipartMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    for (const v of message.parts) {
      MessagePart.encode(v, writer.uint32(18).fork()).join();
    }
    if (message.replyTo !== void 0) {
      ReplyTarget.encode(message.replyTo, writer.uint32(26).fork()).join();
    }
    if (message.subject !== void 0) {
      writer.uint32(34).string(message.subject);
    }
    if (message.effectId !== void 0) {
      writer.uint32(42).string(message.effectId);
    }
    if (message.enableDataDetection !== void 0) {
      writer.uint32(48).bool(message.enableDataDetection);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendMultipartMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.parts.push(MessagePart.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.replyTo = ReplyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.enableDataDetection = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      parts: globalThis.Array.isArray(object?.parts) ? object.parts.map((e) => MessagePart.fromJSON(e)) : [],
      replyTo: isSet15(object.replyTo) ? ReplyTarget.fromJSON(object.replyTo) : isSet15(object.reply_to) ? ReplyTarget.fromJSON(object.reply_to) : void 0,
      subject: isSet15(object.subject) ? globalThis.String(object.subject) : void 0,
      effectId: isSet15(object.effectId) ? globalThis.String(object.effectId) : isSet15(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      enableDataDetection: isSet15(object.enableDataDetection) ? globalThis.Boolean(object.enableDataDetection) : isSet15(object.enable_data_detection) ? globalThis.Boolean(object.enable_data_detection) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.parts?.length) {
      obj.parts = message.parts.map((e) => MessagePart.toJSON(e));
    }
    if (message.replyTo !== void 0) {
      obj.replyTo = ReplyTarget.toJSON(message.replyTo);
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.enableDataDetection !== void 0) {
      obj.enableDataDetection = message.enableDataDetection;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendMultipartMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendMultipartMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.parts = object.parts?.map((e) => MessagePart.fromPartial(e)) || [];
    message.replyTo = object.replyTo !== void 0 && object.replyTo !== null ? ReplyTarget.fromPartial(object.replyTo) : void 0;
    message.subject = object.subject ?? void 0;
    message.effectId = object.effectId ?? void 0;
    message.enableDataDetection = object.enableDataDetection ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSendMiniAppMessageRequest() {
  return { chatGuid: "", url: "", preview: void 0, clientMessageId: void 0 };
}
var SendMiniAppMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.preview !== void 0) {
      MiniAppPreview.encode(message.preview, writer.uint32(26).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendMiniAppMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.preview = MiniAppPreview.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      url: isSet15(object.url) ? globalThis.String(object.url) : "",
      preview: isSet15(object.preview) ? MiniAppPreview.fromJSON(object.preview) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.preview !== void 0) {
      obj.preview = MiniAppPreview.toJSON(message.preview);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendMiniAppMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendMiniAppMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.url = object.url ?? "";
    message.preview = object.preview !== void 0 && object.preview !== null ? MiniAppPreview.fromPartial(object.preview) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseMiniAppPreview() {
  return {
    title: "",
    subtitle: void 0,
    body: void 0,
    imageJpeg: void 0,
    caption: void 0,
    footer: void 0,
    detail: void 0,
    summary: void 0
  };
}
var MiniAppPreview = {
  encode(message, writer = new BinaryWriter()) {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.subtitle !== void 0) {
      writer.uint32(18).string(message.subtitle);
    }
    if (message.body !== void 0) {
      writer.uint32(26).string(message.body);
    }
    if (message.imageJpeg !== void 0) {
      writer.uint32(34).bytes(message.imageJpeg);
    }
    if (message.caption !== void 0) {
      writer.uint32(42).string(message.caption);
    }
    if (message.footer !== void 0) {
      writer.uint32(50).string(message.footer);
    }
    if (message.detail !== void 0) {
      writer.uint32(58).string(message.detail);
    }
    if (message.summary !== void 0) {
      writer.uint32(66).string(message.summary);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMiniAppPreview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.subtitle = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.body = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.imageJpeg = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.caption = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.footer = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.detail = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      title: isSet15(object.title) ? globalThis.String(object.title) : "",
      subtitle: isSet15(object.subtitle) ? globalThis.String(object.subtitle) : void 0,
      body: isSet15(object.body) ? globalThis.String(object.body) : void 0,
      imageJpeg: isSet15(object.imageJpeg) ? bytesFromBase646(object.imageJpeg) : isSet15(object.image_jpeg) ? bytesFromBase646(object.image_jpeg) : void 0,
      caption: isSet15(object.caption) ? globalThis.String(object.caption) : void 0,
      footer: isSet15(object.footer) ? globalThis.String(object.footer) : void 0,
      detail: isSet15(object.detail) ? globalThis.String(object.detail) : void 0,
      summary: isSet15(object.summary) ? globalThis.String(object.summary) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.subtitle !== void 0) {
      obj.subtitle = message.subtitle;
    }
    if (message.body !== void 0) {
      obj.body = message.body;
    }
    if (message.imageJpeg !== void 0) {
      obj.imageJpeg = base64FromBytes6(message.imageJpeg);
    }
    if (message.caption !== void 0) {
      obj.caption = message.caption;
    }
    if (message.footer !== void 0) {
      obj.footer = message.footer;
    }
    if (message.detail !== void 0) {
      obj.detail = message.detail;
    }
    if (message.summary !== void 0) {
      obj.summary = message.summary;
    }
    return obj;
  },
  create(base) {
    return MiniAppPreview.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMiniAppPreview();
    message.title = object.title ?? "";
    message.subtitle = object.subtitle ?? void 0;
    message.body = object.body ?? void 0;
    message.imageJpeg = object.imageJpeg ?? void 0;
    message.caption = object.caption ?? void 0;
    message.footer = object.footer ?? void 0;
    message.detail = object.detail ?? void 0;
    message.summary = object.summary ?? void 0;
    return message;
  }
};
function createBaseMiniAppLayout() {
  return {
    caption: void 0,
    subcaption: void 0,
    trailingCaption: void 0,
    trailingSubcaption: void 0,
    image: void 0,
    imageTitle: void 0,
    imageSubtitle: void 0,
    summary: void 0
  };
}
var MiniAppLayout = {
  encode(message, writer = new BinaryWriter()) {
    if (message.caption !== void 0) {
      writer.uint32(42).string(message.caption);
    }
    if (message.subcaption !== void 0) {
      writer.uint32(26).string(message.subcaption);
    }
    if (message.trailingCaption !== void 0) {
      writer.uint32(50).string(message.trailingCaption);
    }
    if (message.trailingSubcaption !== void 0) {
      writer.uint32(58).string(message.trailingSubcaption);
    }
    if (message.image !== void 0) {
      writer.uint32(34).bytes(message.image);
    }
    if (message.imageTitle !== void 0) {
      writer.uint32(10).string(message.imageTitle);
    }
    if (message.imageSubtitle !== void 0) {
      writer.uint32(18).string(message.imageSubtitle);
    }
    if (message.summary !== void 0) {
      writer.uint32(66).string(message.summary);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMiniAppLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.caption = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.subcaption = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.trailingCaption = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.trailingSubcaption = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.image = reader.bytes();
          continue;
        }
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.imageTitle = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.imageSubtitle = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      caption: isSet15(object.caption) ? globalThis.String(object.caption) : void 0,
      subcaption: isSet15(object.subcaption) ? globalThis.String(object.subcaption) : void 0,
      trailingCaption: isSet15(object.trailingCaption) ? globalThis.String(object.trailingCaption) : isSet15(object.trailing_caption) ? globalThis.String(object.trailing_caption) : void 0,
      trailingSubcaption: isSet15(object.trailingSubcaption) ? globalThis.String(object.trailingSubcaption) : isSet15(object.trailing_subcaption) ? globalThis.String(object.trailing_subcaption) : void 0,
      image: isSet15(object.image) ? bytesFromBase646(object.image) : void 0,
      imageTitle: isSet15(object.imageTitle) ? globalThis.String(object.imageTitle) : isSet15(object.image_title) ? globalThis.String(object.image_title) : void 0,
      imageSubtitle: isSet15(object.imageSubtitle) ? globalThis.String(object.imageSubtitle) : isSet15(object.image_subtitle) ? globalThis.String(object.image_subtitle) : void 0,
      summary: isSet15(object.summary) ? globalThis.String(object.summary) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.caption !== void 0) {
      obj.caption = message.caption;
    }
    if (message.subcaption !== void 0) {
      obj.subcaption = message.subcaption;
    }
    if (message.trailingCaption !== void 0) {
      obj.trailingCaption = message.trailingCaption;
    }
    if (message.trailingSubcaption !== void 0) {
      obj.trailingSubcaption = message.trailingSubcaption;
    }
    if (message.image !== void 0) {
      obj.image = base64FromBytes6(message.image);
    }
    if (message.imageTitle !== void 0) {
      obj.imageTitle = message.imageTitle;
    }
    if (message.imageSubtitle !== void 0) {
      obj.imageSubtitle = message.imageSubtitle;
    }
    if (message.summary !== void 0) {
      obj.summary = message.summary;
    }
    return obj;
  },
  create(base) {
    return MiniAppLayout.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMiniAppLayout();
    message.caption = object.caption ?? void 0;
    message.subcaption = object.subcaption ?? void 0;
    message.trailingCaption = object.trailingCaption ?? void 0;
    message.trailingSubcaption = object.trailingSubcaption ?? void 0;
    message.image = object.image ?? void 0;
    message.imageTitle = object.imageTitle ?? void 0;
    message.imageSubtitle = object.imageSubtitle ?? void 0;
    message.summary = object.summary ?? void 0;
    return message;
  }
};
function createBaseSendCustomizedMiniAppMessageRequest() {
  return {
    chatGuid: "",
    teamId: "",
    extensionBundleId: "",
    appName: "",
    url: "",
    layout: void 0,
    appStoreId: void 0,
    clientMessageId: void 0
  };
}
var SendCustomizedMiniAppMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.teamId !== "") {
      writer.uint32(18).string(message.teamId);
    }
    if (message.extensionBundleId !== "") {
      writer.uint32(26).string(message.extensionBundleId);
    }
    if (message.appName !== "") {
      writer.uint32(34).string(message.appName);
    }
    if (message.url !== "") {
      writer.uint32(42).string(message.url);
    }
    if (message.layout !== void 0) {
      MiniAppLayout.encode(message.layout, writer.uint32(50).fork()).join();
    }
    if (message.appStoreId !== void 0) {
      writer.uint32(56).int64(message.appStoreId);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendCustomizedMiniAppMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.teamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.extensionBundleId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.appName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.url = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.layout = MiniAppLayout.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.appStoreId = longToNumber7(reader.int64());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      teamId: isSet15(object.teamId) ? globalThis.String(object.teamId) : isSet15(object.team_id) ? globalThis.String(object.team_id) : "",
      extensionBundleId: isSet15(object.extensionBundleId) ? globalThis.String(object.extensionBundleId) : isSet15(object.extension_bundle_id) ? globalThis.String(object.extension_bundle_id) : "",
      appName: isSet15(object.appName) ? globalThis.String(object.appName) : isSet15(object.app_name) ? globalThis.String(object.app_name) : "",
      url: isSet15(object.url) ? globalThis.String(object.url) : "",
      layout: isSet15(object.layout) ? MiniAppLayout.fromJSON(object.layout) : void 0,
      appStoreId: isSet15(object.appStoreId) ? globalThis.Number(object.appStoreId) : isSet15(object.app_store_id) ? globalThis.Number(object.app_store_id) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.teamId !== "") {
      obj.teamId = message.teamId;
    }
    if (message.extensionBundleId !== "") {
      obj.extensionBundleId = message.extensionBundleId;
    }
    if (message.appName !== "") {
      obj.appName = message.appName;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.layout !== void 0) {
      obj.layout = MiniAppLayout.toJSON(message.layout);
    }
    if (message.appStoreId !== void 0) {
      obj.appStoreId = Math.round(message.appStoreId);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendCustomizedMiniAppMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendCustomizedMiniAppMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.teamId = object.teamId ?? "";
    message.extensionBundleId = object.extensionBundleId ?? "";
    message.appName = object.appName ?? "";
    message.url = object.url ?? "";
    message.layout = object.layout !== void 0 && object.layout !== null ? MiniAppLayout.fromPartial(object.layout) : void 0;
    message.appStoreId = object.appStoreId ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseMessageResponse() {
  return { message: void 0 };
}
var MessageResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.message !== void 0) {
      Message.encode(message.message, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.message = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { message: isSet15(object.message) ? Message.fromJSON(object.message) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.message !== void 0) {
      obj.message = Message.toJSON(message.message);
    }
    return obj;
  },
  create(base) {
    return MessageResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageResponse();
    message.message = object.message !== void 0 && object.message !== null ? Message.fromPartial(object.message) : void 0;
    return message;
  }
};
function createBaseEditMessageRequest() {
  return { target: void 0, newText: "", backwardCompatText: void 0, clientMessageId: void 0 };
}
var EditMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.newText !== "") {
      writer.uint32(18).string(message.newText);
    }
    if (message.backwardCompatText !== void 0) {
      writer.uint32(26).string(message.backwardCompatText);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEditMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.newText = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.backwardCompatText = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet15(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      newText: isSet15(object.newText) ? globalThis.String(object.newText) : isSet15(object.new_text) ? globalThis.String(object.new_text) : "",
      backwardCompatText: isSet15(object.backwardCompatText) ? globalThis.String(object.backwardCompatText) : isSet15(object.backward_compat_text) ? globalThis.String(object.backward_compat_text) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.newText !== "") {
      obj.newText = message.newText;
    }
    if (message.backwardCompatText !== void 0) {
      obj.backwardCompatText = message.backwardCompatText;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return EditMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseEditMessageRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.newText = object.newText ?? "";
    message.backwardCompatText = object.backwardCompatText ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseUnsendMessageRequest() {
  return { target: void 0, clientMessageId: void 0 };
}
var UnsendMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUnsendMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet15(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return UnsendMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUnsendMessageRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSetReactionRequest() {
  return { target: void 0, reaction: void 0, isSet: false, clientMessageId: void 0 };
}
var SetReactionRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.reaction !== void 0) {
      MessageReaction.encode(message.reaction, writer.uint32(18).fork()).join();
    }
    if (message.isSet !== false) {
      writer.uint32(24).bool(message.isSet);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetReactionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.reaction = MessageReaction.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.isSet = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet15(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      reaction: isSet15(object.reaction) ? MessageReaction.fromJSON(object.reaction) : void 0,
      isSet: isSet15(object.isSet) ? globalThis.Boolean(object.isSet) : isSet15(object.is_set) ? globalThis.Boolean(object.is_set) : false,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.reaction !== void 0) {
      obj.reaction = MessageReaction.toJSON(message.reaction);
    }
    if (message.isSet !== false) {
      obj.isSet = message.isSet;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SetReactionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetReactionRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.reaction = object.reaction !== void 0 && object.reaction !== null ? MessageReaction.fromPartial(object.reaction) : void 0;
    message.isSet = object.isSet ?? false;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBasePlaceStickerRequest() {
  return { target: void 0, sticker: void 0, placement: void 0, clientMessageId: void 0 };
}
var PlaceStickerRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.sticker !== void 0) {
      AttachmentRef.encode(message.sticker, writer.uint32(18).fork()).join();
    }
    if (message.placement !== void 0) {
      StickerPlacement.encode(message.placement, writer.uint32(26).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePlaceStickerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.sticker = AttachmentRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.placement = StickerPlacement.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet15(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      sticker: isSet15(object.sticker) ? AttachmentRef.fromJSON(object.sticker) : void 0,
      placement: isSet15(object.placement) ? StickerPlacement.fromJSON(object.placement) : void 0,
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.sticker !== void 0) {
      obj.sticker = AttachmentRef.toJSON(message.sticker);
    }
    if (message.placement !== void 0) {
      obj.placement = StickerPlacement.toJSON(message.placement);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return PlaceStickerRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePlaceStickerRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.sticker = object.sticker !== void 0 && object.sticker !== null ? AttachmentRef.fromPartial(object.sticker) : void 0;
    message.placement = object.placement !== void 0 && object.placement !== null ? StickerPlacement.fromPartial(object.placement) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseNotifySilencedMessageRequest() {
  return { chatGuid: "", messageGuid: "", clientMessageId: void 0 };
}
var NotifySilencedMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseNotifySilencedMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      messageGuid: isSet15(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet15(object.message_guid) ? globalThis.String(object.message_guid) : "",
      clientMessageId: isSet15(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet15(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return NotifySilencedMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseNotifySilencedMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.messageGuid = object.messageGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseGetMessageRequest() {
  return { messageGuid: "" };
}
var GetMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet15(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet15(object.message_guid) ? globalThis.String(object.message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    return obj;
  },
  create(base) {
    return GetMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetMessageRequest();
    message.messageGuid = object.messageGuid ?? "";
    return message;
  }
};
function createBaseGetMessageResponse() {
  return { message: void 0 };
}
var GetMessageResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.message !== void 0) {
      Message.encode(message.message, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.message = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { message: isSet15(object.message) ? Message.fromJSON(object.message) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.message !== void 0) {
      obj.message = Message.toJSON(message.message);
    }
    return obj;
  },
  create(base) {
    return GetMessageResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetMessageResponse();
    message.message = object.message !== void 0 && object.message !== null ? Message.fromPartial(object.message) : void 0;
    return message;
  }
};
function createBaseListRecentMessagesRequest() {
  return {
    pageSize: void 0,
    pageToken: void 0,
    isFromMe: void 0,
    isRead: void 0,
    before: void 0,
    after: void 0
  };
}
var ListRecentMessagesRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pageSize !== void 0) {
      writer.uint32(8).int32(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      writer.uint32(18).string(message.pageToken);
    }
    if (message.isFromMe !== void 0) {
      writer.uint32(24).bool(message.isFromMe);
    }
    if (message.isRead !== void 0) {
      writer.uint32(32).bool(message.isRead);
    }
    if (message.before !== void 0) {
      Timestamp.encode(toTimestamp6(message.before), writer.uint32(42).fork()).join();
    }
    if (message.after !== void 0) {
      Timestamp.encode(toTimestamp6(message.after), writer.uint32(50).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListRecentMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.pageSize = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.pageToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isRead = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.before = fromTimestamp6(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.after = fromTimestamp6(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pageSize: isSet15(object.pageSize) ? globalThis.Number(object.pageSize) : isSet15(object.page_size) ? globalThis.Number(object.page_size) : void 0,
      pageToken: isSet15(object.pageToken) ? globalThis.String(object.pageToken) : isSet15(object.page_token) ? globalThis.String(object.page_token) : void 0,
      isFromMe: isSet15(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet15(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : void 0,
      isRead: isSet15(object.isRead) ? globalThis.Boolean(object.isRead) : isSet15(object.is_read) ? globalThis.Boolean(object.is_read) : void 0,
      before: isSet15(object.before) ? fromJsonTimestamp6(object.before) : void 0,
      after: isSet15(object.after) ? fromJsonTimestamp6(object.after) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pageSize !== void 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      obj.pageToken = message.pageToken;
    }
    if (message.isFromMe !== void 0) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.isRead !== void 0) {
      obj.isRead = message.isRead;
    }
    if (message.before !== void 0) {
      obj.before = message.before.toISOString();
    }
    if (message.after !== void 0) {
      obj.after = message.after.toISOString();
    }
    return obj;
  },
  create(base) {
    return ListRecentMessagesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListRecentMessagesRequest();
    message.pageSize = object.pageSize ?? void 0;
    message.pageToken = object.pageToken ?? void 0;
    message.isFromMe = object.isFromMe ?? void 0;
    message.isRead = object.isRead ?? void 0;
    message.before = object.before ?? void 0;
    message.after = object.after ?? void 0;
    return message;
  }
};
function createBaseListRecentMessagesResponse() {
  return { messages: [], nextPageToken: void 0 };
}
var ListRecentMessagesResponse = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.messages) {
      Message.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== void 0) {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListRecentMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messages.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: globalThis.Array.isArray(object?.messages) ? object.messages.map((e) => Message.fromJSON(e)) : [],
      nextPageToken: isSet15(object.nextPageToken) ? globalThis.String(object.nextPageToken) : isSet15(object.next_page_token) ? globalThis.String(object.next_page_token) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => Message.toJSON(e));
    }
    if (message.nextPageToken !== void 0) {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },
  create(base) {
    return ListRecentMessagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListRecentMessagesResponse();
    message.messages = object.messages?.map((e) => Message.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? void 0;
    return message;
  }
};
function createBaseListChatMessagesRequest() {
  return {
    chatGuid: "",
    pageSize: void 0,
    pageToken: void 0,
    isFromMe: void 0,
    isRead: void 0,
    before: void 0,
    after: void 0
  };
}
var ListChatMessagesRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.pageSize !== void 0) {
      writer.uint32(16).int32(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      writer.uint32(26).string(message.pageToken);
    }
    if (message.isFromMe !== void 0) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.isRead !== void 0) {
      writer.uint32(40).bool(message.isRead);
    }
    if (message.before !== void 0) {
      Timestamp.encode(toTimestamp6(message.before), writer.uint32(50).fork()).join();
    }
    if (message.after !== void 0) {
      Timestamp.encode(toTimestamp6(message.after), writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListChatMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.pageSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.pageToken = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isRead = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.before = fromTimestamp6(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.after = fromTimestamp6(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      pageSize: isSet15(object.pageSize) ? globalThis.Number(object.pageSize) : isSet15(object.page_size) ? globalThis.Number(object.page_size) : void 0,
      pageToken: isSet15(object.pageToken) ? globalThis.String(object.pageToken) : isSet15(object.page_token) ? globalThis.String(object.page_token) : void 0,
      isFromMe: isSet15(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet15(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : void 0,
      isRead: isSet15(object.isRead) ? globalThis.Boolean(object.isRead) : isSet15(object.is_read) ? globalThis.Boolean(object.is_read) : void 0,
      before: isSet15(object.before) ? fromJsonTimestamp6(object.before) : void 0,
      after: isSet15(object.after) ? fromJsonTimestamp6(object.after) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.pageSize !== void 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      obj.pageToken = message.pageToken;
    }
    if (message.isFromMe !== void 0) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.isRead !== void 0) {
      obj.isRead = message.isRead;
    }
    if (message.before !== void 0) {
      obj.before = message.before.toISOString();
    }
    if (message.after !== void 0) {
      obj.after = message.after.toISOString();
    }
    return obj;
  },
  create(base) {
    return ListChatMessagesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListChatMessagesRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.pageSize = object.pageSize ?? void 0;
    message.pageToken = object.pageToken ?? void 0;
    message.isFromMe = object.isFromMe ?? void 0;
    message.isRead = object.isRead ?? void 0;
    message.before = object.before ?? void 0;
    message.after = object.after ?? void 0;
    return message;
  }
};
function createBaseListChatMessagesResponse() {
  return { messages: [], nextPageToken: void 0 };
}
var ListChatMessagesResponse = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.messages) {
      Message.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== void 0) {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListChatMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messages.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: globalThis.Array.isArray(object?.messages) ? object.messages.map((e) => Message.fromJSON(e)) : [],
      nextPageToken: isSet15(object.nextPageToken) ? globalThis.String(object.nextPageToken) : isSet15(object.next_page_token) ? globalThis.String(object.next_page_token) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => Message.toJSON(e));
    }
    if (message.nextPageToken !== void 0) {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },
  create(base) {
    return ListChatMessagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListChatMessagesResponse();
    message.messages = object.messages?.map((e) => Message.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? void 0;
    return message;
  }
};
function createBaseGetEmbeddedMediaRequest() {
  return { chatGuid: "", messageGuid: "" };
}
var GetEmbeddedMediaRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetEmbeddedMediaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      messageGuid: isSet15(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet15(object.message_guid) ? globalThis.String(object.message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    return obj;
  },
  create(base) {
    return GetEmbeddedMediaRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetEmbeddedMediaRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.messageGuid = object.messageGuid ?? "";
    return message;
  }
};
function createBaseGetEmbeddedMediaResponse() {
  return { media: void 0 };
}
var GetEmbeddedMediaResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.media !== void 0) {
      EmbeddedMedia.encode(message.media, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetEmbeddedMediaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.media = EmbeddedMedia.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { media: isSet15(object.media) ? EmbeddedMedia.fromJSON(object.media) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.media !== void 0) {
      obj.media = EmbeddedMedia.toJSON(message.media);
    }
    return obj;
  },
  create(base) {
    return GetEmbeddedMediaResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetEmbeddedMediaResponse();
    message.media = object.media !== void 0 && object.media !== null ? EmbeddedMedia.fromPartial(object.media) : void 0;
    return message;
  }
};
function createBaseSubscribeMessageEventsRequest() {
  return { chatGuid: void 0 };
}
var SubscribeMessageEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== void 0) {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeMessageEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet15(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet15(object.chat_guid) ? globalThis.String(object.chat_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== void 0) {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribeMessageEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeMessageEventsRequest();
    message.chatGuid = object.chatGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribeMessageEventsResponse() {
  return { sequence: void 0, messageChanged: void 0, heartbeat: void 0 };
}
var SubscribeMessageEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.messageChanged !== void 0) {
      MessageChangeEvent.encode(message.messageChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeMessageEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber7(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.messageChanged = MessageChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet15(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      messageChanged: isSet15(object.messageChanged) ? MessageChangeEvent.fromJSON(object.messageChanged) : isSet15(object.message_changed) ? MessageChangeEvent.fromJSON(object.message_changed) : void 0,
      heartbeat: isSet15(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.messageChanged !== void 0) {
      obj.messageChanged = MessageChangeEvent.toJSON(message.messageChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribeMessageEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeMessageEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.messageChanged = object.messageChanged !== void 0 && object.messageChanged !== null ? MessageChangeEvent.fromPartial(object.messageChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var MessageServiceDefinition = {
  name: "MessageService",
  fullName: "photon.imessage.v1.MessageService",
  methods: {
    /** Writes */
    sendTextMessage: {
      name: "SendTextMessage",
      requestType: SendTextMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    sendAttachmentMessage: {
      name: "SendAttachmentMessage",
      requestType: SendAttachmentMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    sendMultipartMessage: {
      name: "SendMultipartMessage",
      requestType: SendMultipartMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    sendMiniAppMessage: {
      name: "SendMiniAppMessage",
      requestType: SendMiniAppMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    /** Sends an iMessage mini-app card backed by the caller's own extension. */
    sendCustomizedMiniAppMessage: {
      name: "SendCustomizedMiniAppMessage",
      requestType: SendCustomizedMiniAppMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    editMessage: {
      name: "EditMessage",
      requestType: EditMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    /** Retracts an existing message and returns Empty after helper success. */
    unsendMessage: {
      name: "UnsendMessage",
      requestType: UnsendMessageRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    setReaction: {
      name: "SetReaction",
      requestType: SetReactionRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    placeSticker: {
      name: "PlaceSticker",
      requestType: PlaceStickerRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {}
    },
    /** Triggers Apple's per-message "Notify Anyway" action. */
    notifySilencedMessage: {
      name: "NotifySilencedMessage",
      requestType: NotifySilencedMessageRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    /** Reads */
    getMessage: {
      name: "GetMessage",
      requestType: GetMessageRequest,
      requestStream: false,
      responseType: GetMessageResponse,
      responseStream: false,
      options: {}
    },
    listRecentMessages: {
      name: "ListRecentMessages",
      requestType: ListRecentMessagesRequest,
      requestStream: false,
      responseType: ListRecentMessagesResponse,
      responseStream: false,
      options: {}
    },
    listChatMessages: {
      name: "ListChatMessages",
      requestType: ListChatMessagesRequest,
      requestStream: false,
      responseType: ListChatMessagesResponse,
      responseStream: false,
      options: {}
    },
    getEmbeddedMedia: {
      name: "GetEmbeddedMedia",
      requestType: GetEmbeddedMediaRequest,
      requestStream: false,
      responseType: GetEmbeddedMediaResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Live durable-event subscription. Pair with `EventService.CatchUpEvents`
     * for gap-free history-then-live consumption.
     */
    subscribeMessageEvents: {
      name: "SubscribeMessageEvents",
      requestType: SubscribeMessageEventsRequest,
      requestStream: false,
      responseType: SubscribeMessageEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase646(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes6(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function toTimestamp6(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp6(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp6(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp6(Timestamp.fromJSON(o));
  }
}
function longToNumber7(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet15(value) {
  return value !== null && value !== void 0;
}
function createBaseCreatePollRequest() {
  return { chatGuid: "", title: "", options: [], clientMessageId: void 0 };
}
var CreatePollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    for (const v of message.options) {
      writer.uint32(26).string(v);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreatePollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.options.push(reader.string());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet16(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet16(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      title: isSet16(object.title) ? globalThis.String(object.title) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e) => globalThis.String(e)) : [],
      clientMessageId: isSet16(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet16(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.options?.length) {
      obj.options = message.options;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return CreatePollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreatePollRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.title = object.title ?? "";
    message.options = object.options?.map((e) => e) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBasePollResponse() {
  return { poll: void 0 };
}
var PollResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.poll !== void 0) {
      PollInfo.encode(message.poll, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.poll = PollInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { poll: isSet16(object.poll) ? PollInfo.fromJSON(object.poll) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.poll !== void 0) {
      obj.poll = PollInfo.toJSON(message.poll);
    }
    return obj;
  },
  create(base) {
    return PollResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollResponse();
    message.poll = object.poll !== void 0 && object.poll !== null ? PollInfo.fromPartial(object.poll) : void 0;
    return message;
  }
};
function createBaseVotePollRequest() {
  return { pollMessageGuid: "", optionIdentifier: "", clientMessageId: void 0 };
}
var VotePollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.optionIdentifier !== "") {
      writer.uint32(18).string(message.optionIdentifier);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseVotePollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.optionIdentifier = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet16(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet16(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      optionIdentifier: isSet16(object.optionIdentifier) ? globalThis.String(object.optionIdentifier) : isSet16(object.option_identifier) ? globalThis.String(object.option_identifier) : "",
      clientMessageId: isSet16(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet16(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.optionIdentifier !== "") {
      obj.optionIdentifier = message.optionIdentifier;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return VotePollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseVotePollRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.optionIdentifier = object.optionIdentifier ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseUnvotePollRequest() {
  return { pollMessageGuid: "", clientMessageId: void 0 };
}
var UnvotePollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUnvotePollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet16(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet16(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      clientMessageId: isSet16(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet16(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return UnvotePollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUnvotePollRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseAddPollOptionRequest() {
  return { pollMessageGuid: "", optionText: "", clientMessageId: void 0 };
}
var AddPollOptionRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.optionText !== "") {
      writer.uint32(18).string(message.optionText);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddPollOptionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.optionText = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet16(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet16(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      optionText: isSet16(object.optionText) ? globalThis.String(object.optionText) : isSet16(object.option_text) ? globalThis.String(object.option_text) : "",
      clientMessageId: isSet16(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet16(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.optionText !== "") {
      obj.optionText = message.optionText;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return AddPollOptionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddPollOptionRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.optionText = object.optionText ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseGetPollRequest() {
  return { pollMessageGuid: "" };
}
var GetPollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetPollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet16(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet16(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    return obj;
  },
  create(base) {
    return GetPollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetPollRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    return message;
  }
};
function createBaseSubscribePollEventsRequest() {
  return { pollMessageGuid: void 0 };
}
var SubscribePollEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== void 0) {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribePollEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet16(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet16(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== void 0) {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribePollEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribePollEventsRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribePollEventsResponse() {
  return { sequence: void 0, pollChanged: void 0, heartbeat: void 0 };
}
var SubscribePollEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.pollChanged !== void 0) {
      PollChangeEvent.encode(message.pollChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribePollEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber8(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.pollChanged = PollChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet16(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      pollChanged: isSet16(object.pollChanged) ? PollChangeEvent.fromJSON(object.pollChanged) : isSet16(object.poll_changed) ? PollChangeEvent.fromJSON(object.poll_changed) : void 0,
      heartbeat: isSet16(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.pollChanged !== void 0) {
      obj.pollChanged = PollChangeEvent.toJSON(message.pollChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribePollEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribePollEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.pollChanged = object.pollChanged !== void 0 && object.pollChanged !== null ? PollChangeEvent.fromPartial(object.pollChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var PollServiceDefinition = {
  name: "PollService",
  fullName: "photon.imessage.v1.PollService",
  methods: {
    /** Writes */
    createPoll: {
      name: "CreatePoll",
      requestType: CreatePollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {}
    },
    votePoll: {
      name: "VotePoll",
      requestType: VotePollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {}
    },
    unvotePoll: {
      name: "UnvotePoll",
      requestType: UnvotePollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {}
    },
    addPollOption: {
      name: "AddPollOption",
      requestType: AddPollOptionRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {}
    },
    /** Reads */
    getPoll: {
      name: "GetPoll",
      requestType: GetPollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {}
    },
    /** Streams */
    subscribePollEvents: {
      name: "SubscribePollEvents",
      requestType: SubscribePollEventsRequest,
      requestStream: false,
      responseType: SubscribePollEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function longToNumber8(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet16(value) {
  return value !== null && value !== void 0;
}

// src/utils/idempotency.ts
function generateIdempotencyKey() {
  return crypto.randomUUID();
}

// src/utils/retry.ts
var DEFAULT_RETRY_OPTIONS = {
  maxAttempts: 4,
  initialDelay: 200,
  maxDelay: 5e3
};

// src/utils/sleep.ts
function sleep(ms, signal) {
  return new Promise((resolve) => {
    if (signal?.aborted) {
      resolve();
      return;
    }
    const onAbort = () => {
      clearTimeout(timer);
      resolve();
    };
    const timer = setTimeout(() => {
      signal?.removeEventListener("abort", onAbort);
      resolve();
    }, ms);
    signal?.addEventListener("abort", onAbort, { once: true });
  });
}

// src/transport/metadata.ts
function authMiddleware(token) {
  return async function* authMw(call, options) {
    const resolvedToken = typeof token === "function" ? await token() : token;
    const metadata = Metadata(options.metadata);
    metadata.set("authorization", `Bearer ${resolvedToken}`);
    const nextOptions = {
      ...options,
      metadata
    };
    return yield* call.next(call.request, nextOptions);
  };
}
var MUTATING_METHODS = /* @__PURE__ */ new Set([
  "/photon.imessage.v1.AttachmentService/UploadAttachment",
  "/photon.imessage.v1.ChatService/CreateChat",
  "/photon.imessage.v1.ChatService/MarkChatRead",
  "/photon.imessage.v1.ChatService/RemoveBackground",
  "/photon.imessage.v1.ChatService/SetBackground",
  "/photon.imessage.v1.ChatService/SetTyping",
  "/photon.imessage.v1.ChatService/ShareContactInfo",
  "/photon.imessage.v1.GroupService/AddParticipants",
  "/photon.imessage.v1.GroupService/LeaveGroup",
  "/photon.imessage.v1.GroupService/RemoveIcon",
  "/photon.imessage.v1.GroupService/RemoveParticipants",
  "/photon.imessage.v1.GroupService/SetDisplayName",
  "/photon.imessage.v1.GroupService/SetIcon",
  "/photon.imessage.v1.LocationService/RequestFriendLocationSharing",
  "/photon.imessage.v1.MessageService/EditMessage",
  "/photon.imessage.v1.MessageService/NotifySilencedMessage",
  "/photon.imessage.v1.MessageService/PlaceSticker",
  "/photon.imessage.v1.MessageService/SendAttachmentMessage",
  "/photon.imessage.v1.MessageService/SendMultipartMessage",
  "/photon.imessage.v1.MessageService/SendTextMessage",
  "/photon.imessage.v1.MessageService/SetReaction",
  "/photon.imessage.v1.MessageService/UnsendMessage",
  "/photon.imessage.v1.PollService/AddPollOption",
  "/photon.imessage.v1.PollService/CreatePoll",
  "/photon.imessage.v1.PollService/UnvotePoll",
  "/photon.imessage.v1.PollService/VotePoll"
]);
function isMutatingMethod(path) {
  return MUTATING_METHODS.has(path);
}
function idempotencyMiddleware() {
  return async function* idempotencyMw(call, options) {
    if (call.method.options.idempotencyLevel === "NO_SIDE_EFFECTS" || call.method.options.idempotencyLevel === "IDEMPOTENT" || !isMutatingMethod(call.method.path)) {
      return yield* call.next(call.request, options);
    }
    const metadata = Metadata(options.metadata);
    metadata.set("x-idempotency-key", generateIdempotencyKey());
    const nextOptions = {
      ...options,
      metadata
    };
    return yield* call.next(call.request, nextOptions);
  };
}
function retryMiddleware(opts = {}) {
  const maxAttempts = Math.max(
    1,
    opts.maxAttempts ?? DEFAULT_RETRY_OPTIONS.maxAttempts
  );
  const initialDelay = opts.initialDelay ?? DEFAULT_RETRY_OPTIONS.initialDelay;
  const maxDelay = opts.maxDelay ?? DEFAULT_RETRY_OPTIONS.maxDelay;
  return async function* retryMw(call, options) {
    if (call.method.responseStream || call.method.requestStream) {
      return yield* call.next(call.request, options);
    }
    let lastError;
    for (let attempt = 0; attempt < maxAttempts; attempt++) {
      try {
        return yield* call.next(call.request, options);
      } catch (error) {
        lastError = error;
        const retryable = readMetadataValue(error, "x-retryable") === "true";
        if (!retryable || attempt >= maxAttempts - 1) {
          throw error;
        }
        const exponentialDelay = initialDelay * 2 ** attempt;
        const cappedDelay = Math.min(exponentialDelay, maxDelay);
        await sleep(Math.random() * cappedDelay, options.signal);
        if (options.signal?.aborted) {
          throw error;
        }
      }
    }
    throw lastError;
  };
}
function trailingMetadataCaptureMiddleware() {
  return async function* trailingMetadataCaptureMw(call, options) {
    let trailer;
    try {
      return yield* call.next(call.request, {
        ...options,
        onTrailer(t) {
          trailer = t;
          options.onTrailer?.(t);
        }
      });
    } catch (error) {
      await Promise.resolve();
      if (trailer && error instanceof Error) {
        Object.defineProperty(error, "metadata", {
          value: trailer,
          writable: true,
          configurable: true
        });
      }
      throw error;
    }
  };
}
function timeoutMiddleware(timeoutMs) {
  return async function* timeoutMw(call, options) {
    if (call.method.responseStream || call.method.requestStream) {
      return yield* call.next(call.request, options);
    }
    if (options.signal) {
      const combined = AbortSignal.any([
        options.signal,
        AbortSignal.timeout(timeoutMs)
      ]);
      return yield* call.next(call.request, {
        ...options,
        signal: combined
      });
    }
    return yield* call.next(call.request, {
      ...options,
      signal: AbortSignal.timeout(timeoutMs)
    });
  };
}

// src/transport/grpc-client.ts
var MAX_MESSAGE_BYTES = 100 * 1024 * 1024;
var KEEPALIVE_TIME_MS = 3e4;
var KEEPALIVE_TIMEOUT_MS = 2e4;
var KEEPALIVE_PERMIT_WITHOUT_CALLS = 1;
var KEEPALIVE_MAX_PINGS_WITHOUT_DATA = 0;
function createGrpcClients(options) {
  const credentials = options.tls ?? true ? ChannelCredentials.createSsl() : ChannelCredentials.createInsecure();
  const channelOptions = {
    "grpc.max_receive_message_length": MAX_MESSAGE_BYTES,
    "grpc.max_send_message_length": MAX_MESSAGE_BYTES,
    "grpc.keepalive_time_ms": KEEPALIVE_TIME_MS,
    "grpc.keepalive_timeout_ms": KEEPALIVE_TIMEOUT_MS,
    "grpc.keepalive_permit_without_calls": KEEPALIVE_PERMIT_WITHOUT_CALLS,
    "grpc.http2.max_pings_without_data": KEEPALIVE_MAX_PINGS_WITHOUT_DATA,
    ...options.channelOptions
  };
  const channel = createChannel(options.address, credentials, channelOptions);
  const streamChannel = createChannel(
    options.address,
    credentials,
    channelOptions
  );
  let factory = createClientFactory();
  if (options.autoIdempotency) {
    factory = factory.use(idempotencyMiddleware());
  }
  if (options.retry) {
    const retryOpts = options.retry === true ? {} : options.retry;
    factory = factory.use(retryMiddleware(retryOpts));
  }
  if (options.timeout) {
    factory = factory.use(timeoutMiddleware(options.timeout));
  }
  if (options.token) {
    factory = factory.use(authMiddleware(options.token));
  }
  factory = factory.use(trailingMetadataCaptureMiddleware());
  return {
    messages: factory.create(MessageServiceDefinition, channel),
    messagesStream: factory.create(MessageServiceDefinition, streamChannel),
    chats: factory.create(ChatServiceDefinition, channel),
    chatsStream: factory.create(ChatServiceDefinition, streamChannel),
    events: factory.create(EventServiceDefinition, streamChannel),
    groups: factory.create(GroupServiceDefinition, channel),
    groupsStream: factory.create(GroupServiceDefinition, streamChannel),
    attachments: factory.create(AttachmentServiceDefinition, channel),
    attachmentsStream: factory.create(
      AttachmentServiceDefinition,
      streamChannel
    ),
    addresses: factory.create(AddressServiceDefinition, channel),
    polls: factory.create(PollServiceDefinition, channel),
    pollsStream: factory.create(PollServiceDefinition, streamChannel),
    locations: factory.create(LocationServiceDefinition, channel),
    locationsStream: factory.create(LocationServiceDefinition, streamChannel),
    channel,
    streamChannel
  };
}

// src/client.ts
function createClient(options) {
  const clients = createGrpcClients({
    address: options.address,
    autoIdempotency: options.autoIdempotency,
    channelOptions: options.channelOptions,
    retry: options.retry,
    timeout: options.timeout,
    tls: options.tls,
    token: options.token
  });
  const onHeartbeat = options.onHeartbeat;
  const messages = new MessagesResource(
    clients.messages,
    clients.messagesStream,
    onHeartbeat
  );
  const chats = new ChatsResource(clients.chats, clients.chatsStream, onHeartbeat);
  const events = new EventsResource(clients.events);
  const groups = new GroupsResource(
    clients.groups,
    clients.groupsStream,
    onHeartbeat
  );
  const attachments = new AttachmentsResource(
    clients.attachments,
    clients.attachmentsStream
  );
  const addresses = new AddressesResource(clients.addresses);
  const polls = new PollsResource(clients.polls, clients.pollsStream, onHeartbeat);
  const locations = new LocationsResource(
    clients.locations,
    clients.locationsStream,
    onHeartbeat
  );
  function close() {
    clients.channel.close();
    clients.streamChannel.close();
    return Promise.resolve();
  }
  return {
    messages,
    chats,
    groups,
    attachments,
    addresses,
    polls,
    locations,
    events,
    close,
    async [Symbol.asyncDispose]() {
      await close();
    }
  };
}

// src/types/effects.ts
var MessageEffect = {
  slam: "com.apple.MobileSMS.expressivesend.impact",
  loud: "com.apple.MobileSMS.expressivesend.loud",
  gentle: "com.apple.MobileSMS.expressivesend.gentle",
  invisible: "com.apple.MobileSMS.expressivesend.invisibleink",
  confetti: "com.apple.messages.effect.CKConfettiEffect",
  fireworks: "com.apple.messages.effect.CKFireworksEffect",
  balloons: "com.apple.messages.effect.CKBalloonEffect",
  heart: "com.apple.messages.effect.CKHeartEffect",
  lasers: "com.apple.messages.effect.CKLasersEffect",
  celebration: "com.apple.messages.effect.CKHappyBirthdayEffect",
  sparkles: "com.apple.messages.effect.CKSparklesEffect",
  spotlight: "com.apple.messages.effect.CKSpotlightEffect",
  echo: "com.apple.messages.effect.CKEchoEffect"
};
var TextEffect = {
  big: "big",
  small: "small",
  shake: "shake",
  nod: "nod",
  explode: "explode",
  ripple: "ripple",
  bloom: "bloom",
  jitter: "jitter"
};

// src/types/errors.ts
var ErrorCode = {
  // Authentication / authorization
  unauthenticated: "unauthenticated",
  tokenExpired: "tokenExpired",
  tokenBlocked: "tokenBlocked",
  unauthorized: "unauthorized",
  // Rate limiting
  dailyLimitExceeded: "dailyLimitExceeded",
  recipientLimitExceeded: "recipientLimitExceeded",
  uploadRateExceeded: "uploadRateExceeded",
  contentDuplicateExceeded: "contentDuplicateExceeded",
  recipientCoolingDown: "recipientCoolingDown",
  recipientLocked: "recipientLocked",
  sendReceiveRatioExceeded: "sendReceiveRatioExceeded",
  // Duplicate
  duplicateMessage: "duplicateMessage",
  // Not found
  chatNotFound: "chatNotFound",
  messageNotFound: "messageNotFound",
  attachmentNotFound: "attachmentNotFound",
  addressNotFound: "addressNotFound",
  sharedFriendLocationNotFound: "sharedFriendLocationNotFound",
  groupIconNotFound: "groupIconNotFound",
  pollNotFound: "pollNotFound",
  // Validation / precondition
  invalidArgument: "invalidArgument",
  preconditionFailed: "preconditionFailed",
  operationNotSupported: "operationNotSupported",
  attachmentNotReady: "attachmentNotReady",
  privateApiUnavailable: "privateApiUnavailable",
  // Infrastructure
  serviceUnavailable: "serviceUnavailable",
  timeout: "timeout",
  internalError: "internalError",
  databaseError: "databaseError",
  networkError: "networkError"
};

export { AuthenticationError, ConnectionError, ErrorCode, IMessageError, MessageEffect, NotFoundError, RateLimitError, TextEffect, TypedEventStream, ValidationError, createClient };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map